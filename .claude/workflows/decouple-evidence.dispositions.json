{
 "dispositions": [
  {
   "locator": "\u00a71 Executive summary, line 17",
   "excerpt": "By audit-finding category, roughly three-quarters of findings fall in this mechanical class (a categorization estimate, not a measured rework-time share \u2014 see Appendix A).",
   "type": "repo_metric",
   "design_claim": "In an unguarded probabilistic pipeline, the large majority of defects are cheap, mechanical ones (missing sections, broken cross-refs, stale predecessors) that a deterministic check could catch, rather than substantive design flaws.",
   "action": "strip_keep_claim",
   "restatement": "The large majority of defects in an unguarded pipeline are cheap, mechanical ones \u2014 missing sections, broken cross-references, stale predecessors \u2014 exactly the class a deterministic check can catch, rather than substantive design flaws.",
   "target_note": "The 'see Appendix A' pointer and the 'three-quarters' figure are repo-specific; the design claim (mechanical defects dominate) is timeless.",
   "preserved": false,
   "loss": "The restatement keeps the qualitative claim (mechanical defects dominate) but silently DROPS the epistemic hedge that is the load-bearing part of this sentence. The original is careful to say 'roughly three-quarters ... a categorization estimate, not a measured rework-time share.' That parenthetical is not a citation \u2014 it is a rationale telling the reader how much to trust the claim and on what basis (category mapping, not measured time). The restatement asserts 'the large majority' as flat fact with no calibration. Worse, the RULE requires repo-specific evidence worth keeping to land in the plan or an evidence file; the 125-of-165 figure and the 'see Appendix A' provenance currently live ONLY in this doc (verified: grep finds them nowhere in implementation-plan.md). The disposition specifies no landing target, so the quantitative evidence and its strength-of-evidence caveat vanish entirely rather than relocating. A reader of the principles doc loses the 'how confident, and why' that the original deliberately supplied. To preserve: keep the 'categorization estimate, not measured rework-time' caveat in the restated claim, and relocate the 125/165 + Appendix A evidence to the plan or an evidence file."
  },
  {
   "locator": "\u00a72 Problem A, line 27",
   "excerpt": "Four of seven pre-handoff seams are ungated; one more relies only on a human approval gate.",
   "type": "repo_metric",
   "design_claim": "Validation between stages is uneven; many stage-to-stage handoff seams are ungated, and some rely only on a human approval gate rather than a machine check.",
   "action": "strip_keep_claim",
   "restatement": "Validation between stages is uneven: many stage-to-stage handoff seams are ungated, and some rely only on a human approval gate rather than a deterministic check.",
   "target_note": "Counts (four of seven, one more) are this repo's topology; the claim that seams are unevenly gated is general.",
   "preserved": true,
   "loss": "Low loss. The counts ('four of seven', 'one more') are genuinely this repo's pipeline topology, and the abstract claim \u2014 seams are unevenly gated, some rely only on a human approval gate rather than a deterministic check \u2014 fully survives in the restatement with no rationale dropped. The 'why it matters' (uneven validation lets the cheapest failures through ungated seams) is carried by the surrounding \u00a72 lede, not this bullet, so stripping the count loses no causal reasoning. The one residual: the specific fact that human-approval-only gating exists at a real seam is corroborating evidence; if line 27's count is the sole record of which seams those are, that topology should land in the plan/evidence file. But as a design claim, nothing a reader needs to understand the design is lost."
  },
  {
   "locator": "\u00a72 Problem A, line 28",
   "excerpt": "The four lightest documents (Research Plan, Acceptance Tests, Phase Validators, `tasks.json`) have no machine-readable contract or no wired validator.",
   "type": "repo_metric",
   "design_claim": "The lightest-weight artifacts in a pipeline tend to lack a machine-readable contract or a wired validator, leaving them unguarded.",
   "action": "strip_keep_claim",
   "restatement": "The lightest-weight artifacts in a pipeline tend to lack a machine-readable contract or a wired validator, leaving them unguarded.",
   "target_note": "Named document types and the count are repo-specific; the general claim is that light artifacts are under-contracted.",
   "preserved": false,
   "loss": "The named documents are NOT pure decoration here \u2014 they ARE the evidence for the claim, and they encode a non-obvious 'why': these four are the LIGHTEST artifacts, and the claim is precisely that lightness correlates with being under-contracted. Dropping the names leaves 'lightest-weight artifacts tend to lack a contract' as an unfalsifiable generalization with its supporting instances removed. Appendix A independently relies on these names ('Contracts for ATs / Phase Validators / Research Plan / tasks.json ... Template-less artifacts'), so stripping line 28 without relocating the list creates a dangling reference in the evidence map. The disposition names no landing target, so the concrete instances vanish. To preserve: relocate the named-document list (which artifacts lacked a contract/validator, and that they were the lightest) to the plan or evidence file so Appendix A still resolves and the claim keeps its proof."
  },
  {
   "locator": "\u00a72 Problem A, line 29",
   "excerpt": "Triage is real but not immediate: every issue propagates to the reconciler, queues against a 4-cycle cap, and re-invokes the expensive author.",
   "type": "repo_metric",
   "design_claim": "Without an early gate, every defect propagates downstream to a reconciliation stage and re-invokes the expensive authoring agent, instead of being caught and triaged immediately at its source.",
   "action": "strip_keep_claim",
   "restatement": "Without an early gate, every defect propagates downstream to a reconciliation stage and re-invokes the expensive authoring agent, rather than being caught and triaged immediately at its source.",
   "target_note": "The '4-cycle cap' and 'reconciler' are repo mechanism names; the claim about late, expensive triage is general.",
   "preserved": true,
   "loss": "Acceptable. The mechanism names ('reconciler', '4-cycle cap') are repo-specific, and the design claim \u2014 without an early gate, every defect propagates downstream to a reconciliation stage and re-invokes the expensive authoring agent instead of being triaged at source \u2014 is preserved intact, including its 'why' (late triage is expensive because it re-invokes the costly author). The restatement keeps both the cost rationale and the structural cause. Minor residual: '4-cycle cap' is a concrete constraint that appears elsewhere as a real pipeline parameter; if this is its only mention it belongs in the plan, but its loss does not damage THIS claim, which is about the timing/cost of triage, not the cap value."
  },
  {
   "locator": "\u00a72 Problem A, line 30",
   "excerpt": "Contract drift between templates and authoring agents is undetected \u2014 no agent pins a template version, so a field rename propagates silently.",
   "type": "repo_metric",
   "design_claim": "When authoring agents reference templates without pinning a version, contract drift between template and agent goes undetected and a field rename propagates silently.",
   "action": "strip_keep_claim",
   "restatement": "When authoring agents reference templates without pinning a version, contract drift between template and agent goes undetected, and a field rename propagates silently.",
   "target_note": "Already abstract; only minor de-repo-ifying needed (no named agent).",
   "preserved": true,
   "loss": "No meaningful loss. The disposition correctly flags this line as already essentially abstract \u2014 it states a general mechanism (authoring agents reference templates without pinning a version, so a field rename propagates silently) with no repo-specific number or named artifact. The restatement is nearly verbatim and preserves the full causal chain (no version pin -> drift undetected -> silent rename propagation), which is the entire 'why'. The 'strip' here removes almost nothing; calling it strip_keep_claim is slightly mislabeled (there is no repo metric to strip), but applying it loses no information a reader needs."
  },
  {
   "locator": "\u00a72 Problem A, line 31",
   "excerpt": "In-run version lineage is untracked \u2014 when an upstream artifact is revised mid-run, nothing detects that downstream artifacts derive from the superseded version (Part IV).",
   "type": "repo_metric",
   "design_claim": "Without in-run lineage tracking, revising an upstream artifact mid-run leaves downstream artifacts silently derived from a superseded version. (Cross-ref Part IV kept.)",
   "action": "strip_keep_claim",
   "restatement": "Without in-run lineage tracking, revising an upstream artifact mid-run leaves downstream artifacts silently derived from a superseded version (Part IV).",
   "target_note": "Largely abstract already; keep the \u00a7Part IV cross-ref. No repo specifics to strip beyond confirming none.",
   "preserved": true,
   "loss": "No loss. This line is already an abstract mechanism statement and the restatement preserves it verbatim including the '(Part IV)' cross-reference, which the disposition explicitly retains. The causal chain (no in-run lineage tracking -> upstream revised mid-run -> downstream silently derives from superseded version) is intact, and the cross-ref keeps the reader's path to the mechanism that solves it. The disposition's own note ('confirm none' to strip) is correct: there are no repo specifics here to lose."
  },
  {
   "locator": "\u00a72 Problem A, line 32",
   "excerpt": "Runs are not measurable \u2014 the only runtime telemetry is install events, so where cycle time goes is unknown (Part V).",
   "type": "repo_metric",
   "design_claim": "Without runtime telemetry beyond install events, a run is not measurable and where cycle time goes is unknown. (Cross-ref Part V kept.)",
   "action": "strip_keep_claim",
   "restatement": "Without runtime telemetry beyond setup/install events, a run is not measurable and where cycle time goes is unknown (Part V).",
   "target_note": "'install events' is a repo specific telemetry detail; generalize to setup/install events. Keep Part V cross-ref.",
   "preserved": true,
   "loss": "No loss. The restatement keeps the full design claim (no telemetry beyond setup/install events => a run is not measurable => where cycle time goes is unknown) and keeps the Part V cross-ref. Generalizing the repo-specific 'install events' to 'setup/install events' is faithful \u2014 the rationale (cycle-time invisibility) and the dependency on a telemetry baseline both survive. The same point is independently reinforced at line 454 and the Part V intro (line 437), so even the cross-ref target is intact."
  },
  {
   "locator": "\u00a72 'Evidence \u2014 prior runs' heading + line 36",
   "excerpt": "### Evidence \u2014 prior runs\\n\\nAcross 14 feature runs, eleven reached the audit stages and eight required at least one reconciliation cycle.",
   "type": "named_run",
   "design_claim": "Empirically, most runs that reach the audit stages require at least one reconciliation cycle \u2014 i.e., late-caught defects forcing rework are the norm, not the exception.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "The whole 'Evidence \u2014 prior runs' subsection (the prose count of 14/11/8 runs) is repo provenance motivating the design. Move to the PLAN as brownfield motivation. The abstract claim (most runs need reconciliation; late defects are the norm) is already carried by the \u00a72 bullets, so the principles doc loses nothing by relocating the evidence.",
   "preserved": false,
   "loss": "The disposition's premise is wrong: the abstract claim is NOT already carried by the \u00a72 bullets. The empirical frequency claim \u2014 'most runs that reach the audit stages require at least one reconciliation cycle; late-caught defects forcing rework are the NORM, not the exception' \u2014 appears ONLY at line 36. The \u00a72 bullets (lines 25-32) state seam-level structural facts (ungated seams, light docs, triage queueing, lineage, measurability); none assert that reconciliation is the dominant empirical outcome. The \u00a71 summary (line 17) asserts the mechanism (defects caught late trigger rework) and the category mix (~three-quarters mechanical), but not the run-level frequency. A grep for 'norm/exception/most runs' finds the claim nowhere else in principles-doc-bound prose. FIX: change the disposition from a bare relocate_to_plan with empty restatement to relocate_to_plan PLUS an abstract restatement left in the principles doc, e.g. 'Empirically, most runs that reach the audit stages require at least one reconciliation cycle \u2014 late-caught defects forcing expensive rework are the norm, not the exception.' Relocating the 14/11/8 counts to the plan is correct; dropping the abstract frequency claim is the loss."
  },
  {
   "locator": "\u00a72 prior-runs table, lines 38-49",
   "excerpt": "| Run | Blueprint versions | PRD versions | Plan versions | Reconciliation cycles | Worst audit count | ... execution-pipeline-design v1\u2013v5 ... devcontainer-mcp-provisioning ... 3 runs stalled pre-audit ... 1 run abandoned mid-pipeline",
   "type": "named_run",
   "design_claim": "Across real runs, core artifacts churned through many versions and accumulated multiple reconciliation cycles and high audit-issue counts \u2014 concrete proof that late, expensive rework is the dominant cost.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Entire table is named-run provenance (named slugs, version counts, audit counts). Move verbatim to the PLAN as the brownfield evidence base. Figure id 'V1' referenced elsewhere stays in the doc; this table is not figure V1.",
   "preserved": true,
   "loss": "No loss from relocation itself \u2014 the table is pure named-run provenance (slugs, per-artifact version counts, audit counts) and belongs in the brownfield plan verbatim. Its abstract claim splits cleanly: the 'late expensive rework dominates' half is carried abstractly by \u00a71's mechanical-class framing (line 17) and by item 4's dominance restatement, and the 'artifacts churn through many versions' half is repo provenance whose only design purpose is to evidence that same dominance. Caveat: this preservation DEPENDS on item 2 being fixed \u2014 if the run-level frequency claim is dropped (item 2's defect), the table's relocation removes the last quantitative support for it from the principles doc. Once item 2 retains its abstract restatement, this table relocates losslessly. The target_note correctly distinguishes this table from Figure V1."
  },
  {
   "locator": "\u00a72 line 51",
   "excerpt": "**Top revision triggers across all audit JSONs:** (1) cross-artifact consistency drift \u2014 72 instances ...; (2) completeness / missing sections \u2014 53 instances; (3) clarity / substantive design \u2014 40 instances. Categories 1 and 2 account for **125 of 165** findings \u2014 about three-quarters \u2014 and are exactly what a mechanical validator can catch. Category 3 is genuinely substantive: it is what expensive auditors *should* spend their tokens on.",
   "type": "repo_metric",
   "design_claim": "Defects sort into three classes: cross-artifact consistency drift, completeness/missing-sections, and substantive design \u2014 and the first two (mechanical, deterministically catchable) dominate, while only the third is what expensive auditors should spend tokens on.",
   "action": "strip_keep_claim",
   "restatement": "Defects sort into three classes \u2014 cross-artifact consistency drift, completeness/missing-section gaps, and substantive design problems. The first two are mechanical and deterministically catchable, and they dominate; only the third is genuinely substantive and worth an expensive auditor's tokens.",
   "target_note": "Strip the counts (72/53/40, 125 of 165, 'across all audit JSONs'). Keep the three-class taxonomy and the dominance claim. The raw counts can also be carried in the PLAN/evidence file alongside the prior-runs table.",
   "preserved": true,
   "loss": "No loss. The restatement keeps the full three-class taxonomy (cross-artifact consistency drift; completeness/missing-section gaps; substantive design) AND both halves of the dominance claim \u2014 the first two classes are mechanical/deterministically-catchable and dominate, the third is the only one worth an expensive auditor's tokens. Stripping the counts (72/53/40, 125 of 165, 'across all audit JSONs') is correct; they are repo metrics and land in the plan/evidence file. 'Deterministically catchable' faithfully restates 'exactly what a mechanical validator can catch'. The categorization-estimate caveat already lives in Appendix A (line 944) and \u00a71 (line 17), so the 'estimate not measurement' nuance is not lost."
  },
  {
   "locator": "\u00a72 Figure V1, lines 53-75",
   "excerpt": "**Figure V1 \u2014 The validation chain today.** Where the chain leaks: red = ungated seams, amber = reviewed-only. [mermaid flowchart Intent\u2192PRD\u2192Research Plan\u2192Discovery Research\u2192Synthesis\u2192per-layer Designs\u2192Blueprint+ADRs\u2192Architecture Audit\u2192Plan\u2192Acceptance Tests+Phase Validators\u2192Cross-Artifact Audit\u2192Task Decomposition\u2192Deliverable Packaging\u2192Run complete, with C/D/E/J/L styled as leaking]",
   "type": "worked_example",
   "design_claim": "In a real pipeline, the ungated/reviewed-only seams cluster at specific transitions (early research-to-synthesis handoffs and the late test-authoring and task-decomposition handoffs), illustrating that leaks concentrate where light artifacts cross stage boundaries.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Figure V1 depicts THIS pipeline's exact 13-stage topology and named stages \u2014 it is a worked diagram of the actual repo pipeline. Move to the PLAN. Note: keep the figure-id reference 'V1' wherever the principles doc cites it; if the principles doc needs a leak illustration, replace with an abstract stage-agnostic flowchart, but the concrete one belongs in the plan.",
   "preserved": true,
   "loss": "Relocating the concrete diagram is correct \u2014 it renders THIS pipeline's exact 13-stage topology with named stages, which is repo-specific. The abstract design claim (leaks concentrate where light artifacts cross stage boundaries: early research-to-synthesis, late test-authoring and task-decomposition) is preserved in principle, but with a CONDITIONAL risk the rewriter must honor: its current prose homes (line 28 'the four lightest documents...' and the \u00a72 seam table lines 89-95) are themselves repo-specific and may be stripped by sibling dispositions. The disposition hedges this correctly by offering an abstract stage-agnostic leak flowchart as the principles-doc replacement \u2014 but that replacement is phrased as optional ('if the principles doc needs a leak illustration'). FIX/WATCH: make it non-optional that the principles doc retains either an abstract leak-clustering sentence or an abstract flowchart, so the design claim does not silently evaporate when the named-seam prose is also stripped. With that, no loss."
  },
  {
   "locator": "\u00a72 'What enforces today' line 81",
   "excerpt": "**`shared-document-reviewer`** \u2014 a Gate 0 (structural) + Gate 1 (semantic) review on five document types (Intent, PRD, Design Doc, Plan, Deliverable Archive).",
   "type": "code_citation",
   "design_claim": "Structural enforcement that does exist is partial: a reviewer covers only a subset of document types with a structural-plus-semantic gate, leaving the rest uncovered.",
   "action": "strip_keep_claim",
   "restatement": "The structural enforcement that does exist is partial \u2014 a document reviewer applies a structural-plus-semantic gate to only a subset of document types, leaving the others uncovered.",
   "target_note": "Named agent (shared-document-reviewer), named doc types, and the count (five) are repo-specific. The claim that enforcement covers only a subset is general.",
   "preserved": true,
   "loss": "No loss. The restatement keeps the design claim (existing structural enforcement is PARTIAL \u2014 a structural-plus-semantic gate covers only a subset of document types, leaving the rest uncovered) and preserves the Gate 0/Gate 1 character via 'structural-plus-semantic gate'. Stripping the named agent (shared-document-reviewer), the named doc types, and the count 'five' is correct repo-specificity removal. The 'subset of nine' gap is the load-bearing point and it survives; the specific list of which docs are covered vs uncovered is repo detail that belongs in the plan if anywhere, not a design rationale the principles reader needs."
  },
  {
   "locator": "\u00a72 'What enforces today' line 82",
   "excerpt": "**`verdict_findings_parity.py`** \u2014 hard-halts the pipeline if any reviewer's or auditor's verdict contradicts its own findings (five surfaces).",
   "type": "code_citation",
   "design_claim": "A deterministic consistency check can hard-halt the pipeline when a reviewer's verdict contradicts its own findings \u2014 a useful pattern, but applied at only a few surfaces today.",
   "action": "strip_keep_claim",
   "restatement": "A deterministic consistency check can hard-halt the pipeline when a reviewer's verdict contradicts its own findings \u2014 though today such checks exist at only a few surfaces.",
   "target_note": "Named script and the '(five surfaces)' count are repo-specific; the verdict-vs-findings-parity pattern is a general design idea worth keeping.",
   "preserved": true,
   "loss": "No meaningful loss. The restatement keeps the full design claim (a deterministic verdict-vs-findings consistency check can hard-halt the pipeline) AND its load-bearing 'why' qualifier ('only a few surfaces today'), which is the point that motivates generalizing the pattern. What vanishes is the script name (verdict_findings_parity.py) and the exact count ('five surfaces') \u2014 both pure repo-specific citations carrying no rationale a reader of a principles doc needs. Note: the precise count is evidence; if the plan/evidence file later needs to scope the rollout ('extend from 5 surfaces to all gated handoffs'), that '5' should be captured there, not just dropped \u2014 but its absence from the principles doc loses no design information."
  },
  {
   "locator": "\u00a72 'What enforces today' line 83",
   "excerpt": "**`validate_adr_placement.py`** + **`check_feature_touch_predicate.py`** \u2014 close-gate the Design Composition stage.",
   "type": "code_citation",
   "design_claim": "Some stages do have deterministic close-gates wired in \u2014 but only a scattered few, not systematically across all stages.",
   "action": "strip_keep_claim",
   "restatement": "Some stages do have deterministic close-gates wired in, but only a scattered few \u2014 enforcement is not systematic across all stages.",
   "target_note": "Two named scripts and the named stage are repo-specific; the general point (a few stages are gated, most are not) survives.",
   "preserved": true,
   "loss": "No design loss. The restatement preserves the substantive claim ('a scattered few stages are gated; enforcement is not systematic'), which is the constraint a reader needs. Stripped: two script names (validate_adr_placement.py, check_feature_touch_predicate.py) and the named stage (Design Composition) \u2014 pure citations. Minor caveat: the original tells the reader WHICH stage happens to be gated, which is evidence for the brownfield change inventory; that fact has value in the plan, not the principles doc. Confirmed the plan does not yet carry it, so if it matters for scoping it must be added there \u2014 but the abstract claim survives intact."
  },
  {
   "locator": "\u00a72 line 85",
   "excerpt": "**No hooks fire on document writes.** `settings.json` registers SessionStart/SessionEnd plus three PreToolUse matchers (a Task-intercept and two serena helpers) \u2014 **none on Write/Edit**; write-time validation is entirely orchestrator-driven, so every gate depends on the orchestrator remembering to invoke it. There is no defense-in-depth.",
   "type": "code_citation",
   "design_claim": "When validation is entirely orchestrator-driven with no write-time hook enforcement, every gate depends on the orchestrator remembering to invoke it \u2014 there is no defense-in-depth and a forgotten invocation silently skips the check.",
   "action": "strip_keep_claim",
   "restatement": "When validation is entirely orchestrator-driven with no automatic write-time enforcement, every gate depends on the orchestrator remembering to invoke it \u2014 there is no defense-in-depth, and a forgotten invocation silently skips the check.",
   "target_note": "settings.json, SessionStart/SessionEnd, the PreToolUse matcher inventory, and serena are repo-specific config facts. The design claim (orchestrator-driven gates lack defense-in-depth) is timeless.",
   "preserved": true,
   "loss": "The full causal chain \u2014 the actual 'why' \u2014 is preserved verbatim in the restatement: orchestrator-driven gates depend on the orchestrator remembering, so there is no defense-in-depth and a forgotten invocation silently skips the check. That is the entire design argument. What is lost is the EVIDENCE that proves the assertion: the concrete inventory (SessionStart/SessionEnd + three PreToolUse matchers, none on Write/Edit). After the strip, 'no automatic write-time enforcement' becomes an unsupported assertion rather than a demonstrated fact. For a principles doc this is acceptable (claims, not proofs, belong there), but the target_note marks these facts to strip WITHOUT relocating \u2014 so the proof disappears entirely. Recommend the hook-matcher inventory land in the plan/evidence file as the brownfield baseline; otherwise the architecture loses its grounding for this claim."
  },
  {
   "locator": "\u00a72 leaks table, lines 89-95",
   "excerpt": "| Seam | What passes unchecked | Why it bites later | ... Discovery Research \u2192 Synthesis | `codebase-analysis.json` + N research notes ... | Synthesis \u2192 per-layer Design | `synthesis.md` ... | Test Authoring \u2192 Cross-Artifact Audit | `acceptance-tests.md` + `phase-validators.md` ... | Task Decomposition \u2192 Packager | `tasks.json` ... | Research Plan \u2192 Discovery Research (reviewed, not gated)",
   "type": "worked_example",
   "design_claim": "Each unguarded seam admits an uncontracted artifact whose defects surface only much later and much more expensively downstream \u2014 the cost of a leak is paid at a distance from where it occurred.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "This table names this repo's specific seams, artifact filenames (codebase-analysis.json, synthesis.md, acceptance-tests.md, phase-validators.md, tasks.json), and stages. Move to the PLAN as the concrete leak inventory. The abstract claim (uncontracted artifacts at ungated seams bite expensively downstream) is already stated in \u00a72 bullets and \u00a71, so principles doc loses nothing.",
   "preserved": true,
   "loss": "Conditionally preserved \u2014 and the condition is real, not satisfied yet. The abstract claim (uncontracted artifacts at ungated seams surface expensively downstream) IS already stated abstractly at \u00a71 line 17, \u00a72 line 25, and \u00a72 line 27 ('four of seven seams ungated; one relies only on a human gate'), so the principles doc loses nothing. BUT the table's 'Why it bites later' column is genuine rationale, not citation \u2014 e.g. 'the expensive cross-artifact auditor is the first eye on them', 'designers improvise off uneven synthesis'. The action says 'Move to the PLAN as the concrete leak inventory'; 'inventory' could be read as just the seam list. I verified the plan does NOT yet contain this table, so the move is net-new: if only the seam/artifact columns travel and the 'Why it bites later' column is dropped, per-seam rationale is permanently lost. Preservation holds ONLY IF the full table including the rationale column lands in the plan."
  },
  {
   "locator": "\u00a72 'The contract layer today' line 99",
   "excerpt": "The documentation KB owns templates and disciplines for nine document types; coverage is asymmetric.",
   "type": "code_citation",
   "design_claim": "A central documentation source can own templates and authoring disciplines for the pipeline's document types, but in practice contract coverage across those types is asymmetric \u2014 some fully specified, some not.",
   "action": "strip_keep_claim",
   "restatement": "A central documentation source may own templates and authoring disciplines for the pipeline's document types, yet contract coverage across those types is typically asymmetric \u2014 some fully specified, others barely.",
   "target_note": "'documentation KB' and the count (nine) are repo-specific; the asymmetric-coverage claim is general.",
   "preserved": true,
   "loss": "No design loss. The restatement preserves the substantive claim (a central doc source can own templates/disciplines, yet coverage across doc types is typically asymmetric \u2014 some fully specified, others barely). Stripped: the source name ('documentation KB') and the count ('nine') \u2014 pure repo facts. The 'why it matters' (asymmetry, not the count) survives. Acceptable."
  },
  {
   "locator": "\u00a72 contract-layer table, lines 101-111",
   "excerpt": "| Doc type | Template? | Schema? | Frontmatter spec? | Cross-artifact contract? | Authoring agent integration | Validator wired in? | ... Intent Clarification | yes, v1.0.0 ... | ADR | yes, v1.0.0 | placement + prescription validators ... | Acceptance Tests | none ... | tasks.json | different KB, inline literal ...",
   "type": "worked_example",
   "design_claim": "Contract completeness varies sharply by document type: heavier, earlier documents tend to have templates, schemas, and wired validators, while lighter or later artifacts have none \u2014 coverage correlates with a document's perceived weight, not its risk.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Full per-doc-type matrix with repo version strings (v1.0.0), named validators, and named doc types. Move to the PLAN as the concrete contract-coverage audit. Abstract claim (coverage is asymmetric and uncorrelated with risk) survives via line 99 restatement and the \u00a72 drift list.",
   "preserved": false,
   "loss": "This is the one item that loses an idea. The disposition's OWN design_claim contains a sharp analytical insight \u2014 'coverage correlates with a document's perceived weight, not its risk' \u2014 i.e. the pipeline gates documents by how heavy they feel, not by how much damage they cause, which is backwards. I searched the whole architecture doc: that weight-vs-risk inversion appears NOWHERE abstractly. \u00a72 line 28 only states the flat fact that 'the four lightest documents have no machine-readable contract' \u2014 it never makes the point that lightness is a bad proxy for risk. The proposed survivor (line 99 restatement) keeps only 'asymmetric... some barely specified', which is strictly weaker: asymmetry is a description; 'weight not risk' is the diagnosis. Relocating the matrix to the plan as raw data and leaving only line 99 in the principles doc therefore drops the actual rationale the disposition itself identified as worth keeping. Fix: carry the 'coverage tracks perceived weight, not risk' sentence into the principles doc (or strengthen the line-99 restatement to include it) before relocating the matrix. Also confirmed the plan does not yet hold this matrix, so the move is net-new and the matrix data itself is only preserved if it actually lands there."
  },
  {
   "locator": "\u00a72 line 113",
   "excerpt": "The cross-artifact-discipline validator scripts (`validate_pipeline_frontmatter.py`, `detect_stubs.py`, `audit_canonical_drift.py`, and peers) exist but are nearly orphan \u2014 invoked only by the document reviewer and the phase-check harness, never at authoring time. They are available but unenforced.",
   "type": "code_citation",
   "design_claim": "Validators can exist yet be effectively orphaned \u2014 present in the codebase but invoked only at narrow points and never at authoring time, so they are available but unenforced.",
   "action": "strip_keep_claim",
   "restatement": "Validators can exist yet be effectively orphaned \u2014 present but invoked only at narrow points and never at authoring time, so they are available but unenforced.",
   "target_note": "Three named scripts and the named invocation sites (document reviewer, phase-check harness) are repo-specific. The orphaned-validator pattern is a general anti-pattern worth keeping.",
   "preserved": true,
   "loss": "The abstract orphaned-validator claim survives intact, and the three script names are pure citations that rightly vanish. Minor residual risk: the restatement also drops the *specific* invocation sites (document reviewer, phase-check harness), which name exactly which narrow points the validators ARE wired to. That is a real constraint a plan author hardening the enforcement needs (it tells them where to extend coverage from). The action is strip_keep_claim with no relocation target, yet the target_note says these sites 'belong in the plan.' If the plan does not actually capture 'currently wired only at: reviewer + phase-check harness,' that fact is lost. Verdict: claim preserved; ensure the two invocation sites land in the plan, otherwise a small but useful constraint disappears."
  },
  {
   "locator": "\u00a72 'specific drift surfaces' list, lines 115-124",
   "excerpt": "1. Acceptance Tests + Phase Validators \u2014 no template in the KB ... 2. tasks.json \u2014 schema is an inline literal in a different KB ... 3. Research Plan \u2014 the template carries no `version:` line ... 4. FR \u2192 AC \u2192 task \u2192 test traceability ... 5. doc-type emission backfill \u2014 planning-side agents still need `doc_type:` frontmatter ... 6. No template-version pinning ... 7. Conditional template language \u2014 test authors say \"if a dedicated template exists\" for templates that never existed.",
   "type": "worked_example",
   "design_claim": "Specific, recurring drift modes follow predictable patterns: missing templates for light artifacts, schemas trapped as inline literals, missing version lines defeating conformance checks, untraced traceability chains, missing required frontmatter, unpinned template versions, and 'temporary' conditional language masking permanent gaps.",
   "action": "strip_keep_claim",
   "restatement": "Drift recurs in predictable modes: light artifacts ship with no template; schemas live as inline literals rather than enforceable files; missing version lines defeat conformance checks; declared traceability chains have no programmatic validator; required frontmatter is unevenly emitted; templates are referenced by path rather than pinned version (so renames propagate silently); and conditional 'if a template exists' language masks a permanent gap as temporary.",
   "target_note": "The list's structure is general drift-mode taxonomy worth keeping in abstract form; strip the named doc types, named KB, doc_type field name, version: line, and FR/AC/task/test specifics. The concrete enumerated instances belong in the PLAN.",
   "preserved": true,
   "loss": "All seven drift modes survive in abstract form (no-template light artifacts, inline-literal schemas, missing version lines, untraced traceability, uneven frontmatter, path-not-version pinning, conditional 'if a template exists' language). One repo-specific consequence is dropped without an abstract home: item 5's 'until then, new artifacts produce a structural finding' \u2014 i.e., the cost of the missing frontmatter is a recurring review finding, not a silent gap. That nuance is repo-specific and acceptable to relocate, provided the plan records it; it is not a general design claim, so its loss from the principles doc is correct. No abstract design information is lost."
  },
  {
   "locator": "\u00a73 Problem B, lines 127-139",
   "excerpt": "The project is assembled from **domains** ... Each domain ships, by convention, a standard bundle: a **Platform KB** (facts), a **Design KB** (discipline), an **Auditor**, and a per-layer **designer** agent. ... [coverage table: claude-code/cicd full; mcp folded-in; codespaces/frontend/backend... missing auditors; observability all-\u274c] ... Only **two** domains carry the complete bundle. The registry that records layers (`engineering-domain-layers.yaml`) has **no auditor field, no MCP-server inventory, and no install-scope tracking**. Auditor dispatch is a hard-coded table.",
   "type": "worked_example",
   "design_claim": "When a composed system's domains are supposed to ship a standard bundle of parts but the convention is unenforced, real coverage is ragged \u2014 most domains are missing parts, the registry of record omits key fields, and dispatch is hard-coded rather than derived from a declared bill-of-materials.",
   "action": "strip_keep_claim",
   "restatement": "When a composed system's domains are each supposed to ship a standard bundle of parts but the convention is unenforced, real coverage turns out ragged: most domains are missing parts, the registry of record omits key fields, and dispatch is hard-coded rather than derived from a declared bill-of-materials.",
   "target_note": "Strip the per-domain coverage matrix, named domains, the named registry file (engineering-domain-layers.yaml), the 'only two' count, and the bundle's repo-specific part names (Platform KB / Design KB / Auditor / designer). Keep the abstract claim (unenforced bundle convention yields ragged coverage; registry incomplete; dispatch hard-coded). The concrete coverage matrix and registry-gap detail relocate to the PLAN.",
   "preserved": true,
   "loss": "The core claim (unenforced bundle convention \u2192 ragged coverage; registry of record incomplete; dispatch hard-coded rather than BoM-derived) survives. Two compressions to watch. (1) The three named registry gaps (no auditor field, no MCP-server inventory, no install-scope tracking) collapse to 'omits key fields' \u2014 correct abstraction, but install-scope tracking is the field that later powers cross-scope orphan detection (the item-4/5 anecdote); the plan must keep that specific gap or the connective tissue between Problem B and its evidence frays. (2) The original's bidirectional check gap \u2014 no check that a declared domain is whole AND no check that flags an installed-but-undeclared part \u2014 is only half-carried by 'most domains are missing parts.' The orphan-detection direction rides on the 'bill-of-materials' phrasing; it is implied, not stated. Recommend the restatement keep an explicit 'and nothing flags an installed-but-undeclared part' clause so the orphan direction is not lost."
  },
  {
   "locator": "\u00a73 'Evidence \u2014 the removal that proved the gap' lines 141-143",
   "excerpt": "Removing one MCP server (gitnexus) touched **15+ surfaces by hand**, and nothing tracked that the same capability was also installed at **user scope** (`~/.claude/skills/` + a hook) ... The earlier removal of another server is recorded only as a deprecation annotation, not in any live registry. **There is no source of truth that reconciles all install sites.**",
   "type": "named_run",
   "design_claim": "Without a registry that reconciles all install sites, removing a domain is a manual hunt across many surfaces that predictably leaves orphans \u2014 especially parts installed at a scope the removal never reached \u2014 which then resurface until found by hand.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Named server (gitnexus), the '15+ surfaces' count, the specific user-scope path, and the second-removal anecdote are repo provenance. Move the worked anecdote to the PLAN. The abstract claim (no install-site source of truth \u2192 manual removal leaves orphans) survives via a restatement in the principles doc; provide that restatement too.",
   "preserved": true,
   "loss": "Clean relocation. The worked anecdote (named server, 15+ surfaces, user-scope path, second-removal-as-annotation) is repo provenance and correctly moves to the plan with an empty principles-doc restatement, because the paired row (item 5) supplies the abstract survivor. The 'why' that matters \u2014 a removal leaves orphans specifically at a scope the removal never reached \u2014 is carried by item 5, so no design rationale is stranded. Caveat: preservation here is CONTINGENT on item 5 actually landing in the principles doc and this anecdote actually landing in the plan. If either move is skipped, the evidence-for-claim linkage breaks. Treated as a pair, no loss."
  },
  {
   "locator": "\u00a73 'Evidence \u2014 the removal that proved the gap' (abstract claim restatement)",
   "excerpt": "(restatement to live in principles doc in place of the gitnexus anecdote)",
   "type": "named_run",
   "design_claim": "Without a single source of truth that reconciles every install site, removing a domain becomes a manual hunt across many surfaces and predictably leaves orphans \u2014 particularly parts installed at a scope the removal never reached, which resurface until manually deleted.",
   "action": "strip_keep_claim",
   "restatement": "Without a single source of truth that reconciles every install site, removing a domain becomes a manual hunt across many surfaces and predictably leaves orphans \u2014 particularly parts installed at a scope the removal never reached, which resurface until manually deleted.",
   "target_note": "This is the abstract sentence that replaces the gitnexus worked example in the principles doc; the worked example itself relocates to the PLAN (prior row).",
   "preserved": true,
   "loss": "No loss. This is the survivor sentence and it carries the full rationale: no single source of truth reconciling every install site \u2192 manual cross-surface hunt \u2192 predictable orphans, *particularly parts installed at a scope the removal never reached, which resurface until manually deleted.* The root cause (scope mismatch), the mechanism (manual hunt), and the symptom (recurring resurfacing) all survive. This is the strongest disposition in the set."
  },
  {
   "locator": "\u00a73 Figure V2, line 145",
   "excerpt": "**Figure V2 \u2014 The domain-bundle coverage matrix** is the table immediately above: only `claude-code` and `cicd` carry the full bundle; the all-\u274c `observability` row is the obvious gap. (Rendered as the table rather than a redundant heatmap \u2014 the cells already read at a glance.)",
   "type": "worked_example",
   "design_claim": "A coverage matrix makes the bundle gaps legible at a glance \u2014 the domains with the full bundle and the wholly-missing domain stand out immediately, which is why a table beats a redundant heatmap.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Figure V2 just labels the repo-specific coverage table (named domains claude-code/cicd/observability). Move with the table to the PLAN. Keep the figure-id 'V2' convention only if the principles doc still references it; otherwise it travels with the table. The general point (a matrix exposes bundle gaps at a glance) can be folded into the \u00a73 restatement if desired.",
   "preserved": false,
   "loss": "Figure V2 is a pure pointer to the coverage table plus one piece of generalizable rationale: 'a matrix exposes bundle gaps at a glance, which is why a table beats a redundant heatmap.' The disposition relocates the figure with the table to the plan (correct, since the figure has no meaning without the table) but leaves folding the matrix-reads-at-a-glance point into \u00a73 as OPTIONAL ('if desired'). That one clause is the only design-presentation rationale in the figure \u2014 it explains a deliberate choice (table over heatmap) and is reusable wherever the principles doc presents coverage. Leaving it optional risks dropping it entirely: the table moves to the plan, the figure moves with it, and the rationale evaporates because nobody is obligated to fold it anywhere. Secondary risk: if any surviving principles-doc text still says 'Figure V2,' that reference dangles once the table leaves. Marked not-preserved because the disposition does not guarantee the one generalizable claim survives; fix by making the 'a coverage matrix exposes bundle gaps at a glance' fold mandatory in the \u00a73 restatement rather than optional, and sweep for residual 'Figure V2' references."
  },
  {
   "locator": "\u00a74 line 161",
   "excerpt": "The substrate also imposes **technology boundaries** \u2014 architecturally-significant constraints (tech-agnostic; they name no vendor) that bind which technologies the plan may choose. These are collected as a binding registry in **Appendix F**.",
   "type": "evidence_map",
   "design_claim": "The substrate also imposes architecturally-significant, technology-agnostic constraints that bind which technologies an implementation may choose, collected as a binding registry. (Cross-ref to Appendix F is intra-document and kept.)",
   "action": "strip_keep_claim",
   "restatement": "The substrate also imposes architecturally-significant, technology-agnostic constraints that bind which technologies an implementation may choose; these are collected as a binding registry (Appendix F).",
   "target_note": "This is already tech-agnostic and the Appendix F reference is an intra-document cross-ref (KEEP per instructions). Minor: ensure 'the plan' is generalized to 'an implementation' if the principles doc should not assume a separate plan artifact. No repo specifics to strip.",
   "preserved": true,
   "loss": "No design information is lost. The restatement keeps all five load-bearing elements: the constraints are architecturally-significant, technology-agnostic, they bind which technologies an implementation may choose, they are collected as a binding registry, and the intra-document Appendix F cross-ref is retained (correctly \u2014 it is document structure, not a codebase reference). The original excerpt already named no vendor, so there is nothing repo-specific to strip; the only edit is generalizing 'the plan may choose' to 'an implementation may choose', which is the right move for a doc that should not presuppose a separate plan artifact. Minor, non-blocking: the parenthetical gloss '(tech-agnostic; they name no vendor)' is compressed to the single word 'technology-agnostic'. That word carries the meaning, so no rationale is lost, but if maximal clarity is wanted the 'name no vendor' gloss could be retained verbatim since it costs nothing and contains no repo specifics."
  },
  {
   "locator": "\u00a76 The substrate components, table row 1 (Canonical Layer), 'Realized as' column",
   "excerpt": "`.claude/canonical/*.yaml` + the pipeline manifest + the domain bill-of-materials",
   "type": "code_citation",
   "design_claim": "The Canonical Layer is realized as versioned declarative files (schemas/vocabulary/contracts), a pipeline manifest, and a domain bill-of-materials \u2014 everything else derives, nothing restates.",
   "action": "strip_keep_claim",
   "restatement": "Realized as a set of versioned declarative files (vocabulary, schemas, contracts), a pipeline manifest, and a domain bill-of-materials.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 The substrate components, table row 2 (Validation Library), 'Realized as' column",
   "excerpt": "`auditing-shared/scripts/` + the `canonical.py` typed accessor",
   "type": "code_citation",
   "design_claim": "The Validation Library is realized as a shared set of deterministic checker scripts plus a typed accessor that reads rules from canonical rather than hardcoding them.",
   "action": "strip_keep_claim",
   "restatement": "Realized as a shared library of deterministic checker scripts plus a typed accessor that reads rules from the canonical source.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 The substrate components, table row 3 (Actors), 'Realized as' column",
   "excerpt": "the sub-agents",
   "type": "code_citation",
   "design_claim": "Actors are the probabilistic producers/consumers, realized as the platform's sub-agents \u2014 stateless, single-purpose, communicate by reference, never self-approve, never dispatch each other.",
   "action": "strip_keep_claim",
   "restatement": "Realized as the agent-platform's sub-agents (the stateless single-purpose producers/consumers).",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 The substrate components, table row 4 (Coordinator), 'Realized as' column",
   "excerpt": "per-segment Dynamic Workflow scripts + a sequencing coordinator (D-ORCH-1)",
   "type": "code_citation",
   "design_claim": "The Coordinator drives the manifest loop (read manifest \u2192 dispatch actor \u2192 run gate \u2192 route) and is realized as per-segment workflow scripts plus a sequencing coordinator, with human gates between segments since a gate cannot live inside a workflow script.",
   "action": "strip_keep_claim",
   "restatement": "Realized as per-segment workflow scripts plus a sequencing coordinator (D-ORCH-1), with human gates placed between segments. (D-ORCH-1 is an internal decision reference, retained.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 The substrate components, table row 5 (Knowledge Layer), 'Realized as' column",
   "excerpt": "the `KB-*` skills",
   "type": "code_citation",
   "design_claim": "The Knowledge Layer is passive authoring discipline read by actors (not executable), realized as a set of knowledge skills.",
   "action": "strip_keep_claim",
   "restatement": "Realized as a set of passive knowledge/authoring-discipline skills read by actors.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 The substrate components, table row 6 (Drift Sentinel), 'Realized as' column",
   "excerpt": "the canonical-drift + conformance audits",
   "type": "code_citation",
   "design_claim": "The Drift Sentinel is the config-time/CI meta-gate that audits the substrate itself (every contract has a validator, every manifest edge has a gate, every declared domain has its full bundle, nothing redefines canonical or is orphaned), realized as canonical-drift and conformance audits.",
   "action": "strip_keep_claim",
   "restatement": "Realized as config-time/CI canonical-drift and conformance audits.",
   "preserved": true,
   "loss": "No loss. The substantive design claim \u2014 what the Drift Sentinel is and what it audits (every contract has a validator, every manifest edge a gate, every domain its full bundle, nothing redefines canonical or is orphaned; config-time/CI not runtime) \u2014 lives entirely in the adjacent Role column, which is untouched. The stripped excerpt ('the canonical-drift + conformance audits') is a pure realization citation, and the restatement preserves the abstract realization ('config-time/CI canonical-drift and conformance audits'). No rationale, constraint, or 'why' is carried only by the stripped text. Disposition is sound."
  },
  {
   "locator": "\u00a76 The substrate components, table row 7 (Observability), 'Realized as' column",
   "excerpt": "the run-event surface",
   "type": "code_citation",
   "design_claim": "Observability is an append-only run-event log plus projection serving a triple role (audit, recovery journal \u00a718, improvement-loop flywheel Part V), realized as a run-event surface.",
   "action": "strip_keep_claim",
   "restatement": "Realized as an append-only run-event surface (log plus projection). (\u00a718 and Part V are internal cross-references, retained.)",
   "preserved": true,
   "loss": "No loss. The design claim \u2014 append-only run-event log + projection, runtime not config-time, distinct from the sentinel, with the triple role (audit / recovery journal / improvement-loop flywheel) \u2014 is in the Role column and preserved. The restatement keeps the abstract realization ('append-only run-event surface, log plus projection') and the disposition correctly notes \u00a718 and Part V are intra-document cross-references that stay. The phrase 'the run-event surface' adds no information beyond what \u00a717 (titled 'The run-event surface') already defines. Sound."
  },
  {
   "locator": "\u00a76 Figure V3 System Context, System_Ext(ci)",
   "excerpt": "System_Ext(ci, \"GitHub Actions\", \"CI: drift sentinel + validators\")",
   "type": "worked_example",
   "design_claim": "The pipeline is validated by an external CI system that runs the drift sentinel and validators.",
   "action": "strip_keep_claim",
   "restatement": "External CI system (runs the drift sentinel + validators), shown as a generic 'CI/CD platform' actor rather than a named vendor.",
   "preserved": true,
   "loss": "Design claim (external CI runs drift sentinel + validators) survives in the restatement. One dependency to flag, not a defect in this item: the doc's own 'Platform foundations' subsection (\u00a76, after V3) deliberately argues these vendor names are architecturally-significant *bindings*, not vendor leakage ('a platform swap is an architecture-level change'). Genericizing the V3 node to 'CI/CD platform' is safe ONLY IF that binding rationale is itself restated/relocated rather than silently dropped. The 'why GitHub Actions specifically matters' lives in that prose table, not in the diagram node, so this node's genericization loses nothing on its own \u2014 but the playbook must handle the Platform-foundations table as a paired item or the architectural-significance rationale is lost project-wide."
  },
  {
   "locator": "\u00a76 Figure V3 System Context, System_Ext(cs)",
   "excerpt": "System_Ext(cs, \"GitHub Codespaces\", \"Ephemeral devcontainer runtime\")",
   "type": "worked_example",
   "design_claim": "The pipeline runs inside an ephemeral, single-container devcontainer runtime.",
   "action": "strip_keep_claim",
   "restatement": "Ephemeral devcontainer runtime the pipeline runs inside, shown as a generic 'Devcontainer environment' actor.",
   "preserved": true,
   "loss": "Design claim (pipeline runs inside an ephemeral single-container devcontainer runtime) survives in the restatement, and 'ephemeral single-container' is the load-bearing constraint that shapes durability \u2014 correctly retained, not just the vendor name dropped. Same paired dependency as the ci node: the 'Devcontainer environment / GitHub Codespaces' binding is argued as architecturally significant (TB1, TB2) in the Platform foundations table; that rationale must be restated/relocated separately. This node alone loses nothing."
  },
  {
   "locator": "\u00a76 Figure V3 System Context, System_Ext(mcp)",
   "excerpt": "System_Ext(mcp, \"MCP servers\", \"serena, context7, exa, \u2026\")",
   "type": "worked_example",
   "design_claim": "The pipeline queries external context/tool servers (the MCP layer) to do its work.",
   "action": "strip_keep_claim",
   "restatement": "External MCP (tool/context) servers the pipeline queries, shown generically without naming specific servers.",
   "preserved": true,
   "loss": "Design claim (pipeline queries external tool/context servers \u2014 the MCP layer) survives. Dropping the specific server names (serena, context7, exa) is correct for a timeless doc and loses no design information \u2014 the named servers are pure examples, not constraints. The only caveat is the cross-cutting one: if the MCP binding carries architectural significance elsewhere (AGENTS.md lists MCP as part of the Claude Code primitive set the whole pattern is built from), the playbook should ensure that claim is not orphaned. For this diagram node in isolation: no loss."
  },
  {
   "locator": "\u00a76 Figure V3 System Context, System_Ext(git)",
   "excerpt": "System_Ext(git, \"Git repository\", \"Durable system of record\")",
   "type": "worked_example",
   "design_claim": "A version-controlled repository is the durable system of record for artifacts.",
   "action": "strip_keep_claim",
   "restatement": "Version-controlled repository as the durable system of record (generic, vendor-agnostic).",
   "preserved": true,
   "loss": "No loss. Design claim (a version-controlled repository is the durable system of record) is fully preserved and is the most genuinely vendor-agnostic of the set \u2014 'Git repository' / 'Durable system of record' already reads as a generic role, so genericizing the label is near-trivial. The 'durable system of record' property is the load-bearing part and it is retained verbatim in the restatement. Sound."
  },
  {
   "locator": "\u00a76 Figure V4 Container View, Container(val) tech tag",
   "excerpt": "Container(val, \"Validation Library\", \"Python\", \"deterministic gates\")",
   "type": "worked_example",
   "design_claim": "The Validation Library is deterministic code implementing gates.",
   "action": "strip_keep_claim",
   "restatement": "Validation Library shown as deterministic code (no specific language tag), implementing the gates.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 Figure V4 Container View, Container(obs) tech tag",
   "excerpt": "Container(obs, \"Observability\", \"JSONL + backend\", \"run-event log + projection\")",
   "type": "worked_example",
   "design_claim": "Observability is an append-only run-event log plus a projection/backend.",
   "action": "strip_keep_claim",
   "restatement": "Observability shown as an append-only run-event log plus a projection/backend (generic serialization, no specific format).",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 Platform foundations, intro paragraph (line 232)",
   "excerpt": "The pattern is **instantiated on a platform substrate** the rest of the architecture assumes. Per the Appendix F litmus, each platform choice here is **architecturally significant** ... so it is named as a *binding*, not treated as vendor leakage: the *role* is technology-agnostic, the *binding* is the architecturally-significant decision.",
   "type": "adr_as_evidence",
   "design_claim": "A principle pattern is always instantiated on some platform substrate; architecturally-significant platform choices should be named as explicit bindings (role technology-agnostic, binding the significant decision) rather than buried as vendor leakage.",
   "action": "strip_keep_claim",
   "restatement": "The pattern is always instantiated on some concrete platform substrate. Each architecturally-significant platform choice should be named as an explicit binding \u2014 the role stays technology-agnostic, the binding records the significant decision (Appendix F litmus). The specific bindings for any given instantiation belong in that instantiation's plan, not in the principles.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 Platform foundations table, row 'AI-agent platform'",
   "excerpt": "| **AI-agent platform** | Claude Code | the whole pattern is built from its primitives \u2014 skills, sub-agents, hooks, MCP, slash commands; swapping it rewrites the Actors, Coordinator, and Knowledge layers | TB4 ... TB6 ... TB7 ... TB8 ..., the primitive vocabulary |",
   "type": "worked_example",
   "design_claim": "The AI-agent platform is an architecturally-significant binding: the whole pattern is built from its primitives, so swapping it rewrites the Actors, Coordinator, and Knowledge layers.",
   "action": "relocate_to_plan",
   "target_note": "The concrete binding (Claude Code) and its specific primitives move to the plan's platform-binding table. The principles doc keeps the abstract row: 'AI-agent platform \u2014 architecturally significant because the Actors/Coordinator/Knowledge layers are built from its primitives; swapping it rewrites those layers.' Internal TB references stay with whichever doc carries the row.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 Platform foundations table, row 'Devcontainer environment'",
   "excerpt": "| **Devcontainer environment** | GitHub Codespaces | the ephemeral single-container lifecycle shapes durability and provisioning for every component | TB1, TB2 |",
   "type": "worked_example",
   "design_claim": "The devcontainer environment is an architecturally-significant binding because its ephemeral single-container lifecycle shapes durability and provisioning for every component.",
   "action": "relocate_to_plan",
   "target_note": "The concrete binding (GitHub Codespaces) moves to the plan's binding table. Principles keep the abstract row: 'Devcontainer environment \u2014 significant because its ephemeral single-container lifecycle shapes durability and provisioning.' TB1/TB2 internal refs retained.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 Platform foundations table, row 'Remote VCS of record'",
   "excerpt": "| **Remote VCS of record** | GitHub | the remote counterpart to local git-of-record; pull requests are a human-gate surface | TB2 (remote half) |",
   "type": "worked_example",
   "design_claim": "A remote version-control host is an architecturally-significant binding: it is the remote counterpart to the local git-of-record and its merge-request surface is a human-gate surface.",
   "action": "relocate_to_plan",
   "target_note": "Concrete binding (GitHub) moves to the plan. Principles keep abstract row: 'Remote VCS of record \u2014 significant because it is the remote counterpart to the local repo-of-record and its merge-request surface is a human gate.' TB2 internal ref retained.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a76 Platform foundations table, row 'IDE'",
   "excerpt": "| **IDE** | VSCode (inside the Codespace) | where the human reviewer adjudicates, diagrams render, and the agent extension runs | the human-gate + visual surfaces |",
   "type": "worked_example",
   "design_claim": "The IDE is an architecturally-significant binding: it is where the human reviewer adjudicates gates, diagrams render, and the agent extension runs.",
   "action": "relocate_to_plan",
   "target_note": "Concrete binding (VSCode in the Codespace) moves to the plan. Principles keep abstract row: 'IDE / human-review surface \u2014 significant because it is where the human reviewer adjudicates gates, diagrams render, and the agent client runs.'",
   "preserved": true,
   "loss": "Mostly sound, with one caveat. The concrete binding (VSCode-in-Codespace) and its two functional roles \u2014 human reviewer adjudicating gates, diagrams rendering, the agent client running \u2014 already land in the plan (D-PF-1 at implementation-plan.md:39,133 and the \u00a77 Codespaces lifecycle section), so the where/what facts survive. The abstract row the disposition proposes ('IDE / human-review surface \u2014 significant because it is where the human reviewer adjudicates gates, diagrams render, and the agent client runs') only survives if the REFACTORED architecture doc keeps an abstract \u00a76 platform-foundations section. Caveat to flag, not a blocker: the durable META-principle that frames this row \u2014 'platform choices can be architecturally-significant bindings, hard-to-reverse and structural, so a platform swap is an architecture-level change, not a plan-level technology choice' (governed-pipeline-architecture.md:232,242; D-PF-1) \u2014 has no home in the project-agnostic rubric at .claude/workflows/forensic-audit.RUBRIC/architecture-principles.md (no SP/R/A/TB encodes it). If 'principles doc' is read as that rubric, the WHY behind naming this a binding is lost; if it is the refactored arch doc, it survives. The disposition's target_note should name which 'principles' artifact holds the abstract row so the binding-significance rationale is not dropped."
  },
  {
   "locator": "\u00a76 Platform foundations table, row 'CI/CD platform'",
   "excerpt": "| **CI/CD platform** | GitHub Actions | where the drift sentinel, technology-boundary fitness functions, and conformance/freshness checks execute | enforces the TB-set + R8 |",
   "type": "worked_example",
   "design_claim": "The CI/CD platform is an architecturally-significant binding: it is where the drift sentinel, technology-boundary fitness functions, and conformance/freshness checks execute.",
   "action": "relocate_to_plan",
   "target_note": "Concrete binding (GitHub Actions) moves to the plan. Principles keep abstract row: 'CI/CD platform \u2014 significant because it is where the drift sentinel, fitness functions, and conformance/freshness checks execute.' Internal TB-set and R8 refs retained.",
   "preserved": true,
   "loss": "Sound. The concrete binding (GitHub Actions) and the three things it hosts \u2014 drift sentinel, technology-boundary fitness functions, conformance/freshness checks \u2014 land in the plan explicitly (WS-0 builds the fitness functions and the single drift sentinel in CI: implementation-plan.md:30,92,128,130,133; CI/Codespace parity at :396). The internal 'TB-set + R8' source refs are retained per the rule (intra-doc structure, not stripped). The abstract row survives in the refactored \u00a76. Same single caveat as the IDE row: the meta-principle that a CI/CD-platform swap is architecture-level (because enforcement is bound to it) is not captured in the agnostic rubric \u2014 but R8 ('the substrate audits itself') in the rubric does carry the underlying enforcement-self-audit idea, so the design-relevant content is better covered here than for the IDE row. No information a reader needs is lost."
  },
  {
   "locator": "\u00a79 \"Hardening the reviewer gate\", line 337",
   "excerpt": "Current evidence: a temperature-0 judge flipped its verdict on ~28% of re-runs of one pair; style/formatting bias now dominates (judges prefer identical content rendered in markdown over plain prose) while position bias has gone near-negligible on frontier judges; and reliability degrades monotonically as scoring moves from binary to multi-level ordinal.",
   "type": "repo_metric",
   "design_claim": "An LLM judge is a biased, non-deterministic instrument; a single-pass single judge with no rubric is the most fragile configuration, so the reviewer gate must be hardened (binary verdicts, calibrated rubric, abstain path, panel for high tiers).",
   "action": "strip_keep_claim",
   "restatement": "Empirically, LLM judges are non-deterministic (a temperature-0 judge can flip its verdict across re-runs of the same pair), are biased toward surface form (preferring identically-worded content rendered in richer formatting), and grow less reliable as the verdict space widens from binary to multi-level ordinal; treat the judge as a noisy instrument that must be hardened.",
   "target_note": "These are external LLM-as-judge research findings, not repo facts, but the specific percentage is a moving field benchmark that would age a timeless doc. Drop the exact figure; keep the qualitative claims (non-reproducibility, format-bias dominance, monotonic ordinal degradation). If the precise figures and their source matter, relocate the citation to architecture-evidence.md.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a79 \"Hardening the reviewer gate\", line 340",
   "excerpt": "A calibrated, human-authored rubric \u2014 the dominant reliability lever (atomic human-authored rubrics beat self-generated by ~26pp).",
   "type": "repo_metric",
   "design_claim": "A calibrated, human-authored rubric is the dominant reliability lever for the reviewer gate (more impactful than other hardening measures).",
   "action": "strip_keep_claim",
   "restatement": "A calibrated, human-authored rubric is the single dominant reliability lever for the reviewer gate \u2014 atomic, human-authored rubrics substantially outperform rubrics the judge generates for itself.",
   "target_note": "External research metric, not a repo metric, but the specific '~26pp' figure is a field benchmark that dates the doc. Drop the number; keep the comparative claim. Move the cited figure/source to architecture-evidence.md if provenance is wanted.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a79 \"Hardening the reviewer gate\", line 340",
   "excerpt": "The rubric is canonical (the review-discipline KB + the routing classifier), not improvised per run.",
   "type": "code_citation",
   "design_claim": "The rubric and the mechanical-vs-semantic routing classifier must live as canonical, declarative data shared across runs \u2014 not be improvised by the judge each run.",
   "action": "strip_keep_claim",
   "restatement": "The rubric and the routing classifier live as canonical, declarative artifacts shared across runs, not as per-run improvisation by the judge.",
   "target_note": "\"the review-discipline KB\" names a specific repo skill (KB-review-disciplines) and \"the routing classifier\" points at a specific repo artifact; the parenthetical naming of which concrete artifacts hold the rubric is repo-specific. The abstract claim (rubric is canonical, not improvised) survives in the restatement; the concrete artifact identity belongs in the plan.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a79 \"Hardening the reviewer gate\", line 343",
   "excerpt": "full-tier (feature / execution) gates use a k=3 cross-model panel; low-tier flows keep the single-pass judge.",
   "type": "worked_example",
   "design_claim": "Reviewer hardening is scaled to blast radius: high-tier pipelines get a diverse cross-provider quorum panel; low-tier flows keep a single-pass judge.",
   "action": "strip_keep_claim",
   "restatement": "Reviewer hardening scales with blast radius: the highest-tier pipelines run a diverse multi-member cross-provider panel, while low-tier flows keep a single-pass judge.",
   "target_note": "\"(feature / execution)\" names this repo's specific pipeline tiers. Generalize to \"highest-tier pipelines\"; the named tier identities are repo-specific and belong in the plan. The k=3 panel size is a design parameter, not a repo fact \u2014 keep it or state it as \"a small odd-sized panel (e.g. three members)\".",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a79 \"Hardening the reviewer gate\", line 346",
   "excerpt": "a re-run could flip a member from fail to pass (judges are ~28% non-reproducible) and silently drop a real finding (D-DR-1).",
   "type": "repo_metric",
   "design_claim": "Because judges are non-reproducible, each panel member's verdict must be its own recorded boundary event so a crash mid-panel cannot lose a completed member finding on replay.",
   "action": "strip_keep_claim",
   "restatement": "Because judges are non-reproducible, each panel member's verdict must be its own recorded boundary event, so a crash mid-panel cannot lose a completed member finding on replay.",
   "target_note": "Same external benchmark figure as line 337; drop the '~28%' number, keep the non-reproducibility claim. The '(D-DR-1)' is an intra-document defect cross-reference and is KEPT verbatim.",
   "preserved": true,
   "loss": "No information lost. The design claim (each panel member's verdict is its own recorded boundary event so a crash mid-panel cannot lose a completed member finding on replay) survives verbatim in the restatement, including the 'why' clause about losing a real finding. The dropped '~28%' figure is not unique to this line: it appears verbatim at line 337 ('flipped its verdict on ~28% of re-runs'), which is retained, so the benchmark survives in the document. The intra-document '(D-DR-1)' cross-reference is correctly kept. Verified line 337 carries the same number \u2014 the dedup rationale holds."
  },
  {
   "locator": "\u00a710 \"Primitive role assignment\", line 360",
   "excerpt": "the orchestrator is the sole dispatcher \u2014 realized as a Claude Code Dynamic Workflows script that spawns actors but forbids actor-spawns-actor (one-level nesting), so a gate can always be interposed (D-ORCH-1)",
   "type": "code_citation",
   "design_claim": "The orchestrator is the sole dispatcher: one-level nesting (actors cannot spawn actors) guarantees a gate can always be interposed at every handoff.",
   "action": "strip_keep_claim",
   "restatement": "The orchestrator is the sole dispatcher: it spawns actors but actors may not spawn actors (one-level nesting), so a gate can always be interposed at every handoff.",
   "target_note": "\"realized as a Claude Code Dynamic Workflows script\" is a platform/implementation-specific binding. Strip the realization clause; keep the sole-dispatcher / one-level-nesting design rule. The concrete platform (Claude Code Dynamic Workflows) moves to the plan. '(D-ORCH-1)' is an intra-document cross-reference and is KEPT.",
   "preserved": true,
   "loss": "No design information lost at the principles-doc level. The sole-dispatcher rule, the one-level-nesting constraint (actors cannot spawn actors), and the load-bearing 'why' (a gate can always be interposed at every handoff) all survive verbatim in the restatement. The '(D-ORCH-1)' cross-reference is kept. The only thing stripped is the platform-binding clause 'realized as a Claude Code Dynamic Workflows script' \u2014 a concrete realization, not a design rationale. Caveat (not a loss if honored): the disposition commits that clause to the plan; if it is dropped rather than relocated, the reader loses the concrete platform that realizes the rule. The design claim itself does not depend on it."
  },
  {
   "locator": "\u00a710 \"Primitive role assignment\", line 356",
   "excerpt": "| A machine-checkable rule | **Canonical YAML + a validator script** | A checkable rule in prose is an unenforced rule \u2014 it drifts. |",
   "type": "code_citation",
   "design_claim": "A machine-checkable rule belongs in a declarative canonical source plus a deterministic validator; a checkable rule left in prose is unenforced and drifts.",
   "action": "discard_with_rationale",
   "target_note": "\"Canonical YAML\" reads as a concrete file format but is used here as the document's own abstract term for the declarative canonical source (consistent with its use throughout the doc). It carries no repo path, line, or named file. Nothing repo-specific to strip; the row stays as written. Listed only for exhaustiveness \u2014 no edit needed.",
   "preserved": true,
   "loss": "No loss; no edit required. Verified line 356 reads exactly '| A machine-checkable rule | **Canonical YAML + a validator script** | A checkable rule in prose is an unenforced rule \u2014 it drifts. |' with no repo path, line, or named file. 'Canonical YAML' is the document's own abstract term for the declarative source, used consistently throughout. The rationale ('an unenforced rule drifts') stays in place. Correctly listed for exhaustiveness only."
  },
  {
   "locator": "\u00a79 Runtime-flow table, lines 305 & 309; \u00a78 Figure V5 caption line 277; \u00a79 line 337 (R10), line 344 (R20)",
   "excerpt": "\"(Part IV)\", \"(Part V)\", \"(\u00a79)\", \"(R10)\", \"(R20)\" \u2014 internal cross-references to other parts, sections, requirements.",
   "type": "evidence_map",
   "design_claim": "These anchor the reviewer-hardening and gate-timing claims to the document's own requirement and part structure (e.g. blast-radius scaling = R10; never-sole-authority = R20).",
   "action": "discard_with_rationale",
   "target_note": "Per the task's KEEP rule, intra-document cross-references (\u00a7N, Part N, Rn, Dn, figure ids) are document structure, not codebase references. They are retained verbatim and require no action. Listed for completeness to confirm they were evaluated and deliberately NOT stripped.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a713 intro (line 398)",
   "excerpt": "The input-digest detection at the core of this mechanism is empirically validated and is treated as decided. Six moves:",
   "type": "repo_metric",
   "design_claim": "Input-digest-based staleness detection is the validated core of the freshness mechanism and is settled as the v1 approach.",
   "action": "strip_keep_claim",
   "restatement": "Input-digest staleness detection is the core of this mechanism; the field's prior art (build systems, data pipelines) establishes it as the settled approach. Six moves:",
   "preserved": true,
   "loss": "Design claim survives (input-digest detection is the settled/decided v1 core of the freshness mechanism). Minor justification shift worth flagging: the original asserts the approach is 'empirically validated', the restatement re-grounds the same decided-status in 'the field's prior art (build systems, data pipelines)'. The original phrase carried no number and no repo citation \u2014 it was an unsupported assertion, so no measured evidence is being discarded. The \u00a712 table (lines 388-394, Bazel/Dagster/dbt) already establishes that prior-art basis in-document, so the restatement's grounding is actually present and verifiable. If any concrete empirical-validation evidence (e.g. a measured run) ever existed behind 'empirically validated', it should land in an evidence file rather than vanish \u2014 but none is cited in the source text, so on the text as written nothing is lost."
  },
  {
   "locator": "\u00a712 table, Detect staleness row (line 390)",
   "excerpt": "**Content/input hashing** (Bazel content-addressing; Dagster hashes code-version + input data-versions). Version counters second; timestamps weakest.",
   "type": "code_citation",
   "design_claim": "Content/input hashing is the strongest staleness-detection signal; version counters are second-best; timestamps are weakest.",
   "action": "strip_keep_claim",
   "restatement": "Content/input hashing is the strongest staleness signal (the approach mature build systems and data-orchestration tools converge on). Version counters are second; timestamps weakest. (External prior-art tool names \u2014 Bazel, Dagster \u2014 are industry references, not this codebase; they may be kept as illustrative citations in a principles doc, but if a fully tool-neutral phrasing is preferred this restatement drops them.)",
   "preserved": true,
   "loss": "No loss. The ranked design claim (content/input hashing strongest, version counters second, timestamps weakest) survives verbatim. Bazel and Dagster are external industry prior-art references, not this codebase, so the task's strip-repo-references rule does not require removing them \u2014 and keeping them preserves the 'why' (these are the mature systems that converged on content-addressing). The disposition correctly keeps them as illustrative citations while offering a tool-neutral fallback phrasing; either form preserves the design information. Note: removing the parenthetical tool names entirely (the optional tool-neutral variant) would slightly weaken the empirical grounding of the ranking but would not lose the ranking claim itself."
  },
  {
   "locator": "\u00a712 table, Propagate row (line 391)",
   "excerpt": "**Mark-and-flood the reverse transitive closure** \u2014 invalidate exactly the downstream descendants (dbt `state:modified+`; Bazel reverse-closure).",
   "type": "code_citation",
   "design_claim": "Staleness propagates by marking the reverse transitive closure \u2014 invalidating exactly the downstream descendants of a changed input.",
   "action": "strip_keep_claim",
   "restatement": "Staleness propagates by marking the reverse transitive closure \u2014 invalidating exactly the downstream descendants of a changed input (the selection model mature build and transformation tools converge on). (External tool names dbt/Bazel are industry prior art, not this codebase; retained as optional illustrative citations, but the restatement reads cleanly without them.)",
   "preserved": true,
   "loss": "No information lost. The design claim 'invalidate exactly the downstream descendants of a changed input via the reverse transitive closure' carries its own rationale (precise, scoped invalidation rather than over-broad re-derivation), and that survives in the restatement. dbt's state:modified+ and Bazel's reverse-closure are external industry prior art, not this codebase \u2014 the disposition correctly classifies them as optional illustrative citations and the restatement reads cleanly without them. The only thing that vanishes is the convenience of two concrete proof points that the pattern is battle-tested; that motivational weight is already carried by the \u00a712 intro (item 6, kept as-is) which names data pipelines and build systems as the solved-prior-art fields. No constraint or why is dropped."
  },
  {
   "locator": "Figure V7 mermaid nodes (lines 374-375)",
   "excerpt": "PRD2[\"PRD v2\"] --> BP[\"Blueprint v3<br/>derived_from PRD v2\"]; BP --> PLAN[\"Plan v1<br/>derived_from Blueprint v3\"]",
   "type": "code_citation",
   "design_claim": "Each downstream artifact records which upstream version it was derived from; a mid-run upstream revision silently orphans (staleness-cascades to) its descendants along those derivation edges.",
   "action": "strip_keep_claim",
   "restatement": "Keep the figure but render the edge label as the generic relation 'derived-from upstream vN' rather than the repo's literal field name. The diagram illustrates the abstract claim (an upstream revision orphans descendants along recorded derivation edges); PRD/Blueprint/Plan are illustrative artifact-type names for the example, not codebase files, so they stay as a generic worked illustration.",
   "preserved": true,
   "loss": "No load-bearing information lost. The figure's job is to show the cascade shape (an upstream revision orphans descendants along recorded derivation edges); genericizing the edge label from the literal field name 'derived_from' to 'derived-from upstream vN' does not weaken that \u2014 the literal field-name binding is carried by move 1 and by the plan (implementation-plan.md:178, frontmatter-fields.yaml). PRD/Blueprint/Plan are illustrative artifact-type names, not repo files, and rightly stay as a worked example. Minor watch-item, not a loss: the doc must keep the figure's node label and \u00a713 move 1's terminology consistent so a reader does not read the genericized figure as describing a different mechanism than the concrete one in the plan; this is a copy-edit coherence concern, not dropped information."
  },
  {
   "locator": "\u00a713 move 3 (line 402)",
   "excerpt": "**The walk seeds from the in-git `derived_from` graph** \u2014 the very edges the digests are stamped on (move 1) \u2014 *not* a separate `pipelines.yaml` traversal. The two must not diverge: a completeness check asserts every `derived_from` edge maps to a declared manifest lineage edge, so the stale set can't be silently incomplete. (Walking the manifest while stamping on `derived_from` \u2014 two graphs never reconciled \u2014 would surface an *incomplete* stale set and let a terminal artifact whose handoff already passed ship stale.)",
   "type": "code_citation",
   "design_claim": "The staleness walk must seed from the SAME graph the digests are stamped on (the per-artifact recorded-derivation graph), not a separate manifest traversal; a completeness check must assert the two graphs agree, or the stale set can be silently incomplete and a terminal artifact can ship stale.",
   "action": "strip_keep_claim",
   "restatement": "The staleness walk seeds from the same derivation graph the digests are stamped on (move 1) \u2014 the per-artifact recorded-derivation edges \u2014 not a separately-declared pipeline manifest. The two representations must not diverge: a completeness check asserts every recorded-derivation edge maps to a declared manifest lineage edge, so the stale set cannot be silently incomplete. (Walking the manifest while stamping on the recorded-derivation edges \u2014 two graphs never reconciled \u2014 would surface an incomplete stale set and let a terminal artifact whose handoff already passed ship stale.)",
   "preserved": true,
   "loss": "No information lost. This is the strongest-preserved item: the entire rationale chain is retained almost verbatim \u2014 the two graphs must seed from the same source, the completeness check asserting every recorded-derivation edge maps to a declared manifest lineage edge, and the explicit failure consequence (an incomplete stale set lets a terminal artifact whose handoff already passed ship stale). Only the literal identifiers ('derived_from', 'pipelines.yaml') are swapped for generic terms ('recorded-derivation edges', 'declared manifest'). The why, the constraint, and the failure mode all survive. The concrete identifiers also already live in the plan (implementation-plan.md:181), which carries the same divergence rationale, so the binding is not orphaned."
  },
  {
   "locator": "\u00a713 move 3 (line 402), substrate phrase \"in-git\"",
   "excerpt": "The walk seeds from the in-git `derived_from` graph",
   "type": "code_citation",
   "design_claim": "The lineage/derivation graph is held in the version-control substrate rather than a separate provenance store.",
   "action": "relocate_to_plan",
   "target_note": "The specific choice that the derivation graph lives 'in-git' (and the literal field name `derived_from` and manifest filename `pipelines.yaml`) is a repo-substrate decision. The principles doc should say 'the derivation graph held in the durable substrate of record'; the concrete binding to git, the `derived_from` field, and the `pipelines.yaml` manifest move to the plan as the implementation realization. (Note: the git-as-record substrate choice is registered separately as TB2, an intra-document trade-off that itself stays.)",
   "preserved": true,
   "loss": "No information lost across the two landing sites, but this item demands the most care and only passes because of an existing safety net. The relocation strips the substrate choice (in-git, the derived_from field, the pipelines.yaml manifest) from the principles doc, leaving the abstract 'durable substrate of record'. The risk would be losing the WHY the derivation graph lives in version control rather than a separate provenance store. That rationale is NOT in the stripped sentence itself \u2014 it lives in the TB2 trade-off (governed-pipeline-architecture.md:1073: 'anything that must survive rebuild is committed; the runtime dir is ephemeral') and is reinforced by D-OBS-2 / TB11 (no second service for lineage already held in git). The disposition explicitly flags that TB2 stays, and the plan (implementation-plan.md:181, :30, :275) already carries the concrete in-git binding WITH its rationale. So both halves land: abstract principle in the principles doc, concrete realization + why in the plan + TB2. Caveat for the executor: if TB2 were ever stripped in a sibling disposition, this relocation WOULD orphan the substrate rationale \u2014 the preservation here is contingent on TB2 surviving as asserted."
  },
  {
   "locator": "\u00a711 (line 368)",
   "excerpt": "A downstream artifact was derived from v2; nothing forces it to re-sync to the corrected upstream. ... In plain terms this is a **cache-invalidation problem over the run's artifact dependency graph**",
   "type": "worked_example",
   "design_claim": "When reconciliation revises an upstream artifact, downstream artifacts derived from the old version become stale; this is fundamentally a cache-invalidation problem over the run's artifact dependency graph, and unchecked the staleness is caught only by the expensive late auditor.",
   "action": "strip_keep_claim",
   "restatement": "Keep as-is. This is a generic illustration (a requirements doc revised v2 to v3, a downstream artifact left on v2) using abstract artifact types, with no codebase file, count, or named run. The cache-invalidation framing is the timeless design claim and survives unchanged.",
   "preserved": true,
   "loss": "No information lost. The disposition's 'keep as-is' is correct and conservative \u2014 there is nothing to strip. The passage is a fully generic illustration (requirements doc revised v2 to v3, downstream artifact left on v2) using abstract artifact types, with no codebase file, count, or named run. The timeless design claim (this is a cache-invalidation problem over the run's artifact dependency graph, and unchecked it is caught only by the expensive late auditor \u2014 the worst place to catch it) is exactly the kind of rationale a principles doc should carry, and it survives intact. The 'caught only by the late auditor' clause is the cost-rationale that motivates the whole freshness gate; keeping the passage verbatim preserves it."
  },
  {
   "locator": "\u00a712 intro (line 386)",
   "excerpt": "No major agent framework detects a superseded-upstream derivation and forces re-sync; the agent world either **pins** (durable-execution engines reuse cached results and contain version *skew*, but do not re-derive dependents) or relies on **forward-only gating**. The settled answer lives in the two adjacent fields that solved this \u2014 data pipelines and build systems",
   "type": "code_citation",
   "design_claim": "Agent frameworks do not solve superseded-upstream re-sync (they pin or forward-only-gate); the solved prior art lives in data pipelines and build systems.",
   "action": "strip_keep_claim",
   "restatement": "Keep as-is. This is field-landscape prior art (agent frameworks vs. data pipelines vs. build systems), not a reference to this codebase \u2014 it is exactly the kind of timeless motivation a principles doc should carry. No repo file, count, named run, or ADR-as-evidence appears.",
   "preserved": true,
   "loss": "No information lost. Correctly kept as-is. This is field-landscape prior art (agent frameworks pin or forward-only-gate and do not solve superseded-upstream re-sync; the solved answer lives in data pipelines and build systems), not a reference to this codebase \u2014 no repo file, count, named run, or ADR-as-evidence. It is timeless motivation that belongs in a principles doc, and it also backstops item 1: it is where the 'why these two adjacent fields' justification lives, so retaining it is what lets the dbt/Bazel citations in the Propagate row be safely demoted to optional."
  },
  {
   "locator": "\u00a715, line 437",
   "excerpt": "we borrow OpenLineage's run-lifecycle vocabulary to shape that view, but run no OpenLineage service (it would cost a second container for provenance we already hold in git \u2014 TB2)",
   "type": "code_citation",
   "design_claim": "Artifact-lineage telemetry should reuse provenance already captured by the freshness mechanism rather than standing up a separate lineage backend; borrow the lineage vocabulary, not the service.",
   "action": "strip_keep_claim",
   "restatement": "Borrow the lineage standard's run-lifecycle vocabulary to shape the artifact-provenance view, but run no separate lineage service: the provenance is already captured by the freshness mechanism's derivation graph in version control, so a second service would buy nothing (TB2).",
   "target_note": "TB2 is an intra-document technology-boundary id and stays; OpenLineage/OpenTelemetry are external standards and stay. Only the implicit 'we already hold in git' repo-substrate phrasing is generalized to 'in version control / the freshness mechanism's derivation graph'.",
   "preserved": true,
   "loss": "No reader-critical loss. The abstract design claim (borrow the lineage standard's run-lifecycle vocabulary, run no separate lineage service) and its load-bearing 'why' (a second container would buy nothing because the provenance is already captured by the freshness mechanism's derivation graph) both survive the restatement. Generalizing 'git' to 'version control / the freshness mechanism's derivation graph' loses no design information \u2014 the substrate identity is a repo fact, not a design constraint, and the freshness/Part IV cross-reference and the TB2 technology-boundary id are both kept. One cosmetic nit: the restatement genericizes 'OpenLineage' to 'the lineage standard' in this sentence, but the \u00a715 table (line 442) still names OpenLineage explicitly, so the reader can still resolve which standard is meant. No rationale or constraint is dropped."
  },
  {
   "locator": "\u00a715, line 444",
   "excerpt": "the **backend ingests OpenTelemetry only**: artifact provenance is the freshness gate's `derived_from` graph (Part IV), queryable from git and projected into the run-summary",
   "type": "code_citation",
   "design_claim": "The telemetry backend ingests execution telemetry only; artifact provenance is read from the freshness mechanism's in-repository derivation graph and projected into the run summary \u2014 no separate provenance backend.",
   "action": "strip_keep_claim",
   "restatement": "The telemetry backend ingests execution telemetry only; artifact provenance comes from the freshness mechanism's derivation graph (Part IV), queryable directly from version control and projected into the run-summary view \u2014 there is no separate provenance backend.",
   "target_note": "`derived_from` is a concrete field name and 'git' is the concrete substrate; generalize to 'derivation graph' and 'version control'. Part IV cross-reference stays.",
   "preserved": true,
   "loss": "No reader-critical loss. The claim (backend ingests execution telemetry only; artifact provenance is read from the freshness mechanism's derivation graph and projected into the run-summary; no separate provenance backend) is preserved, including the Part IV cross-reference. Generalizing the concrete field name 'derived_from' to 'derivation graph' and 'git' to 'version control' removes repo-specific identifiers, not design meaning. The original's 'a second service would buy nothing' is mildly attenuated to 'there is no separate provenance backend,' but the no-second-backend conclusion and its reason (provenance already captured) both survive. The same 'why' is also carried by the line-437 restatement, so the rationale is not orphaned."
  },
  {
   "locator": "\u00a717, line 460",
   "excerpt": "the runtime captures **per-actor spans** (and the tool calls inside them) nested under the stage \u2014 the \"span = step\" depth, the same per-agent record the workflow runtime already writes",
   "type": "code_citation",
   "design_claim": "Telemetry is two-level: the coordinator emits stage-boundary events and the runtime captures per-actor spans (with their tool calls) nested under the stage; actors stay stateless and are instrumented at their boundaries.",
   "action": "strip_keep_claim",
   "restatement": "Telemetry is two-level: the coordinator emits stage-boundary events and the runtime captures per-actor spans (and the tool calls inside them) nested under the stage \u2014 the \"span = step\" depth.",
   "target_note": "Strip the clause 'the same per-agent record the workflow runtime already writes' \u2014 it asserts that THIS repo's runtime already emits such records (repo-state fact). The design claim (two-level telemetry, runtime-captured per-actor spans) survives intact in the surrounding sentence.",
   "preserved": true,
   "loss": "No loss. This is a confirm-no-substantive-change item: the stateless-actors / instrument-at-boundaries claim is preserved in substance, and V4 is correctly treated as an intra-document figure id (kept, not a codebase citation). The 'why' actors stay stateless is implicit in the surrounding \u00a717 design and the broader file-handoff boundary discipline; nothing reader-critical is removed."
  },
  {
   "locator": "\u00a717, line 460",
   "excerpt": "Actors stay stateless: they do not write the log themselves; the runtime instruments them at their boundaries (hence the Actors \u2192 Observability edge in V4)",
   "type": "evidence_map",
   "design_claim": "Actors remain stateless and never write the log directly; the runtime instruments them at their boundaries \u2014 keeping the emit responsibility off the actor.",
   "action": "strip_keep_claim",
   "restatement": "Actors stay stateless: they never write the log themselves; the runtime instruments them at their boundaries (the Actors \u2192 Observability edge in V4).",
   "target_note": "No repo specifics here \u2014 V4 is an intra-document figure id and is explicitly KEEP. No change needed beyond confirming it is not a codebase citation; the abstract claim and the figure reference both stay.",
   "preserved": true,
   "loss": "No loss. This is a confirm-no-substantive-change item: the stateless-actors / instrument-at-boundaries claim is preserved in substance, and V4 is correctly treated as an intra-document figure id (kept, not a codebase citation). The 'why' actors stay stateless is implicit in the surrounding \u00a717 design and the broader file-handoff boundary discipline; nothing reader-critical is removed."
  },
  {
   "locator": "\u00a718 Export, line 487",
   "excerpt": "landing in a **self-hostable single-container OTel backend** (the plan names the product \u2014 D13/D17 \u2014 now open to re-evaluation). A log-shipping connector is an optional convenience, so the script is preferred.",
   "type": "code_citation",
   "design_claim": "Export the system-of-record log via a small SDK-free script that POSTs over OTLP to a self-hostable single-container telemetry backend; the specific product is deferred to implementation; the pipeline never depends on the backend being up.",
   "action": "strip_keep_claim",
   "restatement": "Export the log via a small SDK-free script that POSTs it over OTLP to a self-hostable single-container telemetry backend; the specific product is an implementation choice left open. A log-shipping connector is an optional convenience, so the script is preferred.",
   "target_note": "Strip 'the plan names the product' (a pointer to the repo-specific plan document as the place the product is chosen). D13/D17 are intra-document decision-record ids and stay as references. The principle \u2014 defer product choice, SDK-free OTLP export, backend-optional \u2014 survives. The fact that THIS doc's companion plan picks a product is repo provenance: relocate that pointer to the plan.",
   "preserved": true,
   "loss": "No reader-critical loss. The design claim survives in full: SDK-free OTLP-POST export, self-hostable single-container OTel backend, product choice left open, script preferred over a log-shipping connector. The separate 'the pipeline never depends on the backend being up' sentence is untouched in line 487, so the backend-optional constraint is retained. Dropping the 'the plan names the product' pointer and the D13/D17 references is safe only because the paired relocation (item 6) lands that provenance in the plan, where D13/D17 already appear (plan lines 37, 39, 41) \u2014 so the pointer is moved, not lost. If item 6 were not applied, this would orphan the 'who decides the product' provenance."
  },
  {
   "locator": "\u00a718 Export, line 487 (provenance of product choice)",
   "excerpt": "the plan names the product \u2014 D13/D17 \u2014 now open to re-evaluation",
   "type": "adr_as_evidence",
   "design_claim": "The concrete backend product is a deferred, revisable implementation decision rather than an architectural commitment.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "The plan (the repo-specific brownfield doc) is where the concrete OTel backend product is named and may be re-evaluated. Move the 'plan names the product / open to re-evaluation' provenance there; the principles doc keeps only 'the specific product is an implementation choice left open.'",
   "preserved": true,
   "loss": "No loss; relocation lands in a richer existing home. The design claim (the concrete backend is a deferred, revisable implementation decision, not an architectural commitment) survives in the architecture doc via item 5's 'the specific product is an implementation choice left open.' The repo-specific provenance \u2014 that the plan names GreptimeDB for D13 and holds it revisable \u2014 already exists and is more complete in implementation-plan.md (line 41: chosen 2026-05-30 via technology-evaluation, ADR pending freeze-lift; line 353: two-way door / re-eval trigger). The empty restatement is correct for a pure relocation. No rationale or constraint is dropped; the 'revisable' framing is reinforced, not weakened."
  },
  {
   "locator": "\u00a718 Durability, line 486",
   "excerpt": "Point the backend's persistent store at a **mounted persistent volume** so run history survives restarts and rebuilds; commit the run-summary projection to the deliverable archive for a durable human record.",
   "type": "code_citation",
   "design_claim": "Durability is achieved by pointing the backend store at a persistent volume so history survives restarts/rebuilds, plus committing the run-summary projection to the deliverable for a durable human record; there is no immutable/WORM/retention-locked audit trail.",
   "action": "strip_keep_claim",
   "restatement": "Achieve durability by pointing the backend's store at a persistent volume so run history survives restarts and rebuilds, and by committing the run-summary projection to the deliverable for a durable human record; there is no immutable/WORM or retention-locked audit trail.",
   "target_note": "'mounted persistent volume' and 'deliverable archive' are generic infra/pipeline concepts, not codebase citations; near-verbatim, just smoothed. No repo path or named artifact here.",
   "preserved": true,
   "loss": "No loss. The excerpt is generic infrastructure vocabulary (a mounted persistent volume; committing the run-summary projection to the deliverable) with no repo path or named artifact, so it is barely a strip at all \u2014 the restatement is near-verbatim. The load-bearing negative claim (no immutable/WORM/object-lock store, no retention lock) survives intact. Adversarial check on what surrounds the excerpt: the same bullet continues with a distinct design claim \u2014 'Config-versioning (pinning prompt/model/tool/template versions per event) is an optional reproducibility aid, not a requirement.' That sentence is NOT in this disposition's excerpt and is a separate sentence, so applying this disposition does not touch it; it must independently remain in the principles doc. Flagging it only as a do-not-collateral-damage watch-item, not a loss of this item."
  },
  {
   "locator": "\u00a718 Online vs offline evals, line 509",
   "excerpt": "*Offline* = the historical-run regression corpus (rollout WS-0), replayed against every change",
   "type": "named_run",
   "design_claim": "An offline regression corpus of historical runs is replayed against every change (new gate, contract bump, manifest edit, rubric/prompt change) to prove each iteration is concretely better, not merely different.",
   "action": "strip_keep_claim",
   "restatement": "Offline evaluation replays the historical-run regression corpus against every change to give a concrete before/after, so each iteration is proven better, not merely different.",
   "target_note": "'WS-0' is an intra-document work-stream id (foundational rollout work-stream) and is in the KEEP set; it is not a repo run-slug. 'historical-run regression corpus' is an abstract construct. Keep WS-0 reference as-is; no repo specifics to strip.",
   "preserved": false,
   "loss": "Real loss. The restatement silently drops the enumerated change-types the source spells out: 'a new gate, a contract bump, a manifest edit, a rubric/prompt change.' That list is a design constraint, not decoration \u2014 it defines the SCOPE of what triggers an offline replay (config/contract/manifest/rubric changes, i.e. the full substrate surface), and the disposition's own design_claim names all four. Collapsing them to 'every change' loses the reader's signal about which classes of change the regression corpus must guard. Since the disposition's stated rationale is that there is nothing repo-specific to strip here (it is a near no-op), the restatement should have been near-verbatim and kept the enumeration. Secondary concern: the disposition classifies 'rollout WS-0' as an intra-document KEEP, but WS-0 is actually a workstream label defined in this project's implementation-plan.md (WS-0 Substrate foundation), so in a codebase-agnostic principles doc it reads as a plan-specific reference, not pure intra-document structure (\u00a7N/Rn/figure) \u2014 its KEEP status is at least debatable. The primary, concrete defect is the dropped change-type enumeration."
  },
  {
   "locator": "Recovery, line 496",
   "excerpt": "real receivers \u2014 `git push`, file writes, Slack, generic MCP POSTs \u2014 **ignore caller-supplied keys**",
   "type": "code_citation",
   "design_claim": "Idempotency cannot rest on the receiver because real-world side-effect receivers ignore caller-supplied keys; the coordinator must therefore record an 'effect-committed' marker before the side effect and check it on replay (the durable idempotency-table pattern).",
   "action": "strip_keep_claim",
   "restatement": "Because real-world side-effect receivers (version-control pushes, file writes, message-delivery, generic remote POSTs) commonly ignore caller-supplied idempotency keys, idempotency cannot rest on the receiver: the coordinator writes a durable 'effect-committed' record before the side effect and checks it on replay.",
   "target_note": "`git push`, `Slack`, `MCP POSTs` are illustrative receiver examples; the first two are generic-enough technology names but MCP is a project-platform term. Generalize the list to abstract receiver categories so the principle is substrate-agnostic. AWS-Powertools is an external consensus citation and stays.",
   "preserved": true,
   "loss": "No loss. The full design claim survives: receivers commonly ignore caller-supplied idempotency keys, so idempotency cannot rest on the receiver; the coordinator writes a durable effect-committed record BEFORE the side effect and checks it on replay. All four receiver categories are retained, merely generalized to substrate-agnostic terms (git push -> version-control pushes; Slack -> message-delivery; MCP POSTs -> generic remote POSTs; file writes unchanged), which is exactly the right move for a portable doc since MCP is a project-platform term. The external AWS-Powertools consensus citation is correctly kept (it is consensus, not a repo fact). Nothing a reader needs \u2014 the why (receivers ignore keys) and the mechanism (pre-effect durable marker) \u2014 is lost."
  },
  {
   "locator": "Recovery, line 495",
   "excerpt": "completed `stage.complete` / `gate.result` boundaries replay from the log rather than re-execute. The journal append is **atomic** (write-temp-then-rename ...)",
   "type": "code_citation",
   "design_claim": "The run-event log doubles as the crash-recovery journal: completed step boundaries replay from the log instead of re-executing, and the journal append must be atomic (write-temp-then-rename, not fsync alone) so a torn write can't drop a completed boundary.",
   "action": "strip_keep_claim",
   "restatement": "The run-event log doubles as the recovery journal: on restart, completed stage/gate boundaries replay from the log rather than re-execute, and each journal append is atomic (write-temp-then-rename \u2014 fsync alone gives durability, not atomicity) so a crash mid-write cannot leave a torn line that drops a completed boundary.",
   "target_note": "`stage.complete` / `gate.result` are this document's own event-schema names defined in the \u00a717 table, not codebase symbols \u2014 intra-document. No repo path. Claim and event names both stay; near-verbatim.",
   "preserved": true,
   "loss": "No loss. Faithful, near-verbatim restatement that keeps every rationale: completed boundaries replay rather than re-execute; the append is atomic; write-temp-then-rename specifically because fsync gives durability but NOT atomicity against a torn line; a torn final line would drop a completed boundary. Verified that stage.complete / gate.result are this document's own run-event-schema names defined in the \u00a717 table (lines 465-466), i.e. intra-document vocabulary in the KEEP set, not codebase symbols \u2014 so retaining them is correct. The bullet's following Caveat sentence (plaintext journal -> keep credentials/PII out, TB10; log is bounded) is outside this excerpt and untouched by the disposition; it must remain independently, but that is a watch-item, not a loss of this item."
  },
  {
   "locator": "\u00a719, line 543 / 548 / 602-602 \u2014 \"the MCP model\" / \"MCP is the exemplar; observability follows it\" / \"a designer `folded_into` the layers\"",
   "excerpt": "Designer \u2014 a per-layer designer agent (or `folded_into` other designers, the MCP model). \u2026 cross-cutting-domain \u2014 domains that ship a triad but are not one of the layers and touch many of them. **MCP is the exemplar; observability follows it.**",
   "type": "code_citation",
   "design_claim": "A domain bundle may legitimately omit a designer by folding it into other designers; cross-cutting domains (which touch many layers but are not a layer) are a distinct registered kind, and naming a concrete exemplar shows the pattern is real rather than hypothetical.",
   "action": "strip_keep_claim",
   "restatement": "A domain bundle may legitimately omit a standalone designer by folding that responsibility into the designers of the layers it touches; a cross-cutting domain is a distinct registered kind for capabilities that span many layers without being a layer themselves.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a720, line 552 \u2014 \"the canonical registry \u2026 install sites \u2026 project `.claude/`, user `~/.claude/`, the MCP config, the devcontainer\"",
   "excerpt": "Every domain is declared once, in the canonical registry, with its full bill-of-materials and its **install sites** (the scopes and paths where each part lives \u2014 project `.claude/`, user `~/.claude/`, the MCP config, the devcontainer).",
   "type": "code_citation",
   "design_claim": "The registry must record, per domain part, the concrete scopes/paths where it is installed, because completeness and orphan checking require knowing every place a part can live (including more than one scope).",
   "action": "strip_keep_claim",
   "restatement": "Every domain is declared once in the canonical registry, with its full bill-of-materials and its install sites \u2014 the set of scopes and paths where each part lives \u2014 so that conformance and orphan checks know every location a part can occupy, including multiple scopes.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a721, line 578 \u2014 \"every installed `KB-*` / `auditing-*` / `design-*` skill, MCP server, and hook is declared\"",
   "excerpt": "every installed `KB-*` / `auditing-*` / `design-*` skill, MCP server, and hook is declared in the registry; anything installed-but-undeclared, **across project and user scope**, is flagged.",
   "type": "code_citation",
   "design_claim": "Orphan detection must scan every installable artifact class that can carry a domain part (knowledge bases, auditors, designers, integrations, lifecycle hooks) across all scopes, and flag anything installed but not declared in the registry.",
   "action": "strip_keep_claim",
   "restatement": "Every installed artifact that can carry a domain part \u2014 a knowledge base, an auditor, a designer, an integration server, or a lifecycle hook \u2014 must be declared in the registry; anything installed but undeclared, across every scope, is flagged as an orphan.",
   "preserved": true,
   "loss": "No information loss. The restatement faithfully generalizes the three concrete skill-name prefixes (KB-*, auditing-*, design-*) and the MCP-server/hook artifact classes into their domain-part categories (knowledge base, auditor, designer, integration server, lifecycle hook), and keeps the load-bearing constraints intact: declared-in-registry, across-every-scope, flag-anything-installed-but-undeclared. The actual glob prefixes \u2014 which are pure machine-checkable citations \u2014 survive where they belong, in the plan's orphan-check deliverable (implementation-plan.md:225, 'KB-* / auditing-* / design-* skill, .mcp.json server, or ~/.claude skill/hook ... scope-aware project + user'). A reader loses no rationale or constraint."
  },
  {
   "locator": "\u00a721, line 578 \u2014 \"the orphaned capability the manual removal missed\"",
   "excerpt": "This is the check that would have caught the orphaned capability the manual removal missed.",
   "type": "named_run",
   "design_claim": "Manual, ad-hoc removal of a domain part leaves orphans; a deterministic orphan check is what catches them \u2014 motivated by a real incident where a manual removal left a capability orphaned.",
   "action": "relocate_to_plan",
   "target_note": "Move the specific past-incident provenance (a manual removal that orphaned a capability the deterministic check would have caught) to the plan as brownfield motivation. The principles doc keeps only the abstract claim: a deterministic orphan check catches orphans that ad-hoc manual removal leaves behind.",
   "preserved": true,
   "loss": "No load-bearing loss. The abstract design claim (a deterministic orphan check catches what ad-hoc manual removal leaves behind) is kept in the principles restatement, and the concrete past-incident provenance lands in the plan twice \u2014 the teardown dry-run 'reconstructs the gitnexus removal from a registry entry and would have enumerated the user-scope ~/.claude/skills/gitnexus-* + hook (the part the manual removal missed)' (implementation-plan.md:232) and the regression-guard framing 'gitnexus is gone \u2014 this is the regression guard that would have caught it' (implementation-plan.md:66). The one detail the architecture line itself never named \u2014 which capability (gitnexus) and which scope (user-scope skill + hook) \u2014 is actually MORE specific in the plan than in the source, so relocation improves provenance rather than losing it."
  },
  {
   "locator": "\u00a722, line 586 \u2014 \"Terraform removes a provider only when every reference drops it; Kubernetes keeps an object \\\"Terminating\\\" until finalizers confirm cleanup\"",
   "excerpt": "This is the lesson the field is unanimous on \u2014 Terraform removes a provider only when every reference drops it; Kubernetes keeps an object \"Terminating\" until finalizers confirm cleanup and owner-references prevent orphans.",
   "type": "worked_example",
   "design_claim": "Removal must be a reconciliation across all install sites with a completion gate, not a delete \u2014 mirroring established infrastructure-tool practice (reference-counted teardown, finalizer-gated termination, owner-reference orphan prevention).",
   "action": "strip_keep_claim",
   "restatement": "Removal must be a reconciliation across every install site with a completion gate rather than a bare delete, mirroring the established discipline of reference-counted teardown and finalizer-gated termination in mature infrastructure tooling.",
   "preserved": false,
   "loss": "Minor but real loss in the restatement: the source names THREE distinct prior-art mechanisms \u2014 reference-counted teardown (Terraform), finalizer-gated termination (Kubernetes 'Terminating'), AND owner-references preventing orphans (Kubernetes). The restatement keeps the first two verbatim but DROPS 'owner-reference orphan prevention.' That third mechanism is not redundant with finalizers: finalizers gate completion, whereas owner-references are the structural device that makes orphan-prevention automatic on cascade \u2014 and orphan-prevention is precisely the \u00a722 lesson (the orphan check as completion gate). The disposition's own design_claim field correctly lists 'owner-reference orphan prevention' as part of the claim, but the restatement silently omits it. Mitigation: the worked example survives intact in the prior-art references section (governed-pipeline-architecture.md:1025, 'Kubernetes finalizers + owner-references'), so the citation is not vanished \u2014 but a \u00a722 reader relying on the restatement alone loses the third analogy that motivates orphan-prevention specifically. Recommend the restatement append '...and owner-reference-style orphan prevention.'"
  },
  {
   "locator": "\u00a723, line 602 \u2014 \"a Platform KB (OpenTelemetry + the self-hosted backend the plan selects; the lineage view is the freshness gate's in-git `derived_from` graph, not an ingested OpenLineage backend)\"",
   "excerpt": "Its bundle: a Platform KB (OpenTelemetry + the self-hosted backend the plan selects; the lineage view is the freshness gate's in-git `derived_from` graph, not an ingested OpenLineage backend), a Design KB (the discipline in Part V), an Auditor (checking the run-event schema, the emitter wiring, the persistent-volume store, the export path), and a designer `folded_into` the layers that instrument (backend, query, api, cicd, iac).",
   "type": "worked_example",
   "design_claim": "The first domain delivered through the pattern proves the scaffold and conformance on a real case; its bundle illustrates each BOM slot (platform KB, design KB, auditor, folded-in designer) and registers as a cross-cutting domain with recorded install sites so it is itself teardown-able.",
   "action": "strip_keep_claim",
   "restatement": "The first cross-cutting domain delivered through the pattern serves double duty \u2014 it both ships its capability and exercises the scaffold and conformance check on a real case. Its bundle fills each slot of the bill-of-materials (a platform KB, a design KB, an auditor over its emitted state, and a designer folded into the layers that instrument), and it registers with recorded install sites so it is itself teardown-able.",
   "preserved": true,
   "loss": "No loss of the load-bearing constraint. The architecturally-significant negative constraint \u2014 lineage is served by the in-git derived_from freshness graph, NOT an ingested OpenLineage backend (decision D-OBS-2) \u2014 is preserved in many places: the technology-boundaries set (implementation-plan.md:30, 'artifact lineage served by the in-git freshness graph rather than an ingested lineage backend \u2014 D-OBS-2'), the event-schema note (implementation-plan.md:261), and both observability KB deliverables (implementation-plan.md:274-275). The restatement of \u00a723 keeps the bundle-slot illustration (platform KB, design KB, auditor over emitted state, folded-in designer) and the teardown-able registration. The OpenLineage-vs-derived_from caveat is dropped from this one sentence but is a stable cross-cutting boundary preserved at higher authority elsewhere, so the \u00a723 reader is not deprived of it across the corpus."
  },
  {
   "locator": "\u00a723 Tool governance, line 606-608 \u2014 \"the tool *vocabulary* is canonical (`tools.yaml`) and MCP is itself a domain\"",
   "excerpt": "Today the tool *vocabulary* is canonical (`tools.yaml`) and MCP is itself a domain (its platform/design KBs + auditor), but the **operational tool lifecycle is ungoverned**",
   "type": "code_citation",
   "design_claim": "A tool vocabulary may already be declared canonically while the operational tool lifecycle (assignment, permission, load, init, availability, add/update/remove) remains ungoverned \u2014 the gap the pattern closes.",
   "action": "strip_keep_claim",
   "restatement": "Even where the set of available tools is already declared canonically, the operational tool lifecycle \u2014 which agent gets which tool, at what permission, how it loads and initializes, whether it is available in the agent's run context, and how it is added, updated, or removed \u2014 can remain ungoverned; that is the gap the pattern closes.",
   "preserved": false,
   "loss": "Real conceptual loss in the restatement. The restatement keeps 'the set of available tools is already declared canonically' (the tools.yaml-as-vocabulary fact) but DROPS the clause 'MCP is itself a domain (its platform/design KBs + auditor).' That dropped clause is load-bearing rationale, not a citation: it is the conceptual bridge that justifies the very next sentence's claim that the fix is 'the same machinery, not a new system' \u2014 MCP already exemplifies the domain-pack pattern (it has platform + design KBs and an auditor), so governing the tool lifecycle reuses an existing shape rather than inventing one. Strip it and the 'reuse, don't build new' argument loses its concrete anchor. Mitigation: the fact that MCP is a registered cross-cutting domain survives in the plan's BOM backfill (implementation-plan.md:222, 'the MCP cross-cutting domain') and in KB-mcp-design, so the substance is not destroyed \u2014 but the 'why this is reuse' rationale at the point of the claim is weakened. Recommend the restatement retain '(MCP itself is already governed as a domain \u2014 its platform/design KBs plus an auditor \u2014 so this reuses that shape).'"
  },
  {
   "locator": "\u00a723 Tool governance, line 608 \u2014 \"a startup probe found a server *reachable but not initialized* (serena with no active project) in a fresh agent context\"",
   "excerpt": "This is where the recurring dev-environment failures live \u2014 a startup probe found a server *reachable but not initialized* (serena with no active project) in a fresh agent context.",
   "type": "named_run",
   "design_claim": "A tool can be reachable yet not usable because it requires an init step that has not run in the agent's context; this is a real, recurring failure mode that a reachability-only probe misses.",
   "action": "strip_keep_claim",
   "restatement": "A tool can be reachable yet unusable in a fresh agent context because it requires an initialization step that has not run; this reachable-but-not-initialized failure is a recurring class that a reachability-only probe will miss.",
   "preserved": true,
   "loss": "No loss. This is the strongest-preserved item. The abstract failure-mode claim (reachable yet unusable because an init step has not run; a reachability-only probe misses it) is restated, AND the full operational mechanism survives untouched at governed-pipeline-architecture.md:615 \u2014 a line this disposition does NOT strip \u2014 which spells out in-session SessionStart, execute the registered init step (activate_project), verify a project-scoped call succeeds (get_current_config / find_symbol), and explicitly that an out-of-session tools/list is a false PASS. The concrete serena/no-active-project incident and the mcp-ping.sh/mcp-auth-probe.sh provenance also land in the plan (implementation-plan.md:229). Both the why (rationale), the constraint (must prove initialized not merely reachable), and the incident provenance are fully retained."
  },
  {
   "locator": "\u00a723 Tool governance, line 613 \u2014 \"an MCP server that additionally requires an **init step** (e.g. `activate_project`)\"",
   "excerpt": "load + init mechanism \u2014 *how* the agent obtains it: `skills:`-preload, subprocess dispatch, deferred tool-search, or an MCP server that additionally requires an **init step** (e.g. `activate_project`). The init requirement is part of the contract, not folklore.",
   "type": "code_citation",
   "design_claim": "The registry must record, per tool, how it is loaded AND any required init step, because an init requirement is part of the tool contract rather than tribal knowledge.",
   "action": "strip_keep_claim",
   "restatement": "The registry records, per tool, how the agent obtains it (preloaded, dispatched, loaded on demand, or via an integration server) and any required initialization step; an init requirement is part of the tool contract, not folklore.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a723 Tool governance, line 615 \u2014 health-check paragraph: \"`tools/list` (the `mcp-ping.sh` seed)\", \"serena has *no active project*\", \"run **in-session at SessionStart**\", \"`activate_project`\", \"`get_current_config` / `find_symbol`\", \"existing `.devcontainer/lib/mcp-ping.sh` + `mcp-auth-probe.sh`\"",
   "excerpt": "Reachability alone is a false PASS for the exact bug this exists to catch: an out-of-session `tools/list` (the `mcp-ping.sh` seed) returns success even when serena has *no active project*. The probe must therefore run **in-session at SessionStart**, **execute the registered init step** (e.g. `activate_project`), and then **verify a project-scoped call succeeds** (`get_current_config` / `find_symbol`) \u2014 only that proves *initialized*, not merely *reachable*. The existing `.devcontainer/lib/mcp-ping.sh` + `mcp-auth-probe.sh` cover reachability; the in-session init-then-verify step is the addition.",
   "type": "code_citation",
   "design_claim": "A health probe must validate initialized-and-usable, not merely reachable: it must run inside the agent's actual session at startup, execute the registered init step, then verify a context-scoped call succeeds \u2014 because an out-of-session reachability ping is a false PASS for exactly the failure the probe exists to catch.",
   "action": "strip_keep_claim",
   "restatement": "A health probe must prove the tool is initialized and usable, not merely reachable: it runs inside the agent's actual session at startup, executes the registered init step, and then verifies that a context-scoped call succeeds. A reachability ping issued outside the agent's session is a false PASS for exactly the failure the probe exists to catch, so reachability checks are necessary but not sufficient and the in-session init-then-verify step is the essential addition.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a723 Tool governance, line 617 \u2014 \"a tool removed from the project but still installed at user scope orphans and resurfaces (the exact failure that took a multi-surface manual hunt)\"",
   "excerpt": "the orphan lesson applies to tools verbatim: a tool removed from the project but still installed at user scope orphans and resurfaces (the exact failure that took a multi-surface manual hunt).",
   "type": "named_run",
   "design_claim": "Tool teardown must reconcile across all install scopes, because a tool removed from one scope but left in another orphans and resurfaces \u2014 a failure that scope-blind manual removal produces.",
   "action": "relocate_to_plan",
   "target_note": "Relocate the specific incident provenance (a tool left at user scope after project removal, found only by a multi-surface manual hunt) to the plan as brownfield motivation. Principles doc keeps the abstract claim: tool teardown must reconcile across all install scopes or a residual install in another scope orphans and resurfaces.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a723 Tool governance, line 617 \u2014 \"tools emit tool-use spans (D-OBS-1): which agent used which tool, failures, latency\"",
   "excerpt": "**Usage is observed** \u2014 tools emit tool-use spans (D-OBS-1): which agent used which tool, failures, latency \u2014 so a never-used grant can be pruned and a failing tool surfaced.",
   "type": "code_citation",
   "design_claim": "Tools should emit usage telemetry (which agent used which tool, with failures and latency) so unused grants can be pruned (least-privilege upkeep) and failing tools surfaced. The cross-ref to the observability design decision (D-OBS-1) is intra-document and is kept.",
   "action": "strip_keep_claim",
   "restatement": "Tools emit usage telemetry \u2014 which agent used which tool, with failures and latency (D-OBS-1) \u2014 so that a never-used grant can be pruned to uphold least privilege and a failing tool can be surfaced.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a723 Keeping domains fresh, line 621 \u2014 \"those sources move (a spec revises, a tool deprecates; the removed servers are examples)\"",
   "excerpt": "A domain's KBs are **durable knowledge** (Part VII) derived from authoritative upstream sources \u2014 and those sources move (a spec revises, a tool deprecates; the removed servers are examples).",
   "type": "named_run",
   "design_claim": "A domain's KBs derive from authoritative upstream sources that change over time (specs revise, tools deprecate), so KBs are subject to staleness \u2014 illustrated by tools that were deprecated and removed.",
   "action": "strip_keep_claim",
   "restatement": "A domain's KBs are durable knowledge derived from authoritative upstream sources, and those sources move over time \u2014 a spec revises, a tool deprecates \u2014 so the KB is continuously subject to staleness.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a723 Keeping domains fresh, line 623 \u2014 \"A periodic check (the `research-and-verify` workflow) re-checks each source\"",
   "excerpt": "Detect drift. A periodic check (the `research-and-verify` workflow) re-checks each source; a material upstream change flags the KB **stale** via bi-temporal supersession (R18 \u2014 mark superseded, surface current), never silently.",
   "type": "code_citation",
   "design_claim": "Drift is detected by a periodic re-check of each declared authoritative source; a material upstream change flags the KB stale via bi-temporal supersession rather than silently. The supersession-lifecycle cross-ref (R18) is intra-document and kept.",
   "action": "strip_keep_claim",
   "restatement": "Drift is detected by a periodic re-verification of each declared authoritative source; a material upstream change flags the KB stale via bi-temporal supersession (mark superseded, surface current; R18), never silently.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a724, line 634",
   "excerpt": "the context/instruction files (`CLAUDE.md` / `AGENTS.md`)",
   "type": "code_citation",
   "design_claim": "Durable knowledge includes context/instruction files (the agent-facing project-context documents), which must be governed for freshness like other across-time state.",
   "action": "strip_keep_claim",
   "restatement": "the context/instruction files that carry project-level guidance to the agents",
   "preserved": true,
   "loss": "No design loss. The disposition strips only the parenthetical filenames `CLAUDE.md`/`AGENTS.md` and keeps 'context/instruction files that carry project-level guidance to the agents' \u2014 the design claim (durable knowledge includes the agent-facing context files, which must be governed for freshness) survives verbatim in the sentence. The stripped filenames are pure citation here; where they ARE load-bearing (the single-source symlink design) they appear again at line 672 and are untouched by this disposition. Safe."
  },
  {
   "locator": "Part VII \u00a724, line 639",
   "excerpt": "supersede, never overwrite \u2014 append-only with invalidation, surfacing a current view (the project already does this for ADRs; it must extend to memory)",
   "type": "repo_metric",
   "design_claim": "Supersede-never-overwrite (append-only with invalidation, surfacing a current view) is the governance discipline for durable knowledge, and it should apply uniformly to both decision records and memory.",
   "action": "strip_keep_claim",
   "restatement": "supersede, never overwrite \u2014 append-only with invalidation, surfacing a current view; apply this uniformly across decision records and memory",
   "preserved": true,
   "loss": "Effectively no design loss; one minor rationale softening. The full discipline (supersede-never-overwrite; append-only with invalidation; surface a current view) is kept verbatim, and the uniformity instruction is kept ('apply uniformly across decision records and memory'). The only change is rewording the parenthetical 'the project already does this for ADRs; it must extend to memory' into 'apply this uniformly across decision records and memory' \u2014 this drops the proven-precedent framing (ADRs already prove the pattern, so extension is low-risk). That 'why' is preserved elsewhere: line 650 states ADR supersession discipline already exists, and Appendix A/B retain the ADR-already-does-this fact. Acceptable."
  },
  {
   "locator": "Part VII \u00a724, line 642",
   "excerpt": "Current state (evidence: Appendix A): 68 live ADRs with ~1,470 internal cross-references and no index, with two live-but-`Superseded` records intermixed; sub-agent and auto-memory carry no freshness field and no conflict/supersession check; and the multi-level memory/context hierarchy is additive \u2014 \"no override, no de-duplication\" by design, so cross-level conflicts go unresolved.",
   "type": "repo_metric",
   "design_claim": "The motivating gap: a large, densely cross-linked decision store with no index and intermixed superseded records; memory with no freshness/conflict checking; and an additive (no-override, no-dedup) context hierarchy that leaves cross-level conflicts unresolved. These are the conditions the across-time governance mechanism is designed to fix.",
   "action": "relocate_to_plan",
   "target_note": "The concrete current-state numbers (live-ADR count, cross-reference count, the two intermixed live-but-superseded records, the per-surface 'no freshness/conflict check' findings, and the 'no override, no de-duplication' hierarchy fact) are repo-specific brownfield motivation. Move the whole inventory to the plan as the baseline the mechanism remediates. In the principles doc replace with: 'Governance of durable knowledge today is typically size-and-credential hygiene only; freshness, content-conflict, and stale-supersession go unchecked across decision records, memory, and context files.' (This abstract framing already exists at line 636 and can carry the claim.)",
   "preserved": true,
   "loss": "No design loss; the relocation target is real and already richer than the source. Every concrete number relocates to a genuine home: the plan carries ~68 ADRs (line 243), the two named live-but-superseded records ADR-0018/ADR-0058 (lines 245, 315 T7), the exact status-casing counts 45 Accepted / 14 accepted (line 317 T10), and 'additive precedence has no override' (lines 82, 247) \u2014 with path:line locators the source line lacked. The proposed abstract replacement is fully covered by existing line 636 ('size and credential hygiene only ... uniform across all three knowledge surfaces'). ONE nuance to note, not a blocking loss: the source's 'additive \u2014 by design' wording (the no-override behavior is an intentional platform property, which is WHY no tool can simply resolve the conflict) is softened to 'no override' in the plan, but that exact rationale is preserved at architecture line 672 ('all levels concatenate and the model arbitrates ... no agent tool runs a true override engine'). Also note a latent internal-consistency item (not a loss): Appendix A row at line 950 still repeats these same numbers; if the strip is meant to remove repo metrics from \u00a724 it leaves the appendix untouched, so the document keeps the numbers regardless."
  },
  {
   "locator": "Part VII \u00a724, line 642 (evidence pointer)",
   "excerpt": "(evidence: Appendix A)",
   "type": "evidence_map",
   "design_claim": "The current-state diagnosis is grounded in observed repository evidence.",
   "action": "strip_keep_claim",
   "restatement": "(remove the inline evidence-map pointer; the diagnosis stands as a stated condition of the durable-knowledge problem space)",
   "preserved": true,
   "loss": "Negligible loss. Removing the inline '(evidence: Appendix A)' breadcrumb does not destroy the grounding: Appendix A still exists in the same document (line 938) and the document's reading guide already directs readers to it, so the diagnosis remains evidence-backed. The only thing lost is a convenience pointer from \u00a724 to the evidence row \u2014 a reader of \u00a724 in isolation loses the direct cross-link. The design claim (the diagnosis is grounded in observed evidence) is weakly served by removing the pointer but is still satisfied by the appendix's continued presence. Worth flagging but not a substantive loss."
  },
  {
   "locator": "Part VII \u00a725, line 648",
   "excerpt": "produced from the folder (e.g. adr-log / Log4brains), never hand-maintained, with a CI drift check against the folder. For a large, un-indexed store this is the single highest-leverage move: it turns ~1,470 raw cross-references into a navigable entry point.",
   "type": "repo_metric",
   "design_claim": "A generated (never hand-maintained) decision-log index, with a CI drift check against the source folder, is the single highest-leverage move for a large, densely cross-linked, un-indexed decision store: it converts a raw cross-reference web into a navigable entry point.",
   "action": "strip_keep_claim",
   "restatement": "produced mechanically from the decision-record folder by a generator, never hand-maintained, with a CI drift check against the folder. For a large, un-indexed store this is the single highest-leverage move: it turns a raw cross-reference web into a navigable entry point. (Tool names adr-log / Log4brains are illustrative and may stay as generic examples, but the per-repo count is dropped.)",
   "preserved": true,
   "loss": "No design loss. The full rationale is kept verbatim: generated-not-hand-maintained index, CI drift check against the folder, and the load-bearing 'single highest-leverage move ... turns a raw cross-reference web into a navigable entry point' claim. Only the '~1,470' count is dropped, which is pure metric and is retained in Appendix A (line 950). Tool names adr-log / Log4brains are kept as generic examples per the restatement, so the illustrative concreteness survives. Safe."
  },
  {
   "locator": "Part VII \u00a725, line 650",
   "excerpt": "Supersession discipline already exists here (append-only; `supersedes` / `superseded_by` frontmatter, all links resolving). Keep it, and close the hygiene gaps: enforce two-way links, normalize the status casing, and relocate the two live-but-superseded records.",
   "type": "repo_metric",
   "design_claim": "Where append-only supersession discipline with two-way frontmatter links already exists, keep it and close residual hygiene gaps: enforce two-way links, normalize status casing, and relocate any live-but-superseded records out of the current view.",
   "action": "strip_keep_claim",
   "restatement": "Where append-only supersession discipline with supersedes/superseded_by frontmatter links already exists, keep it and close the hygiene gaps: enforce two-way links, normalize status casing, and relocate any live-but-superseded records out of the current view. (Drops 'the two' specific count; the discipline is stated generically.)",
   "preserved": true,
   "loss": "No design loss. The complete discipline survives: keep the existing append-only supersession with supersedes/superseded_by frontmatter links, and close the three named hygiene gaps (enforce two-way links, normalize status casing, relocate live-but-superseded records). Only 'the two' specific count is generalized to 'any', which makes the instruction more durable, not weaker. The concrete identities and count are preserved in the plan (lines 245, 315 T7, 317 T10) with path:line locators. The 'why' (clean supersession requires two-way links + a filtered current view) is intact in the surrounding \u00a725 prose. Safe."
  },
  {
   "locator": "Part VII \u00a725, line 652",
   "excerpt": "derived from the committed ADR frontmatter + canonical",
   "type": "code_citation",
   "design_claim": "The decision graph is derived from already-committed decision-record frontmatter plus the canonical ground-truth \u2014 authored data, not LLM-extracted entities.",
   "action": "strip_keep_claim",
   "restatement": "derived from the committed decision-record frontmatter plus canonical ground-truth",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a725, line 652",
   "excerpt": "No graph database: the graph is built from frontmatter into a file/SQLite form (recursive queries suffice well below 10\u2075 edges; we have ~68 ADRs), keeping it inside TB1/TB2.",
   "type": "repo_metric",
   "design_claim": "At the scale of a typical decision store (orders of magnitude below ~10^5 edges) a file/SQLite-derived graph with recursive queries suffices; no graph database is warranted, keeping the mechanism inside the project's single-container / git-of-record technology bounds.",
   "action": "strip_keep_claim",
   "restatement": "No graph database: the graph is built from the frontmatter into a file/embedded-SQL form (recursive queries suffice well below ~10^5 edges, far above a typical decision store's size), keeping it inside the single-container / git-of-record technology bounds (TB1/TB2). (Keeps the TBn cross-refs and the 10^5 threshold; drops the per-repo '~68 ADRs' count.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a725, line 652",
   "excerpt": "Agents *query* it through a typed MCP surface \u2014 predefined parameterized queries (\"which superseded decisions are still in production?\", \"what blocks this migration?\"), not raw Cypher or natural-language-to-query",
   "type": "worked_example",
   "design_claim": "Agents query the decision graph through a typed, parameterized query surface (predefined queries, not raw graph-query-language or NL-to-query) with context-budget discipline.",
   "action": "strip_keep_claim",
   "restatement": "Agents query it through a typed query surface exposing predefined parameterized queries (for example: which superseded decisions are still in force? what blocks a given migration?), not raw query-language or natural-language-to-query, with context-budget discipline (timeouts, truncation). (The MCP framing and example questions are generic enough to keep; no repo specifics are lost.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a725, line 652 (D-KN-2 reference)",
   "excerpt": "The decision graph (the AI-interfacing surface \u2014 D-KN-2).",
   "type": "evidenced_in_column",
   "design_claim": "The decision-graph design point is a named, tracked decision in this document's own decision register.",
   "action": "strip_keep_claim",
   "restatement": "Keep the intra-document decision cross-reference (D-KN-2) as-is \u2014 it is internal document structure, not a codebase reference. No change needed beyond ensuring the Appendix A row it points to is itself stripped of repo counts.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a725, Figure V16, lines 654-666",
   "excerpt": "ADR-0053 / ADR-0051 / ADR-0044 / ADR-0038 / ADR-0018 (superseded); A38 -.->|\"supersedes\"| A18; \"(representative subgraph, not all ADRs)\"",
   "type": "worked_example",
   "design_claim": "A generated, status-filtered index is the human entry point into an otherwise un-navigable decision store; typed edges (supersedes, depends_on, related) connect decisions and let superseded records be filtered out of the current view.",
   "action": "strip_keep_claim",
   "restatement": "Relabel the diagram nodes to generic placeholders (e.g. 'Decision A', 'Decision B', 'Decision C (superseded)') instead of real decision-record numbers, and keep the caption as 'A generated, status-filtered index is the entry point into an otherwise un-navigable store (illustrative subgraph).' The structural claim and edge types survive; the specific record IDs go.",
   "preserved": true,
   "loss": "No loss to the design claim. Relabeling nodes to generic placeholders ('Decision A/B/C (superseded)') and genericizing the caption preserves everything the figure teaches: a generated, status-filtered index as the human entry point; typed edges (supersedes / depends_on / related); and superseded records filtered out of the current view. The real ADR numbers (0053/0051/0044/0038/0018) are pure illustration with no semantic weight \u2014 the subgraph is explicitly 'representative, not all ADRs'. One small caution: the original edge set in the diagram uses 'supersedes', 'depends_on', 'related', while the prose at line 652 lists the typed edges as 'supersedes / depends_on / constrains / conflicts_with'. The relabel should not silently 'fix' the diagram's edge labels to match prose or it would change content; keeping the same three edge types under placeholder nodes is correct."
  },
  {
   "locator": "Part VII \u00a726, line 672",
   "excerpt": "The single-source design is already correct \u2014 `CLAUDE.md` is a symlink to one `AGENTS.md`, under the ~200-line budget, so every tool reads identical bytes.",
   "type": "code_citation",
   "design_claim": "A single-source context file (one physical file, symlinked under whatever names different tools expect) under a bounded line budget ensures every tool reads identical bytes and the guidance cannot drift between tools.",
   "action": "strip_keep_claim",
   "restatement": "The single-source design is the correct baseline \u2014 one physical context file, surfaced under whatever names different tools expect (e.g. via a symlink), kept under a bounded line budget, so every tool reads identical bytes and the guidance cannot drift between tools. (Drops the specific filenames and the ~200-line figure; the line-budget discipline survives abstractly.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a726, line 672",
   "excerpt": "no agent tool runs a true override engine; all levels (enterprise / user / project / local) concatenate and the model arbitrates, so a rule lives at exactly one level, never duplicated",
   "type": "code_citation",
   "design_claim": "Because the context/memory hierarchy concatenates levels and the model arbitrates (no true override engine), cross-level conflicts must be prevented at authoring time by keeping each rule at exactly one level, never duplicated across levels.",
   "action": "strip_keep_claim",
   "restatement": "resolve cross-level conflicts by not authoring them \u2014 where the layered context levels concatenate and the model arbitrates (rather than a true override engine resolving precedence), a rule must live at exactly one level and never be duplicated across levels. (The enterprise/user/project/local enumeration is the host platform's level model; restate generically as 'the layered context levels'.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a726, line 672",
   "excerpt": "move conditional rules into path-scoped `.claude/rules/` (loaded only when work touches that subtree) rather than enlarging the always-on file, because `@import` is organizational only (it does not save tokens) and accuracy degrades as the always-on context grows (\"context rot\").",
   "type": "code_citation",
   "design_claim": "Grow context by path-scoped, conditionally-loaded rule files (loaded only when work touches the relevant subtree) rather than by enlarging the always-on file, because import/include is organizational only (it saves no tokens) and model accuracy degrades as always-on context grows ('context rot').",
   "action": "strip_keep_claim",
   "restatement": "grow by path-scoped loading, not bloat \u2014 as the file nears its budget, move conditional rules into path-scoped rule files that load only when work touches that subtree, rather than enlarging the always-on file, because an import directive is organizational only (it saves no tokens) and accuracy degrades as the always-on context grows ('context rot'). (Drops the literal `.claude/rules/` path and `@import` syntax; the discipline survives.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Part VII \u00a726, Figure V17 caption, line 674",
   "excerpt": "The freshness mechanism (V8) on the across-runs timescale: supersede-not-overwrite, anchored to canonical.",
   "type": "evidence_map",
   "design_claim": "The memory lifecycle is the same freshness mechanism (detect \u2192 resolve \u2192 prune; supersede-not-overwrite; anchored to canonical) applied on the across-runs timescale.",
   "action": "strip_keep_claim",
   "restatement": "Keep as-is \u2014 V8 and the figure id are internal cross-references (intra-document structure), and 'canonical' here is the abstract ground-truth source, not a repo file. No repo-specific content to strip.",
   "preserved": true,
   "loss": "No loss, and arguably no strip needed. Verified: V8 (Appendix E line 1048) and the V17 figure id are intra-document cross-references that resolve, and 'canonical' here denotes the abstract ground-truth source, not a repo file path. The disposition's own restatement is 'keep as-is', so despite the action label 'strip_keep_claim' nothing is actually removed. The design claim (memory lifecycle = the freshness mechanism on the across-runs timescale; supersede-not-overwrite; anchored to canonical) is untouched. Correctly classified as a no-op."
  },
  {
   "locator": "Part VII \u00a726, line 670 and Figure V17",
   "excerpt": "Writes are anchored to canonical \u2014 a memory that contradicts current canonical or a superseding ADR is flagged.",
   "type": "code_citation",
   "design_claim": "Memory writes are anchored to the canonical ground-truth: a memory contradicting current canonical or a superseding decision record is flagged by the sentinel.",
   "action": "strip_keep_claim",
   "restatement": "Writes are anchored to canonical \u2014 a memory that contradicts current canonical ground-truth or a superseding decision record is flagged. ('ADR' restated as 'decision record'; 'canonical' is the abstract ground-truth and stays.)",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a727 R29 (line 725)",
   "excerpt": "Feature, execution, cc-critique, issue-capture, and report-only flows are each a `pipelines.yaml` entry with a declared tier, running on the one orchestration substrate (Dynamic Workflows, D-ORCH-1); the sentinel flags any orphan or prose-orchestrated pipeline.",
   "type": "code_citation",
   "design_claim": "Every pipeline variant is a tiered, declared entry in one manifest on a single orchestration substrate; nothing runs prose-orchestrated or off-manifest, and the sentinel flags any orphan.",
   "action": "strip_keep_claim",
   "restatement": "Every pipeline variant \u2014 full design pipelines, lighter automation flows, and report-only flows alike \u2014 is a declared, tiered entry in the one pipeline manifest, running on a single orchestration substrate; the sentinel flags any pipeline defined in prose or absent from the manifest (D-ORCH-1).",
   "target_note": "Strip the concrete pipeline names (feature/execution/cc-critique/issue-capture), the literal `pipelines.yaml` filename, and the platform-specific 'Dynamic Workflows' product name. Keep internal ref D-ORCH-1.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a727 R33 (line 729)",
   "excerpt": "A breaking change to an externally-consumed surface (OTLP event schema, committed run-summary, cross-pipeline canonical) ships with a compatibility window or migration step \u2014 not a flag-day for consumers outside the commit.",
   "type": "code_citation",
   "design_claim": "Breaking changes to any externally-consumed surface ship with a compatibility window or migration step rather than a flag-day for out-of-commit consumers.",
   "action": "strip_keep_claim",
   "restatement": "A breaking change to an externally-consumed surface (the telemetry event schema, a committed run-summary, or a canonical surface shared across pipelines) ships with a compatibility window or migration step \u2014 not a flag-day for consumers outside the commit.",
   "target_note": "Replace the technology-specific 'OTLP' with the generic 'telemetry event schema'. The three examples are illustrative surface classes, kept in generic form.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "\u00a728 A27 (line 770)",
   "excerpt": "A pipeline defined in prose or absent from `pipelines.yaml` \u2014 it drifts, escapes the sentinel, and runs on no governed substrate (the un-migrated pipeline).",
   "type": "code_citation",
   "design_claim": "A pipeline that is prose-defined or missing from the manifest is an anti-pattern: it drifts, escapes the sentinel, and runs ungoverned.",
   "action": "strip_keep_claim",
   "restatement": "A pipeline defined in prose, or absent from the pipeline manifest \u2014 it drifts, escapes the sentinel, and runs on no governed substrate (the un-migrated pipeline).",
   "target_note": "Replace the literal `pipelines.yaml` filename with 'the pipeline manifest'.",
   "preserved": true,
   "loss": "None. The design claim is the anti-pattern itself \u2014 a pipeline that is prose-defined or missing from the manifest drifts, escapes the sentinel, and runs ungoverned. The restatement keeps all three consequences (drifts / escapes sentinel / ungoverned) and the 'un-migrated pipeline' label verbatim, swapping only the literal filename `pipelines.yaml` for 'the pipeline manifest'. The filename is a pure citation: the manifest concept is named everywhere else in the doc, and the plan (implementation-plan.md:167) owns the concrete `.claude/canonical/pipelines.yaml` path. No rationale, constraint, or 'why' is carried by the filename token. Safe."
  },
  {
   "locator": "\u00a729 Maintainability table, 'Misrouted triage' row (line 783)",
   "excerpt": "The mechanical-vs-semantic classifier is the linchpin; it is a **distinct axis from severity** (routing \u2260 severity \u2014 `severity.yaml` does not own it) and needs its own canonical home \u2014 a routing-classifier entry the plan schedules (WS-0), read by R7/A10.",
   "type": "code_citation",
   "design_claim": "Routing classification (mechanical vs. semantic) is a distinct axis from severity and needs its own canonical home, separate from the severity definition.",
   "action": "strip_keep_claim",
   "restatement": "The mechanical-vs-semantic classifier is the linchpin; it is a distinct axis from severity (routing \u2260 severity), so it needs its own canonical home rather than being folded into the severity definition, read by R7/A10.",
   "target_note": "Strip the literal `severity.yaml` filename and the plan-scheduling pointer 'the plan schedules (WS-0)'. The fact that the plan schedules this routing-classifier entry as a WS-0 work item is repo-specific provenance the plan already carries; do not re-import it into the principles doc. Keep internal refs R7/A10.",
   "preserved": true,
   "loss": "None of substance. The load-bearing claim \u2014 routing is a distinct axis from severity and needs its own canonical home rather than being folded into the severity definition \u2014 survives intact, including the internal refs R7/A10. Two things are dropped: (1) the literal `severity.yaml` filename, a pure citation already owned by the canonical README (line 55) and the plan (implementation-plan.md:129); (2) the 'the plan schedules (WS-0)' pointer. Dropping (2) is defensible BUT note the restatement still says 'so it needs its own canonical home' without saying that home is a scheduled work item \u2014 a reader of the principles doc loses the signal that this is an as-yet-unbuilt deliverable. That provenance is genuinely carried by the plan (implementation-plan.md:129 names the routing-classifier as a new WS-0 build item and explicitly calls out the \u00a729 mis-attribution it closes), so the information is not destroyed at the system level \u2014 only the cross-pointer from the principles doc is removed. Acceptable per the rule (provenance belongs in the plan), with the minor caveat that the principles doc no longer hints the gap is open."
  },
  {
   "locator": "\u00a729 'Two prerequisites' paragraph (line 789)",
   "excerpt": "*Reuse the mature thing* \u2014 the execution side already has schema'd result artifacts, stub detection, and dimensional verdicts; extract its proven gating into the shared harness rather than growing a second one.",
   "type": "repo_metric",
   "design_claim": "When a mature gating implementation already exists in the system, extract and reuse its proven mechanism into a shared harness rather than building a second parallel one.",
   "action": "strip_keep_claim",
   "restatement": "Reuse the mature thing: where a part of the system already has schema'd result artifacts, stub detection, and dimensional verdicts, extract its proven gating into the shared harness rather than growing a second one.",
   "target_note": "Strip the repo-specific assertion that 'the execution side already has' these capabilities (a present-state fact about this codebase). The motivation that this repo's execution pipeline is the concrete reuse source is provenance the PLAN should carry; relocate the repo-specific 'extract the existing execution-side gating' instruction there.",
   "preserved": true,
   "loss": "None for the abstract rule. The restatement preserves the full design principle ('reuse the mature thing': where a part of the system already has schema'd result artifacts, stub detection, and dimensional verdicts, extract its proven gating into the shared harness rather than growing a second one) \u2014 including the three concrete capabilities that make 'mature' falsifiable and the 'don't grow a second one' rationale. Only the repo-specific present-tense assertion 'the execution side already has' is generalized to 'where a part of the system already has'. The 'why' (avoid two parallel harnesses) is fully retained. Safe \u2014 BUT this disposition's safety depends on companion #4 actually landing the execution-side identification in the plan; see that item."
  },
  {
   "locator": "\u00a729 'Two prerequisites' paragraph (line 789) \u2014 relocation companion",
   "excerpt": "the execution side already has schema'd result artifacts, stub detection, and dimensional verdicts; extract its proven gating into the shared harness",
   "type": "repo_metric",
   "design_claim": "The concrete reuse source for the shared gating harness is this repo's existing execution-pipeline gating.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Move the repo-specific identification of the execution pipeline as the existing mature gating implementation into the plan as a build instruction (extract the execution-side result-artifact/stub-detection/dimensional-verdict gating into the shared harness). The principles doc keeps only the abstract 'reuse the mature thing' rule.",
   "preserved": false,
   "loss": "INFORMATION LOSS RISK \u2014 the disposition's premise is partly false. It asserts the repo-specific 'execution pipeline is the existing mature gating implementation' should move to the plan as a build instruction, implying the plan is the safe home. But the plan does NOT currently carry this. implementation-plan.md mentions 'shared gate logic stays in auditing-shared' (lines 288, 355) and stubs the execution pipeline in the manifest (line 167), yet nowhere identifies the execution pipeline as the mature source whose schema'd result artifacts / stub detection / dimensional verdicts get EXTRACTED into the shared harness. The WS-0 corpus harness is even built against execution-pipeline-design-r1 (line 404), but that is a test-corpus use, not 'extract its gating.' So if this disposition is applied as a strip-from-architecture move WITHOUT actually writing the relocated build instruction into the plan, the concrete reuse source \u2014 the single most actionable 'how to build the shared harness' fact \u2014 vanishes entirely. The disposition is only non-lossy if the relocation is performed and verified in the plan (an explicit WS-0/WS-1 instruction: extract the execution-side result-artifact + stub-detection + dimensional-verdict gating into auditing-shared). Treat as a required, not optional, write. Flag to the editor."
  },
  {
   "locator": "\u00a729 'Tier to blast radius (R10)' paragraph (line 791)",
   "excerpt": "Full gate stack for the feature and execution pipelines (expensive downstream, low reversibility); a light self-check for report-only flows; a minimal self-check for single-doc human-approved flows. Once the coordinator drives the manifest via Dynamic Workflows (D-ORCH-1), each new pipeline is a manifest entry that picks a tier.",
   "type": "code_citation",
   "design_claim": "Gate stack depth is tiered to blast radius: heavy downstream/low-reversibility pipelines get the full stack, report-only flows a light self-check, single-doc human-approved flows a minimal self-check; once the coordinator drives the manifest, each new pipeline picks a tier as a manifest entry.",
   "action": "strip_keep_claim",
   "restatement": "Full gate stack for pipelines with expensive downstream and low reversibility; a light self-check for report-only flows; a minimal self-check for single-doc human-approved flows. Once the coordinator drives the manifest, each new pipeline is a manifest entry that picks a tier (R10, D-ORCH-1).",
   "target_note": "Strip the concrete pipeline names ('feature and execution pipelines') and the platform-specific 'Dynamic Workflows' product binding; describe the high-blast-radius tier by its property instead. Keep internal refs R10/D-ORCH-1.",
   "preserved": true,
   "loss": "Minor, acceptable. The tiering principle survives fully: full stack for expensive-downstream/low-reversibility pipelines, light self-check for report-only flows, minimal self-check for single-doc human-approved flows, and 'each new pipeline picks a tier as a manifest entry' (R10, D-ORCH-1 kept). Two strips: (1) concrete pipeline names 'feature and execution pipelines' replaced by their defining property ('expensive downstream and low reversibility') \u2014 this is actually an IMPROVEMENT for a principles doc, no loss since the property is the real selection criterion. (2) 'via Dynamic Workflows' product binding dropped, generalized to 'once the coordinator drives the manifest.' The D-ORCH-1 ref is retained, which points to where the Dynamic-Workflows binding is specified (and the plan carries it at lines 195, 295), so the mechanism's 'how' is recoverable. No rationale lost."
  },
  {
   "locator": "\u00a729 Human oversight, final paragraph (line 800)",
   "excerpt": "(Run-time escalation appears in V5; promotion appears in V18.)",
   "type": "evidence_map",
   "design_claim": "Run-time semantic escalation and over-time promotion are the two human-gate decision classes, each visualized in the document's figures.",
   "action": "strip_keep_claim",
   "restatement": "(keep as-is \u2014 V5 and V18 are intra-document figure ids)",
   "target_note": "No change needed: V5 and V18 are figure ids (intra-document structure), explicitly on the KEEP list. Listed here only for completeness so the reviewer confirms they are not mistaken for repo citations.",
   "preserved": true,
   "loss": "None \u2014 correctly a no-op. Verified V5 and V18 are intra-document figure ids: V5 'Gate decision flow' defined at line 277 and indexed at 1045; V18 'The improvement loop' defined at line 514 and indexed at 1058. They are not repo citations and must not be stripped. The disposition correctly takes no action and lists this only for reviewer confirmation. Nothing is lost; the parenthetical pointing run-time escalation to V5 and promotion to V18 stays. Correct."
  },
  {
   "locator": "\u00a730 heading + intro (lines 802-804)",
   "excerpt": "## 30. Dev-environment (Codespaces) lifecycle constraints \u2014 The pipeline runs inside an ephemeral Codespace (single container: Dockerfile + Features; no docker-compose). The lifecycle treats artifacts very differently:",
   "type": "code_citation",
   "design_claim": "The deployment environment is an ephemeral, single-container dev environment with no multi-container orchestration; the architecture must respect that lifecycle, and different artifact classes have different durability.",
   "action": "strip_keep_claim",
   "restatement": "## Dev-environment lifecycle constraints \u2014 The pipeline runs inside an ephemeral, single-container dev environment with no multi-container orchestration. The lifecycle treats artifact classes very differently in durability.",
   "target_note": "Strip the platform-specific 'Codespaces', 'Dockerfile + Features', and 'docker-compose' product names; restate as the abstract property (ephemeral single-container env, no multi-container orchestration).",
   "preserved": true,
   "loss": "No loss. The abstract design claim \u2014 ephemeral, single-container environment with no multi-container orchestration, and artifact classes differing in durability \u2014 is fully carried by the restatement. Only the product names (Codespaces, Dockerfile, Features, docker-compose) are stripped, and those are pure platform citations, not rationale. A reader still learns the env is ephemeral, single-container, and that durability varies by artifact class, which is everything the design needs."
  },
  {
   "locator": "\u00a730 durability table (lines 806-811)",
   "excerpt": "| Run-event JSONL (gitignored runtime dir) | ephemeral \u2014 lost on rebuild | ... | Backend store (on a mounted persistent volume) | survives rebuild **only if** on a mounted persistent volume | ... | Run-pinning state (`/workspaces`) | survives stop/start, lost on rebuild |",
   "type": "code_citation",
   "design_claim": "Artifacts have distinct durability classes in an ephemeral env: git-committed substrate survives rebuild; runtime logs are ephemeral; a backend store survives only on a mounted persistent volume; run-pinning state survives stop/start but not rebuild \u2014 so durable inputs must live in version control.",
   "action": "strip_keep_claim",
   "restatement": "Keep the durability table but describe rows abstractly: git-committed substrate (contracts, manifest, registry, validators, corpus fixtures) survives rebuild; the ephemeral runtime log dir is lost on rebuild; a backend store survives only on a mounted persistent volume; run-pinning workspace state survives stop/start but not rebuild.",
   "target_note": "Strip the literal 'gitignored', the literal `/workspaces` path, and any tool-specific phrasing; keep the abstract durability classification, which is the load-bearing design claim.",
   "preserved": true,
   "loss": "No loss of design information. All four durability classes survive, and critically the load-bearing CONDITIONAL on the backend-store row ('survives only IF on a mounted persistent volume') is preserved in the restatement. The literal `/workspaces` path and the word 'gitignored' vanish, but those are evidence-citations, not constraints \u2014 the abstract durability semantics (git-committed survives rebuild; runtime log dir lost on rebuild; backend store conditional on persistent volume; run-pinning state survives stop/start but not rebuild) are what a reader needs, and all four survive."
  },
  {
   "locator": "\u00a730 Discipline paragraph (line 813)",
   "excerpt": "install persistent tools (validator deps, the observability backend + its log-shipper) in the **Dockerfile** (prebuild-captured, survives rebuild); start the observability backend **opt-in**, never on every session start, and never via docker-compose for a service not always run; the default machine is 4-core/16 GB (the `8 GB` in `hostRequirements` is a loose minimum, not the provisioned size \u2014 the smallest tier meeting the 4-core floor is 16 GB, per the GitHub Codespaces machine-types docs; resizable at proportional cost ...); forward the backend's port at **private** visibility; route any remote-backend secret through Codespaces Secrets with env-block indirection.",
   "type": "repo_metric",
   "design_claim": "Persistent tooling is baked into the prebuilt image so it survives rebuild; optional services start opt-in (never on every session, never via always-on multi-container orchestration); the machine footprint is a tunable cost dial with a single-container hard rule; service ports are forwarded privately; secrets use env-block / vault indirection, never inline.",
   "action": "strip_keep_claim",
   "restatement": "Install persistent tooling (validator dependencies, the observability backend and its log-shipper) into the prebuilt image so it survives rebuild; start optional services opt-in, never on every session and never as an always-on multi-container service; the machine footprint is a tunable cost dial, but single-container is the hard rule; forward service ports at private visibility; route any remote secret through env-block / vault indirection, never inline.",
   "target_note": "Strip platform/product specifics: 'Dockerfile', 'docker-compose', the concrete machine sizes (4-core/16 GB, 8 GB, the smallest-tier reasoning), the `hostRequirements` key, 'GitHub Codespaces machine-types docs', 'Codespaces Secrets'. Keep the abstract disciplines.",
   "preserved": false,
   "loss": "PARTIAL LOSS of a rationale. The source contains the causal clause 'keep the backend opt-in TO STAY ON THE CHEAP DEFAULT' \u2014 this is the WHY that ties the machine-sizing fact to the opt-in discipline (cost is the reason optional services must not start on every session). The item-3 restatement keeps both the 'opt-in' discipline and the 'footprint is a tunable cost dial' fact as separate statements, but DROPS the explicit causal link between them. Because item 4 relocates only the concrete sizing numbers to the plan (not this cost-causality clause), the 'opt-in to save cost' rationale risks falling through the gap between item 3 (which strips it) and item 4 (which doesn't claim it). Recommendation: add to the item-3 restatement an explicit connective such as 'start optional services opt-in \u2014 running them only when needed is what keeps the environment on its cheap default footprint.' All other disciplines in this paragraph (prebuilt-image persistence, single-container hard rule, private port visibility, env-block secret indirection) are preserved correctly."
  },
  {
   "locator": "\u00a730 Discipline paragraph, machine-sizing detail (line 813)",
   "excerpt": "the default machine is 4-core/16 GB (the `8 GB` in `hostRequirements` is a loose minimum, not the provisioned size \u2014 the smallest tier meeting the 4-core floor is 16 GB, per the GitHub Codespaces machine-types docs; resizable at proportional cost)",
   "type": "repo_metric",
   "design_claim": "The provisioned machine footprint and the rationale for the chosen tier are concrete environment-sizing facts.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "The exact provisioned machine sizing (4-core/16 GB default, the 8 GB hostRequirements-vs-provisioned distinction, the machine-types-doc citation) is repo/environment-specific provisioning detail the plan needs when it configures the dev environment. Relocate it to the plan's dev-environment workstream; the principles doc keeps only 'footprint is a tunable cost dial, single-container is the hard rule.'",
   "preserved": true,
   "loss": "No loss, CONDITIONAL on the relocation actually being executed faithfully. The disposition correctly identifies that the concrete sizing (4-core/16 GB default) AND its non-obvious rationale (the 8 GB hostRequirements is a loose minimum not the provisioned size; smallest tier meeting the 4-core floor is 16 GB; resizable at proportional cost) are genuine provisioning facts the plan needs, and it explicitly directs that the reasoning travel WITH the numbers to the plan's dev-environment workstream. As long as the plan relocation includes that hostRequirements-vs-provisioned distinction (the actual 'why' a reader needs), nothing is lost. The principles doc correctly retains only the abstract 'footprint is a tunable cost dial, single-container is the hard rule.' Caveat: this is the one item whose preservation depends on a downstream write (the plan edit) that this disposition merely instructs but does not itself perform \u2014 verify the plan actually receives the rationale, not just the number."
  },
  {
   "locator": "\u00a730 Discipline paragraph, closing sentence (line 813)",
   "excerpt": "The committed corpus and contracts are durable by virtue of being in git \u2014 which is exactly why the corpus is a reliable regression guard across rebuilds; validation inputs never live in the ephemeral runtime dir.",
   "type": "code_citation",
   "design_claim": "Validation inputs (corpus, contracts) must live in durable version control, never in the ephemeral runtime dir, so they remain a reliable regression guard across rebuilds.",
   "action": "strip_keep_claim",
   "restatement": "The committed corpus and contracts are durable by virtue of being in version control \u2014 which is exactly why the corpus is a reliable regression guard across rebuilds; validation inputs never live in the ephemeral runtime dir.",
   "target_note": "Replace 'in git' with 'in version control' for tech-agnostic phrasing; the design claim (durable validation inputs, never in ephemeral runtime) is fully preserved.",
   "preserved": true,
   "loss": "No loss. The single change is 'in git' -> 'in version control', a pure tech-agnostic rephrase. The complete design claim survives: validation inputs (corpus, contracts) must live in durable version control and never in the ephemeral runtime dir, and the corpus's durability is precisely why it is a reliable regression guard across rebuilds. Both the rule and its 'why' (regression-guard reliability) are intact."
  },
  {
   "locator": "Part IX, \u00a731, Figure V12 mermaid node SUB (line 831)",
   "excerpt": "SUB[\"Enforced by code<br/>canonical + validators + sentinel + observability\"]",
   "type": "code_citation",
   "design_claim": "The architecture discipline is not merely advisory prose; it is enforced by code \u2014 specifically by the contract-manifest store, the validators that check conformance, the drift sentinel that catches orphaned/stale references, and the observability layer.",
   "action": "strip_keep_claim",
   "restatement": "Enforced by code: the contract store, the conformance validators, the drift sentinel, and the observability layer.",
   "preserved": true,
   "loss": "No loss; the restatement is strictly clearer. The terse diagram labels ('canonical + validators + sentinel + observability') are EXPANDED into plain-English named mechanisms (contract store, conformance validators, drift sentinel, observability layer). The load-bearing design claim \u2014 that the discipline is enforced by code rather than advisory prose, via four specific mechanisms \u2014 is fully preserved, and the expansion even names what 'canonical' and 'sentinel' mean, improving a first-time reader's comprehension. No rationale, constraint, or 'why' is dropped."
  },
  {
   "locator": "Part IX, \u00a731, Figure V12 mermaid node INST (line 830)",
   "excerpt": "INST[\"Running instance<br/>the orchestrator that conforms\"]",
   "type": "code_citation",
   "design_claim": "The architecture discipline is realized by a concrete running instance \u2014 an orchestrator that conforms to the discipline \u2014 distinct from the discipline itself.",
   "action": "strip_keep_claim",
   "restatement": "Running instance: the orchestrator that conforms to the discipline.",
   "preserved": true,
   "loss": "No loss. The INST node is already generic ('Running instance / the orchestrator that conforms') \u2014 it carries no repo-specific citation, no file path, no project name. The restatement reproduces it verbatim in prose. The design claim (a concrete running instance, distinct from the discipline, realizes it) is fully carried, and the surrounding edges ('is realized by', 'enforced by') and the SUB node that give the node its meaning are outside this disposition's strip scope, so the relationship the node participates in is not severed. Nothing a reader needs is dropped."
  },
  {
   "locator": "Part IX, \u00a732, table row 'Enforcement' (line 847)",
   "excerpt": "**Enforcement** \u2014 manifest schema, conformance/freshness/orphan checks | **canonical + validation library + drift sentinel** | A skill can't enforce; the teeth must be code.",
   "type": "code_citation",
   "design_claim": "Enforcement (manifest schema plus conformance/freshness/orphan checks) cannot live in a passive skill; it must be implemented as code \u2014 a canonical contract store, a validation library, and a drift sentinel.",
   "action": "strip_keep_claim",
   "restatement": "Enforcement \u2014 manifest schema, conformance/freshness/orphan checks \u2014 homes in a canonical contract store plus a validation library and a drift sentinel, because a skill cannot enforce; the teeth must be code.",
   "preserved": true,
   "loss": "No loss. The restatement carries the full claim AND the rationale verbatim ('a skill cannot enforce; the teeth must be code'). The three concrete homes (canonical contract store, validation library, drift sentinel) are named abstractly, not as repo file paths, so no citation is being stripped here \u2014 the row was already abstract. The 'why' survives intact."
  },
  {
   "locator": "Part IX, \u00a732, table row 'Runtime' (line 848)",
   "excerpt": "**Runtime** \u2014 driving the manifest (hybrid; dynamic routing in code) | **per-segment Dynamic Workflow scripts + a sequencing coordinator** | One workflow per human-gated segment, sequenced via state in the run-event log (a gate can't live inside a script); migrated from prose by Strangler-Fig + parallel-run (D-ORCH-1).",
   "type": "code_citation",
   "design_claim": "Runtime that drives the manifest (with dynamic routing in code) homes in one workflow script per human-gated segment, sequenced by a coordinator using state from the run-event log \u2014 because a human gate cannot live inside a single script. The migration from prose to code follows the Strangler-Fig + parallel-run pattern (D-ORCH-1). The D-ORCH-1 decision reference is an intra-document anchor and is KEPT.",
   "action": "strip_keep_claim",
   "restatement": "Runtime \u2014 driving the manifest, with dynamic routing in code \u2014 homes in one workflow script per human-gated segment, sequenced by a coordinator that reads run-event-log state, because a gate cannot live inside a single script; migrated from prose by Strangler-Fig + parallel-run (D-ORCH-1).",
   "preserved": true,
   "loss": "No loss of rationale. The restatement keeps the load-bearing constraint ('a gate cannot live inside a single script'), the sequencing mechanism (coordinator reads run-event-log state), the migration pattern (Strangler-Fig + parallel-run), and the D-ORCH-1 anchor, which is correctly identified as an intra-document reference to KEEP rather than a repo citation to strip. The 'one workflow per human-gated segment' design rule and its 'why' both survive. Caveat: D-ORCH-1's KEEP status is only valid if D-ORCH-1's defining decision section also survives the broader refactor; the disposition asserts it is intra-document, which the source supports."
  },
  {
   "locator": "Part IX, \u00a732, table row 'Observability' (line 849)",
   "excerpt": "**Observability** \u2014 event schema + emitter + projection | **canonical schema + validation library + run-summary** | Telemetry is code + data, not discipline.",
   "type": "code_citation",
   "design_claim": "Observability (event schema plus emitter plus projection) homes in code+data \u2014 a canonical event schema, a validation library, and a run-summary projection \u2014 not in discipline prose, because telemetry is code and data, not prose.",
   "action": "strip_keep_claim",
   "restatement": "Observability \u2014 event schema, emitter, projection \u2014 homes in a canonical event schema, a validation library, and a run-summary projection, because telemetry is code and data, not discipline.",
   "preserved": true,
   "loss": "No loss. The restatement carries the full claim (event schema + emitter + projection homing in canonical schema + validation library + run-summary) plus the rationale ('telemetry is code and data, not discipline'). The row named abstract homes, not repo paths, so there is no actual citation being stripped \u2014 only a faithful prose restatement. The 'why' survives."
  },
  {
   "locator": "\u00a733 (Worked example), lines 858-861",
   "excerpt": "In one run, the test author improvised acceptance tests with no template, no self-check, and no boundary validator. The expensive cross-artifact auditor was the first eye on them, found two missing AC mappings and a phantom reference, plus four substantive drifts (seven findings total), and the reconciler re-invoked both the test author and the Composer \u2014 three audit rounds, two of them re-runs caused by mechanical findings the author could have caught itself. ... A mid-run Blueprint revision would have tripped the freshness gate before the auditor ever saw the stale tests.",
   "type": "worked_example",
   "design_claim": "When a producer has no template, no self-check, and no boundary validator, cheap mechanical defects (missing mappings, phantom references) survive until the expensive late auditor is the first to see them, multiplying audit/reconciliation rounds; a producer-side self-check plus a boundary validator catches the mechanical class for free and lets the auditor spend its budget only on substantive drift, and a freshness gate stops stale predecessors from reaching the auditor at all.",
   "action": "strip_keep_claim",
   "restatement": "An artifact produced with no template, no producer self-check, and no boundary validator forces the expensive late-stage auditor to be the first reviewer; it then catches both cheap mechanical defects and substantive drift, and each mechanical finding drives an extra re-authoring and re-audit round. Adding a producer self-check and a boundary gate catches the mechanical class in the author's own context for free, so the auditor sees clean input and runs once on substantive issues only, and a freshness gate prevents stale upstream revisions from reaching the auditor.",
   "preserved": true,
   "loss": "Design claim and all rationale survive: producer with no template/self-check/boundary-validator forces the expensive late auditor to be first reviewer; mechanical findings each drive an extra re-author+re-audit round; producer self-check catches the mechanical class for free in-context; boundary gate + freshness gate keep the auditor seeing only clean, fresh input so it runs once on substantive drift. RESIDUAL LOSS (minor): the restatement drops every concrete count from this passage \u2014 'two missing AC mappings', 'four substantive drifts', 'seven findings total', 'three audit rounds', the '~5 min opus run'. The disposition's own rule says repo-specific evidence worth keeping must land in the plan or an evidence file, but the restatement does not route these numbers anywhere. They are not lost project-wide because the \u00a734 Metrics table independently records this run ('execution-pipeline-design | 7 audit, 3 recon'), so the evidence survives elsewhere in the same document \u2014 but only if \u00a734 is preserved by the broader refactor, which is outside this disposition's scope. The numbers that make the stakes vivid disappear from \u00a733 itself; a reader of \u00a733 alone loses the magnitude that motivates the claim. Recommend explicitly cross-referencing \u00a734 (or an evidence file) in the restatement so the quantitative backing is not orphaned."
  },
  {
   "locator": "\u00a733, Figure V13 (before), lines 863-881",
   "excerpt": "sequenceDiagram participant CO as Composer / participant TA as test-acceptance-author / participant XA as cross-artifact-auditor / participant RE as finalize-reconciler ... CO->>CO: blueprint v3 (AC-FR-1..14) ... TA->>TA: improvise tests (misses 13/14, phantom ref) ... XA->>XA: ~5 min opus run \u2014 2 missing, 1 phantom, 4 drifts ... round 3 \u2014 finally passes",
   "type": "worked_example",
   "design_claim": "Mechanical defects reaching the expensive auditor drive multiple audit/reconciliation rounds (the 'before' state).",
   "action": "strip_keep_claim",
   "restatement": "Replace the named-actor sequence diagram with an abstract one: Producer hands an un-self-checked artifact to the expensive Auditor, which finds both mechanical and substantive defects; the Reconciler re-invokes the producer and its upstream, and the auditor runs several rounds before passing. Keep the figure id (Figure V13) and the 'before' framing; remove the specific role names (Composer, test-acceptance-author, cross-artifact-auditor, finalize-reconciler), the version labels, and the runtime/finding counts.",
   "preserved": true,
   "loss": "Abstract design claim survives: an un-self-checked artifact reaching the expensive auditor drives multiple audit/reconciliation rounds, with the reconciler re-invoking both producer and upstream. Removing the named actors (Composer, test-acceptance-author, cross-artifact-auditor, finalize-reconciler) is correct \u2014 these are pipeline role names, and the abstract producer/auditor/reconciler framing keeps the structure. RESIDUAL LOSS (minor): the diagram is the SOLE carrier of the 'misses 13/14' datum \u2014 the author mapped only 1 of 14 acceptance criteria, a far more severe failure rate than the prose's 'two missing AC mappings' conveys. The restatement explicitly removes the finding counts, and unlike item 5 this 13/14 ratio has no home in \u00a734's Metrics table, so it is genuinely lost rather than relocated. It is severity evidence, not part of the claim's logic, so the claim does not break \u2014 but the disposition's rule ('repo-specific evidence worth keeping must land in the plan or an evidence file') is not honored for this datum: it is being deleted, not routed. If the 13/14 magnitude is considered worth keeping it should land in an evidence file; if it is judged disposable that is a defensible call, but the disposition should say so rather than silently drop it under 'remove counts'."
  },
  {
   "locator": "\u00a733, Figure V14 (after), lines 883-902",
   "excerpt": "sequenceDiagram participant CO as Composer / participant TA as test-acceptance-author / participant SC as self-check / participant BV as boundary validator / participant XA as cross-artifact-auditor ... SC-->>TA: FAIL \u2014 AC-FR-13/14 missing, phantom ref ... BV-->>XA: green (schema + cross-ref + freshness) ... XA->>XA: ~5 min opus run \u2014 4 substantive only",
   "type": "worked_example",
   "design_claim": "A producer self-check catches mechanical defects for free; the boundary validator gates on schema + cross-ref + freshness; the auditor then sees clean input and runs once (the 'after' state).",
   "action": "strip_keep_claim",
   "restatement": "Replace with an abstract sequence: Producer validates its draft against a self-check, fixes flagged mechanical defects in the same context, re-validates to pass, then hands off; the boundary validator confirms schema + cross-reference + freshness; the Auditor then runs once on substantive issues only. Keep the figure id (Figure V14) and the schema/cross-ref/freshness gate composition; remove the named roles, the version labels, and the runtime/finding counts.",
   "preserved": true,
   "loss": "No information loss. The abstract design claim (producer self-check catches mechanical defects in-context for free; boundary validator gates on schema + cross-ref + freshness; auditor then runs once on clean substantive-only input) is fully restated, and the figure caption at line 883 that carries that claim is retained. The stripped specifics \u2014 named roles (Composer, test-acceptance-author, etc.), version labels (blueprint v3/v4, AC-FR-13/14), and runtime/finding counts (~5 min opus, 4 substantive) \u2014 are pure repo evidence and are mirrored in the \u00a733 prose narrative (lines 859-861), which the disposition does NOT touch, so the before/after contrast survives in prose plus the untouched 'before' Figure V13. Minor non-loss caveat: abstracting V14 while leaving V13 concrete makes the paired figures stylistically asymmetric within the doc, but that is cosmetic, not informational."
  },
  {
   "locator": "\u00a734 (Metrics) table, lines 906-915",
   "excerpt": "| Run | Audit/recon cycles | Mechanical share (extrapolated) | Projected under pattern | Cycles saved | ... execution-pipeline-design | 7 audit, 3 recon | ~75% | 2 audit, 1 recon | 5 audit + 2 recon ... devcontainer-mcp-provisioning ... issue-capture-mechanism ... audit-findings-remediation ... pipeline-quickwins-hardening ... adr-placement-mechanism-repair ... pipeline-design-time-discipline ... execute-orchestrator-dispatch-repair",
   "type": "repo_metric",
   "design_claim": "Across many prior runs the dominant share of audit/reconciliation cycles is mechanical, so producer self-check plus boundary gating projects a large reduction in audit and reconciliation cycles; the worst case collapses the most.",
   "action": "relocate_to_plan",
   "target_note": "The whole per-run metrics table (named slugs, audit/recon counts, mechanical-share percentages, projected cycles) is repo-specific provenance that quantifies the expected payoff in THIS repo. It is the brownfield business case for the rollout and belongs with the plan (which already owns the rollout). In the principles doc, replace with the abstract claim that mechanical findings dominate audit/reconciliation churn and that producer self-check plus boundary gating is projected to remove most of it. No numbers survive in the principles doc.",
   "preserved": true,
   "loss": "No design information lost, conditional on the relocation actually being performed. The table is repo-specific provenance (named run slugs, per-run audit/recon counts, mechanical-share percentages, projected cycles) and is the brownfield business case \u2014 correctly assigned to the plan, which already owns the rollout. The abstract claim that mechanical findings dominate audit/reconciliation churn and that self-check + boundary gating projects a large reduction is retained, and crucially the 'worst case collapses the most' sub-claim is explicitly kept. WATCH-OUT, not a loss in the disposition itself: the plan does not yet contain this table (only a single passing '~75%' reference at plan lines 112/157). 'relocate_to_plan' is an instruction; the evidence is only preserved if the table is genuinely written into the plan's business case. If the strip happens without the corresponding plan addition, the evidence for WHY mechanical dominates would be destroyed."
  },
  {
   "locator": "\u00a734, aggregate paragraph, line 917",
   "excerpt": "Aggregate (conservative): reconciliation cycles across 8 runs ~15 \u2192 ~6 (\u221260%); architecture-audit re-runs ~14 \u2192 ~8 (\u221243%); mechanical load reaching the expensive auditor ~75% \u2192 ~5% (\u221293%); ~18 expensive opus invocations and ~9 human-gate interrupts avoided. The worst run alone collapses from 3 reconciliation cycles and 7 audit rounds to ~1 and ~2.",
   "type": "repo_metric",
   "design_claim": "In aggregate the pattern is projected to roughly halve reconciliation cycles and audit re-runs and to nearly eliminate the mechanical load reaching the expensive auditor, avoiding many expensive model invocations and human-gate interrupts.",
   "action": "relocate_to_plan",
   "target_note": "All aggregate counts and percentages (8 runs, ~15\u2192~6, ~14\u2192~8, ~75%\u2192~5%, ~18 invocations, ~9 interrupts) are repo-derived projections \u2014 they belong with the plan's business case. In the principles doc, the surviving abstract claim is qualitative only: producer self-check plus boundary gating is expected to sharply reduce reconciliation cycles and auditor re-runs and to keep nearly all mechanical load out of the expensive auditor, avoiding model invocations and human-gate interrupts.",
   "preserved": true,
   "loss": "No design information lost, with the same execution caveat as the metrics table. All aggregate counts (8 runs, ~15\u2192~6, ~14\u2192~8, ~75%\u2192~5%, ~18 invocations, ~9 interrupts) are repo-derived projections and belong with the plan's business case; the surviving abstract claim (sharply reduces reconciliation cycles and auditor re-runs, keeps nearly all mechanical load out of the expensive auditor, avoids model invocations and human-gate interrupts) preserves the qualitative payoff. The 'worst run collapses most' point is redundantly carried by disposition #2's retained claim, so dropping it from this restatement loses nothing. EXECUTION RISK identical to the table: these aggregate numbers are not currently in the plan, so 'preserved' depends on them actually landing there rather than just vanishing from the architecture doc."
  },
  {
   "locator": "\u00a734, Caveat blockquote, line 919",
   "excerpt": "Caveat. These percentages are extrapolated from audit-issue category counts, not measured timings \u2014 the only runtime telemetry today is install events. Direction and relative magnitude are well-supported; precise counts are illustrative. The observability surface (Part V) is what makes future numbers measured.",
   "type": "repo_metric",
   "design_claim": "Projected savings are extrapolated from issue-category counts rather than measured timings, because only minimal runtime telemetry exists today; an observability surface is what turns such projections into measured numbers.",
   "action": "strip_keep_claim",
   "restatement": "Keep as a methodological caveat with the repo specifics removed: projected savings of this kind are extrapolated from defect-category counts, not measured timings, when runtime telemetry is thin; the observability surface (Part V) is what lets future numbers be measured rather than estimated. The repo-specific 'only install events exist today' detail moves with the metrics to the plan.",
   "preserved": true,
   "loss": "No information loss. The methodological rationale a reader needs \u2014 that the savings are extrapolated from defect-category counts rather than measured timings, and that the observability surface (Part V) is what turns projections into measured numbers \u2014 is retained abstractly, including the Part V cross-reference. The only repo-specific detail removed ('the only runtime telemetry today is install events') is correctly moved with the metrics, since that detail is the concrete justification for the caveat and is meaningless detached from the numbers it qualifies. Same soft dependency as #2/#3: the 'install events only' fact must actually accompany the metrics into the plan."
  },
  {
   "locator": "\u00a735 (Rollout), line 923",
   "excerpt": "A single plan \u2014 [`implementation-plan.md`](implementation-plan.md) \u2014 builds this outside the feature pipeline (the pipeline is the artifact under repair; using it to build its own hardening invites the self-referential fragility named in \u00a729). It is structured as workstreams under one shared sequencing view:",
   "type": "code_citation",
   "design_claim": "The hardening is built by a single plan that runs OUTSIDE the pipeline it hardens, because using a system to build its own repair invites self-referential fragility (\u00a729); the work is structured as workstreams under one shared sequencing view.",
   "action": "strip_keep_claim",
   "restatement": "A single plan builds this architecture outside the pipeline it governs \u2014 a system under repair should not be used to build its own repair, which invites the self-referential fragility named in \u00a729 \u2014 and the work is organized as workstreams under one shared sequencing view. Remove the filename link (implementation-plan.md); keep the \u00a729 cross-reference.",
   "preserved": true,
   "loss": "No information loss. The load-bearing rationale is fully kept: the architecture is built OUTSIDE the pipeline it governs because a system under repair must not build its own repair (self-referential fragility), and the \u00a729 cross-reference that anchors that 'why' is explicitly retained. The work-is-organized-as-workstreams-under-one-sequencing-view framing is also kept. Only the implementation-plan.md filename link is removed \u2014 a pure citation with no design content. The same rationale is independently restated in the plan itself (plan line 49), so it is doubly safe."
  },
  {
   "locator": "\u00a735, workstream list, lines 925-930",
   "excerpt": "WS-0 \u2014 Substrate foundation: the historical-run regression corpus, the parallel-run harness, the single drift sentinel, the CI workflow, and the technology-boundary fitness functions. WS-1 \u2014 Document reliability ... WS-2 \u2014 Domain extensibility ... WS-3 \u2014 Knowledge governance ... WS-4 \u2014 Observability: built once ... then packaged as the first domain through the WS-2 scaffold. Close-out: multi-pipeline migration \u2192 a single warn\u2192enforce flip ... run-pinning \u2192 the discipline skill.",
   "type": "worked_example",
   "design_claim": "A coherent rollout sequences the substrate foundation first (regression corpus, parallel-run harness, single drift sentinel, CI fitness functions), then document reliability, domain extensibility, and knowledge governance, building observability once and packaging it as the first domain through the extensibility scaffold, and closing with migration and a single warn-to-enforce flip across gate families.",
   "action": "relocate_to_plan",
   "target_note": "The concrete workstream breakdown (WS-0..WS-4, close-out) is the rollout sequencing for THIS build and is plan material \u2014 it names the specific deliverables and ordering of the implementation. The principles doc keeps only the abstract rollout claim: build the validation substrate first, layer the reliability/extensibility/knowledge mechanisms on it, build cross-cutting capabilities once and ship them through the extensibility pattern, and gate the cutover with a single warn-to-enforce flip. The WS labels and item lists move to the plan.",
   "preserved": true,
   "loss": "No design information lost. The concrete WS-0..WS-4 + close-out breakdown is rollout sequencing for this specific build and already lives in full in the plan (plan lines 88-296), so relocating it duplicates nothing and strands nothing. The abstract rollout shape is retained: substrate/validation foundation first, then the reliability/extensibility/knowledge mechanisms layered on it, cross-cutting capabilities (observability) built once and shipped through the extensibility pattern, gated by a single warn\u2192enforce flip. REVIEWER NOTE (not a loss under the stated scope): two abstract claims sit at line 932, just past this locator's range \u2014 'all workstreams register into the ONE sentinel and read the ONE canonical layer' and 'the validation backbone needs no feature-pipeline run.' These are genuine principles-level claims (single-source convergence applied to rollout; the concrete resolution of the self-referential-fragility worry). Because line 932 is outside lines 925-930, it is not stripped and stays self-contained (it says 'All workstreams' generically, so it does not dangle when the WS bullets go). The disposition is therefore safe as written, but whoever executes it should confirm line 932 is left intact and not swept up with the bullet list, since the abstract restatement in the target_note does not itself re-carry those two claims."
  },
  {
   "locator": "\u00a735, closing paragraph, line 932",
   "excerpt": "All workstreams register their checks into the one drift sentinel and read from the one canonical layer. The validation backbone is the historical-run regression corpus plus parallel-run diffing plus the conformance check run against the live repo \u2014 no feature-pipeline run is required to validate any of it.",
   "type": "worked_example",
   "design_claim": "All checks register into a single drift sentinel and read from one canonical layer; the validation backbone (regression corpus + parallel-run diffing + conformance check against the live system) lets the architecture be validated without invoking the pipeline it governs.",
   "action": "strip_keep_claim",
   "restatement": "All checks register into the one drift sentinel and read from the one canonical layer, and the validation backbone \u2014 a historical regression corpus, parallel-run diffing, and a conformance check against the live system \u2014 validates the architecture without requiring a run of the pipeline it governs. This is already nearly tech-agnostic; only the phrase 'feature-pipeline run' narrows it, so generalize to 'a run of the governed pipeline.'",
   "preserved": true,
   "loss": "No loss. The restatement faithfully keeps all three load-bearing elements of the design claim \u2014 the single drift sentinel, the single canonical layer, and the validation backbone (historical regression corpus + parallel-run diffing + conformance check against the live system) that lets the architecture be validated without running the pipeline it governs. Only the repo-narrowing phrase 'feature-pipeline run' is generalized to 'a run of the governed pipeline,' which loses nothing abstract. The same three-part validation backbone is independently carried in implementation-plan.md (lines 61-66), so even the operational detail is double-anchored. The 'self-referential fragility' rationale for not using the pipeline on itself is already stated abstractly at \u00a729 and \u00a735's preamble (line 923), so the why survives the strip."
  },
  {
   "locator": "Appendix A \u2014 Evidence map, intro, line 940",
   "excerpt": "Direct = the symptom appears verbatim in audit JSONs / version counts / removal records; Inferred = the most likely cause of an observed effect, not a single labelled incident.",
   "type": "evidence_map",
   "design_claim": "Evidence backing each mechanism is classified as Direct (symptom observed verbatim in artifacts) vs Inferred (most-likely cause of an observed effect), so readers can weigh the strength of each motivation.",
   "action": "relocate_to_evidence_file",
   "target_note": "The Direct-vs-Inferred classification convention and the entire evidence map are repo provenance: they map mechanisms to specific observed symptoms in this repo's audit artifacts. The whole table is the proof that the problems were real here, which is neither timeless principle nor forward-looking plan. Move Appendix A in full to a new architecture-evidence.md, keeping the Direct/Inferred convention as that file's header.",
   "preserved": true,
   "loss": "Preserved CONDITIONAL on the relocation actually executing. The Direct-vs-Inferred convention is pure provenance metadata \u2014 it has no meaning in a principles doc once the table it annotates is gone, so moving it with the table loses nothing from the principles doc. The one hard dependency: architecture-evidence.md does not yet exist (confirmed absent) and the plan currently contains none of this evidence. If the rewrite strips Appendix A but the new evidence file is not created in the same change, the convention and the entire proof-the-problems-were-real vanishes. Disposition is correct AS WRITTEN (it creates the file); the risk is purely in execution sequencing, which the workflow's RISK NOTE must enforce."
  },
  {
   "locator": "Appendix A row 1 (self-check + boundary validators), line 944",
   "excerpt": "Producer self-check + boundary validators (III) | Cheap mechanical errors caught only by the late auditor | 165 findings counted (Direct); the 125 \"mechanical\" split is by category mapping (Inferred-by-categorization); one run ran 7 audit rounds (Direct) | Direct / Inferred",
   "type": "evidence_map",
   "design_claim": "Cheap mechanical errors are caught only by the late auditor \u2014 the motivation for producer self-check plus boundary validators.",
   "action": "relocate_to_evidence_file",
   "target_note": "The counts (165 findings, 125 mechanical, 7 audit rounds) are repo metrics that substantiate the mechanism; they belong in architecture-evidence.md alongside the rest of Appendix A. The abstract design claim already lives in Part III; nothing is lost from the principles doc.",
   "preserved": true,
   "loss": "Design claim survives \u2014 verified in Part III \u00a78: gates are ordered 'cheapest first, so the expensive reviewer sees only input that already cleared the cheap checks,' which is exactly the motivation 'cheap mechanical errors caught only by the late auditor.' The counts (165 findings, 125 mechanical, 7 audit rounds) are repo metrics that legitimately relocate. One caveat for the reader, not a blocker: the '125 mechanical' figure is the quantitative basis for the headline '~75% mechanical-win' that drives the whole architecture (plan line 157 leans on it), so the evidence file must keep the Inferred-by-categorization caveat attached to it, or a future reader cannot judge how soft that 75% is. The disposition preserves this by moving the row verbatim with its strength column."
  },
  {
   "locator": "Appendix A row 2 (contracts for ATs/PVs/Research Plan/tasks.json), line 945",
   "excerpt": "Contracts for ATs / Phase Validators / Research Plan / tasks.json (I, III) | Template-less artifacts first seen by the cross-artifact auditor | ATs + PVs have no template; that seam is UNGATED | Direct",
   "type": "evidence_map",
   "design_claim": "Template-less artifacts are first seen by the expensive cross-artifact auditor \u2014 the motivation for giving every produced artifact a contract.",
   "action": "relocate_to_evidence_file",
   "target_note": "Names specific repo artifacts (Acceptance Tests, Phase Validators, Research Plan, tasks.json) and asserts a repo fact (those seams are ungated). Move to architecture-evidence.md. The timeless claim \u2014 every produced artifact needs a contract or it reaches the auditor untriaged \u2014 is carried in Parts I/III.",
   "preserved": true,
   "loss": "Design claim survives \u2014 verified in Part III (\u00a78 'Boundary validator,' \u00a710 'An authored work product -> Sub-agent + a contract in canonical'): every produced artifact needs a contract or it reaches the auditor untriaged. The repo facts (named artifacts Acceptance Tests / Phase Validators / Research Plan / tasks.json; the 'that seam is UNGATED' assertion) are repo-specific and relocate cleanly. No rationale or constraint is lost from the principles doc; the named-artifact list is precisely the kind of repo provenance that belongs in the evidence file, not the timeless doc."
  },
  {
   "locator": "Appendix A row 3 (manifest + gates-declared), line 946",
   "excerpt": "Manifest + gates-declared (III) | Silent gate removal; unguarded seams | 3 UNGATED + 2 reviewed-only seams; filename drift | Direct",
   "type": "evidence_map",
   "design_claim": "Gates that are remembered rather than declared can be silently removed, leaving unguarded seams \u2014 the motivation for a manifest that declares gates.",
   "action": "relocate_to_evidence_file",
   "target_note": "The counts (3 ungated + 2 reviewed-only seams, filename drift) are repo-observed; move to architecture-evidence.md. The design claim is carried in Part III's manifest/gates-declared mechanism.",
   "preserved": true,
   "loss": "Design claim survives intact \u2014 Part III \u00a78 'The keystone' carries it almost verbatim: 'When topology is prose, an edit can silently drop a gate and nothing notices \u2014 the deepest failure mode,' resolved by a manifest where 'the sentinel can assert every edge has a gate.' The counts (3 ungated + 2 reviewed-only seams, filename drift) are repo-observed and relocate. No why is lost."
  },
  {
   "locator": "Appendix A row 4 (freshness gate), line 947",
   "excerpt": "Freshness gate (IV) | Stale-predecessor drift as upstreams are revised | 72 consistency findings incl. stale contract-ID refs + version desync; Blueprint v1\u2013v5, PRD v1\u2013v3 churn | Direct (symptom) / Inferred (that staleness drove the churn)",
   "type": "evidence_map",
   "design_claim": "As upstream artifacts are revised, downstream artifacts drift stale \u2014 the motivation for a freshness gate.",
   "action": "relocate_to_evidence_file",
   "target_note": "The counts (72 consistency findings, Blueprint v1-v5 / PRD v1-v3 churn) are repo metrics; move to architecture-evidence.md. The freshness-gate design claim lives in Part IV.",
   "preserved": true,
   "loss": "Design claim survives \u2014 Part IV \u00a711 states the stale-predecessor problem fully and abstractly (cache-invalidation over the run's artifact dependency graph; untruth injected early, caught only by the expensive late auditor). The counts relocate. One traceability risk the rewrite must handle (not a defect in the disposition itself): the plan RE-CITES the same '72 instances' metric at implementation-plan.md:157 as the justification for the cross-artifact validator, and the plan's traceability spine (lines 15-30) is built on the premise that problem evidence lives 'in the architecture.' Once this row moves to architecture-evidence.md, that spine and the plan's inline '72 instances' citation point at evidence that no longer lives where they say it does. The disposition does not lose design information, but it must be paired with re-pointing the plan's traceability spine at architecture-evidence.md \u2014 exactly the cross-file hazard the workflow's RISK NOTE step exists to catch."
  },
  {
   "locator": "Appendix A row 5 (observability), line 948",
   "excerpt": "Observability (V) | Cannot tell where cycle time goes; runs stall invisibly | Only install telemetry exists; 3 runs stalled pre-audit with no recorded reason; every metric here is extrapolated | Direct",
   "type": "evidence_map",
   "design_claim": "Without an observability surface you cannot tell where cycle time goes and runs stall invisibly \u2014 the motivation for observability.",
   "action": "relocate_to_evidence_file",
   "target_note": "Repo facts (only install telemetry today, 3 runs stalled pre-audit, all metrics extrapolated) move to architecture-evidence.md. The observability design claim is in Part V. The 'metrics are extrapolated' point is also preserved abstractly in the \u00a734 caveat restatement.",
   "preserved": true,
   "loss": "No net loss. The design claim survives independently of the evidence row: 'being unable to tell where cycle time went' is stated in the Part V sentinel-vs-observability table (\u00a716, line 454), and the 'metrics are extrapolated' point is carried abstractly in the \u00a734 caveat (line 919, 'These percentages are extrapolated from audit-issue category counts ... the only runtime telemetry today is install events'). The three repo facts (only install telemetry, 3 runs stalled pre-audit, every metric extrapolated) are pure empirical grounding and belong in architecture-evidence.md. ONE skeptical note: 'runs stalled with no recorded reason' is the specific symptom motivating stall-detection telemetry; the abstract version ('runs stall invisibly') survives in Part I line 18-area and \u00a716, so no rationale is lost \u2014 only the count moves. Disposition is sound."
  },
  {
   "locator": "Appendix A row 6 (domain registry + conformance/orphan check), line 949",
   "excerpt": "Domain registry + conformance/orphan check (VI) | Unpredictable add; orphaned removal | The ragged bundle matrix; one removal touched 15+ surfaces and orphaned a user-scope install | Direct",
   "type": "evidence_map",
   "design_claim": "Without a domain registry, adding a domain is unpredictable and removing one orphans installs across many surfaces \u2014 the motivation for a registry plus conformance/orphan check.",
   "action": "relocate_to_evidence_file",
   "target_note": "Repo facts (ragged bundle matrix, one removal touched 15+ surfaces and orphaned a user-scope install) move to architecture-evidence.md. The extensibility design claim is in Part VI.",
   "preserved": true,
   "loss": "No loss. The motivation (unpredictable add, orphaned removal) is fully restated in Part VI: \u00a719-22 build the registry/conformance/orphan design, and \u00a721 line 578 preserves the exact lesson abstractly ('the check that would have caught the orphaned capability the manual removal missed'). Part I line 18 also carries the abstract problem statement. The repo facts (ragged bundle matrix, 15+ surfaces, orphaned user-scope install) are evidence and relocate cleanly. Note the richer narrative of these facts already lives at lines 127/143 in Part I, so the evidence file is a consolidation, not the sole home. Disposition is sound."
  },
  {
   "locator": "Appendix A row 7 (knowledge governance), line 950",
   "excerpt": "Knowledge governance: index + supersession + freshness/conflict (VII) | ADR sprawl; stale/conflicting memory | 68 ADRs + ~1,470 cross-references (approx., by grep) + no index + 2 live-but-superseded records; memory has no freshness/conflict field or check; multi-level memory is additive (\"no override, no de-duplication\") | Direct (counts approx.)",
   "type": "evidence_map",
   "design_claim": "Decision records sprawl and memory goes stale or self-conflicting without governance \u2014 the motivation for a generated index, supersession lifecycle, and freshness/conflict checks.",
   "action": "relocate_to_evidence_file",
   "target_note": "All counts (68 ADRs, ~1,470 cross-references, 2 live-but-superseded records) are repo metrics; move to architecture-evidence.md. The knowledge-governance design claim is in Part VII. Note the abstract 'additive memory has no override/de-duplication' observation is also a durable claim already carried in Part VII.",
   "preserved": true,
   "loss": "No loss. The design claim survives in Part VII (\u00a724, line 642 restates 'additive \u2014 no override, no de-duplication' verbatim as a durable observation) and in rules R17/R18/R19 plus anti-pattern A19 (line 762, 'no override engine exists'). The disposition correctly flags that the abstract additive-memory point is independently durable, which I confirmed. All three counts (68 ADRs, ~1,470 cross-refs, 2 superseded records) are repo metrics already echoed at line 642 and relocate cleanly. Disposition is sound."
  },
  {
   "locator": "Appendix A row 8 (maintainability/tiering), line 951",
   "excerpt": "Maintainability / tiering (VIII) | Over/under-gating; meta-run fragility | Two runs are self-improving meta-runs; one was abandoned/split mid-pipeline | Direct",
   "type": "evidence_map",
   "design_claim": "Uniform gating over/under-gates and self-improving meta-runs are fragile \u2014 the motivation for blast-radius tiering and meta-run discipline.",
   "action": "relocate_to_evidence_file",
   "target_note": "Repo facts (two self-improving meta-runs, one abandoned/split mid-pipeline) move to architecture-evidence.md. The tiering/meta-run-fragility design claim is in Part VIII (and \u00a729's self-referential-fragility point).",
   "preserved": true,
   "loss": "No loss. Both halves of the design claim survive: over/under-gating motivates the tiering rule R10 (line 706) and \u00a729's 'Tier to blast radius' (line 791); meta-run fragility is preserved in \u00a729's self-referential-fragility failure-mode row (line 787, 'pin the run and tier gates so over-tightened gates don't bite the meta-runs') and anti-pattern A24 (line 767). The repo facts (two self-improving meta-runs, one abandoned/split) are evidence and relocate cleanly. Section reference is accurate \u2014 \u00a729 is 'Maintainability' (line 774). Disposition is sound."
  },
  {
   "locator": "Appendix F, TB1 'Evidenced in' cell, line 1072",
   "excerpt": "Evidenced in | \u00a730; `devcontainer.json`; Codespaces machine-types docs",
   "type": "evidenced_in_column",
   "design_claim": "The single-container / no-compose dev-environment boundary (TB1) is grounded in the project's actual dev-environment configuration.",
   "action": "strip_keep_claim",
   "restatement": "Drop the entire 'Evidenced in' column from the Appendix F table. For TB1 keep only the intra-document reference (\u00a730) inline if needed; remove the repo-file citation (devcontainer.json) and the external-doc citation (Codespaces machine-types docs). The boundary itself stays stated tech-agnostically. The removed file/doc citations relocate to architecture-evidence.md.",
   "preserved": true,
   "loss": "Preserved, with a caveat the disposition itself leaves loose. The boundary and its full rationale (single-container/no-compose hard rule, 4-core/16GB floor logic, cost-dial) are restated inline in the TB1 cell AND fully in \u00a730 (line 813, including the '8 GB hostRequirements is a loose minimum' reasoning and the machine-types-docs logic). So no design info is lost. The external 'Codespaces machine-types docs' citation is a pure citation and may vanish; the devcontainer.json citation is evidence and relocates. RISK: the disposition says 'keep the \u00a730 cross-reference if retained inline' \u2014 that 'if' is load-bearing. If the whole column is dropped without re-anchoring \u00a730 somewhere, a reader loses the in-document path from the boundary to its rationale section. Recommendation: keep the \u00a730 intra-doc anchor (it is not a repo citation \u2014 it is a within-document reference and the RULE only strips repo-references). Treat dropping \u00a730 as out of scope for this strip."
  },
  {
   "locator": "Appendix F, TB2 'Evidenced in' cell, line 1073",
   "excerpt": "Evidenced in | \u00a730 durability table; `.gitignore`",
   "type": "evidenced_in_column",
   "design_claim": "The git-is-system-of-record boundary (TB2) is grounded in the project's durability configuration.",
   "action": "strip_keep_claim",
   "restatement": "Remove the 'Evidenced in' cell; keep the \u00a730 cross-reference if retained inline, drop the repo-file citation (.gitignore). The boundary stays. The .gitignore citation relocates to architecture-evidence.md.",
   "preserved": true,
   "loss": "Preserved, same caveat as TB1. The boundary rationale ('anything that must survive rebuild is committed; the runtime dir is ephemeral') is stated inline in the TB2 cell and fully in the \u00a730 durability table (lines 806-811). The boundary is also reinforced by R30 (line 726, 'git history is permanent (TB2)'), which does not depend on the evidence cell. The .gitignore citation is pure repo evidence and relocates cleanly. RISK: as with TB1, the '\u00a730 durability table' reference is an intra-document anchor, not a repo citation \u2014 it should be retained, not dropped, since the RULE targets repo-references only. Dropping the \u00a730 link would lose the reader's path to the durability rationale even though the rationale text itself survives."
  },
  {
   "locator": "Appendix F, TB3 'Evidenced in' cell, line 1074",
   "excerpt": "Evidenced in | \u00a718 (\"never depends on the backend being up\")",
   "type": "evidenced_in_column",
   "design_claim": "The no-mandatory-external-dependency boundary (TB3) is grounded in the \u00a718 durability discipline.",
   "action": "strip_keep_claim",
   "restatement": "Remove the 'Evidenced in' column. TB3's evidence is an intra-document reference (\u00a718), which the KEEP rule preserves \u2014 it may stay inline as a cross-reference; nothing repo-external to relocate here. The column itself goes for consistency with the other rows.",
   "preserved": true,
   "loss": "No loss. The design claim ('the pipeline completes with backends down') lives in the TB3 row's own constraint text in column 2, not in the 'Evidenced in' cell, so stripping the citation does not touch it. The pointer's target \u2014 \u00a718's verbatim 'The pipeline never depends on the backend being up' (line 487) \u2014 is itself preserved in the home doc, and \u00a718 is intra-document, which the KEEP rule explicitly allows to remain inline. One caveat the disposition glosses: this is the ONLY TB row that points to an internal section (\u00a718) rather than a repo file; TB1/TB2/TB10 also cite \u00a7-sections AND repo files. If the column is removed 'for consistency,' the OTHER rows lose genuine repo-file pointers (devcontainer.json, .gitignore, ADR-0039) \u2014 but those are out of scope for THIS item, and for TB3 specifically the disposition is sound."
  },
  {
   "locator": "Appendix A, line 938 (section heading)",
   "excerpt": "## Appendix A \u2014 Evidence map",
   "type": "evidence_map",
   "design_claim": "Each governance mechanism in the architecture is motivated by an observed problem; the evidence map is the table tying mechanism to problem to corroborating evidence.",
   "action": "discard_with_rationale",
   "target_note": "The entire Appendix A section is an evidence map \u2014 explicitly named as a strip target. The Mechanism\u2192Problem pairings it documents survive because each mechanism is already specified in its home Part (I\u2013VIII) of the principles doc, and the abstract Problem each solves is restated in the per-row entries below. Removing the appendix heading itself loses nothing once those claims are confirmed present in their home sections; the heading is a wrapper around repo-specific evidence.",
   "preserved": true,
   "loss": "Minor, recoverable. The heading itself is a wrapper and carries no design information, so removing it loses nothing on its own. The disposition's load-bearing assumption \u2014 that every Mechanism is specified in its home Part and every Problem is restated there \u2014 holds for the rows I checked: producer self-check + boundary validators are in \u00a78 (lines 270-271) with the 'producer cannot mark its own homework' why; the template-less-artifact problem is in \u00a73 (lines 92-93, 117-123). The one thing the appendix-as-a-whole provides that no single Part does is the consolidated mechanism-to-problem-to-evidence MATRIX VIEW \u2014 the at-a-glance audit that every mechanism traces to an observed problem. That cross-cutting traceability is an artifact property, not a design claim, so it is legitimately not 'principles doc' material; but if neither the plan nor an evidence file reproduces the table, a reader loses the single place that proves no mechanism is unmotivated. Flagged, not blocking."
  },
  {
   "locator": "Appendix A, line 940 (legend)",
   "excerpt": "*Direct* = the symptom appears verbatim in audit JSONs / version counts / removal records; *Inferred* = the most likely cause of an observed effect, not a single labelled incident.",
   "type": "evidence_map",
   "design_claim": "Evidence for a design claim varies in strength: some is a directly observed symptom, some is an inferred cause; the architecture distinguishes the two so claims are not over-stated.",
   "action": "relocate_to_evidence_file",
   "target_note": "The Direct/Inferred distinction is a provenance-grading convention, not a design principle. It references repo-specific sources (audit JSONs, version counts, removal records). Move the convention and its definition to architecture-evidence.md as the grading key for the relocated evidence rows; it does not belong in the timeless principles doc.",
   "preserved": true,
   "loss": "No design-information loss; correct call. The Direct/Inferred convention is a provenance-grading key, not a design principle, and it is meaningless without the repo-specific evidence rows it grades (audit JSONs, version counts, removal records) \u2014 so it belongs wherever those rows land (architecture-evidence.md), exactly as dispositioned. The convention's substance is fully carried by the legend text being relocated verbatim. One thing to watch: \u00a71 line 17 and \u00a73 line 51 ALREADY embed this same distinction inline ('a categorization estimate, not a measured rework-time share') \u2014 so the *idea* survives in the principles doc even without the legend, which strengthens (not weakens) the case for relocating the formal key."
  },
  {
   "locator": "Appendix A, line 942 (table header)",
   "excerpt": "| Mechanism | Problem it solves | Evidence | Strength |",
   "type": "evidence_map",
   "design_claim": "Mechanisms are justified by the problems they solve; the principles doc keeps the mechanism\u2192problem mapping but not the per-row evidence/strength grading.",
   "action": "strip_keep_claim",
   "target_note": "Drop the 'Evidence' and 'Strength' columns (repo-specific). The 'Mechanism' and 'Problem it solves' columns are abstract and already restated inline in their home Parts.",
   "restatement": "Each governance mechanism exists to solve a specific, recurring class of pipeline failure; the mechanism and the failure class it addresses are stated where the mechanism is defined.",
   "preserved": true,
   "loss": "No loss. Dropping the 'Evidence' and 'Strength' columns while keeping 'Mechanism' + 'Problem it solves' is consistent with the rule, and the restatement ('each mechanism exists to solve a specific recurring failure class, stated where the mechanism is defined') is accurate \u2014 \u00a78 and \u00a73 do state mechanism+problem at the point of definition. The header row carries no information independent of its cells, so its removal is purely structural. The only residual risk is the same matrix-view concern from the Appendix A heading item: the abstract mechanism\u2192problem PAIRING is preserved per-mechanism, but the consolidated header that frames them as one auditable mapping is not reproduced unless the plan/evidence file rebuilds the table. Acceptable."
  },
  {
   "locator": "Appendix A, line 944 (row: Producer self-check + boundary validators)",
   "excerpt": "| Producer self-check + boundary validators (III) | Cheap mechanical errors caught only by the late auditor | 165 findings counted (Direct); the 125 \"mechanical\" split is by category mapping (Inferred-by-categorization); one run ran 7 audit rounds (Direct) | Direct / Inferred |",
   "type": "repo_metric",
   "design_claim": "Cheap, mechanical defects (missing sections, broken cross-refs) should be caught at the producing stage and at stage boundaries, not deferred to the expensive late auditor; the majority of defects are of this cheap mechanical class.",
   "action": "strip_keep_claim",
   "target_note": "The counts (165 findings, 125 mechanical, 7 audit rounds, one specific run) are repo metrics; relocate them to the plan as brownfield motivation if the plan wants the empirical backing. The design claim is preserved in Part III where producer self-check + boundary validators are specified.",
   "restatement": "Most pipeline defects are cheap, mechanical errors (missing sections, broken cross-references, stale predecessors); without producer self-checks and boundary validators these slip through to the expensive late auditor, where a single run can spend many audit rounds re-catching them.",
   "preserved": true,
   "loss": "No loss \u2014 this is the best-covered row. The design claim survives twice over: \u00a78 lines 270-271 specify producer self-check + boundary validator WITH the rationale ('cheapest place, context still hot'; 'producer cannot mark its own homework'), and the restatement faithfully captures the 'slips through to the expensive late auditor' consequence. Critically, the repo metrics this row would relocate (165 findings, 125 mechanical, 7 audit rounds) are NOT actually unique to the appendix \u2014 \u00a71 line 17, \u00a73 line 51, and the worked example (lines 859-861, 917) already carry the 125-of-165 count, the ~three-quarters figure, AND the 7-audit-rounds run, each with the measurement caveat. So even if the plan never picks up the metrics, the empirical backing is not lost from the principles doc. The disposition is conservative in a good way; nothing a reader needs disappears."
  },
  {
   "locator": "Appendix A, line 945 (row: Contracts for ATs / Phase Validators / Research Plan / tasks.json)",
   "excerpt": "| Contracts for ATs / Phase Validators / Research Plan / tasks.json (I, III) | Template-less artifacts first seen by the cross-artifact auditor | ATs + PVs have no template; that seam is UNGATED | Direct |",
   "type": "repo_metric",
   "design_claim": "Every produced artifact type needs a contract/template enforced at its producing seam; artifact types lacking a template are first validated only by the downstream cross-artifact auditor, which is too late.",
   "action": "strip_keep_claim",
   "target_note": "The specific artifact names (ATs, Phase Validators, Research Plan, tasks.json) and the 'UNGATED' seam observation are repo-specific; relocate the concrete list of which artifacts lacked templates to the plan as the brownfield gap inventory. Design claim is preserved in Parts I and III.",
   "restatement": "Artifact types that lack a producing-side template/contract are first checked only by a downstream cross-artifact auditor, leaving the producing seam ungated; every artifact type the pipeline emits must have a contract enforced at the seam that produces it.",
   "preserved": true,
   "loss": "No loss; the relocation target already exists. The design claim ('artifact types lacking a producing-side template are first checked only by a downstream cross-artifact auditor \u2014 too late; every emitted artifact type needs a contract at its producing seam') is restated accurately and is grounded in \u00a73 (line 93: 'the expensive cross-artifact auditor is the first eye on them'; lines 117-123 enumerate the template gaps) and Part I/III. The concrete artifact inventory the disposition wants in the plan is ALREADY in implementation-plan.md: Research Plan version field (line 147), tasks.json schema (line 148), and ungated-seam wiring (line 161). The 'UNGATED' seam label is repo-specific and reproduced in \u00a73's Figure V1 (lines 60-66) and the seam table (lines 92-93), so it is not lost either. The only nuance: 'that seam is UNGATED' in the row is graded 'Direct' evidence; if the formal Direct/Inferred grading is what a reader wants, it moves to the evidence file per the line-940 disposition \u2014 consistent across items."
  },
  {
   "locator": "Appendix A, line 946 (row: Manifest + gates-declared)",
   "excerpt": "| Manifest + gates-declared (III) | Silent gate removal; unguarded seams | 3 UNGATED + 2 reviewed-only seams; filename drift | Direct |",
   "type": "repo_metric",
   "design_claim": "Gates must be declared in a canonical manifest and checked for drift, so that a gate cannot be silently removed and so every seam's gating status is explicit; otherwise seams go unguarded and filenames drift undetected.",
   "action": "strip_keep_claim",
   "target_note": "The counts (3 ungated + 2 reviewed-only seams) and the filename-drift observation are repo metrics; relocate the seam inventory to the plan. Design claim is preserved in Part III (manifest + gates-declared).",
   "restatement": "When gates live only in orchestrator prose rather than a declared manifest, gates can be silently removed, seams go unguarded, and artifact filenames drift undetected; declaring gates in a canonical manifest with a drift check makes each seam's gating status explicit and silent removal impossible.",
   "preserved": true,
   "loss": "No net loss. The abstract design claim (gates declared in a canonical manifest + a drift check so a gate cannot be silently removed and every seam's gating status is explicit) is fully carried by Part III \u00a78 \u201cThe keystone\u201d (line 275: topology as declarative manifest data; sentinel asserts \u2018every edge has a gate\u2019). The repo metrics being stripped are NOT unique to this Appendix row \u2014 they already live, in richer form, in the body of the same document: the ungated/reviewed-only seam inventory is at \u00a72 line 27, \u00a73 line 87 (\u2018four ungated seams and one reviewed-only seam\u2019), and Figure V1 (lines 55\u201370); filename drift is named at line 51. The plan also already gates these seams (implementation-plan.md:161). So even if the metric never reaches the plan, the reader loses nothing the document does not state elsewhere. Minor watch-item: the \u2018Direct\u2019 strength qualifier (this is observed verbatim, not inferred) is an epistemic tag that the restatement drops; the body asserts the seam counts as fact without that explicit Direct/Inferred labeling. Low-severity \u2014 the count is self-evidently direct."
  },
  {
   "locator": "Appendix A, line 947 (row: Freshness gate)",
   "excerpt": "| Freshness gate (IV) | Stale-predecessor drift as upstreams are revised | 72 consistency findings incl. stale contract-ID refs + version desync; Blueprint v1\u2013v5, PRD v1\u2013v3 churn | Direct (symptom) / Inferred (that staleness drove the churn) |",
   "type": "repo_metric",
   "design_claim": "When an upstream artifact is revised, its downstream descendants become stale and drift out of consistency; a freshness gate must detect stale predecessors and force reconciliation before the stale artifact is consumed.",
   "action": "strip_keep_claim",
   "target_note": "The counts (72 consistency findings) and the named version churn (Blueprint v1\u2013v5, PRD v1\u2013v3) are repo metrics; relocate to the plan as the empirical motivation for the freshness gate. Design claim is preserved in Part IV.",
   "restatement": "Revising an upstream artifact silently staled its downstream descendants, producing consistency defects such as stale contract-ID references and version desync as documents churned through successive revisions; a freshness gate must detect a stale predecessor and force reconciliation before the descendant is consumed.",
   "preserved": true,
   "loss": "No net loss, but the strength-qualifier loss is real here and worth flagging. The design claim (an upstream revision stales descendants; a freshness gate must detect a stale predecessor and force reconciliation before consumption) is fully carried by Part IV \u00a711 (lines 366\u2013368, the stale-predecessor / cache-invalidation framing). The stripped metrics are already in the body: 72 consistency findings at \u00a72 line 51 (with the same \u2018version desync, filename drift, stale contract-ID references\u2019 breakdown), and the Blueprint v1\u2013v5 / PRD v1\u2013v3 churn is in the per-run table at lines 40\u201344. The plan also cites the 72 instances (implementation-plan.md:157). The one thing ONLY this row states is the explicit epistemic split \u2018Direct (symptom) / Inferred (that staleness drove the churn)\u2019 \u2014 i.e. the honest admission that staleness-as-cause is an inference, not an observed incident. The restatement asserts staleness drove the churn flatly (\u2018Revising an upstream artifact silently staled its downstream descendants\u2019), erasing that this causal link is inferred. If the restatement lands in the plan/principles without the qualifier, a reader loses the \u2018how confident are we\u2019 signal. Recommend the restatement preserve the inferred-causation hedge."
  },
  {
   "locator": "Appendix A, line 948 (row: Observability)",
   "excerpt": "| Observability (V) | Cannot tell where cycle time goes; runs stall invisibly | Only install telemetry exists; 3 runs stalled pre-audit with no recorded reason; every metric here is extrapolated | Direct |",
   "type": "repo_metric",
   "design_claim": "Without run-level observability the pipeline cannot show where cycle time is spent or why a run stalled; stage- and actor-level telemetry is required to make stalls and bottlenecks visible.",
   "action": "strip_keep_claim",
   "target_note": "The observations (only install telemetry exists, 3 runs stalled pre-audit with no recorded reason, metrics extrapolated) are repo-specific; relocate to the plan as the gap that motivates the observability layer. Design claim is preserved in Part V.",
   "restatement": "Without run-level observability, the pipeline cannot show where cycle time is spent and runs can stall with no recorded reason; stage-boundary and per-actor telemetry are required to make bottlenecks and silent stalls visible.",
   "preserved": true,
   "loss": "No net loss. The design claim (without run-level observability you cannot see where cycle time goes or why a run stalled; stage-boundary + per-actor telemetry make stalls/bottlenecks visible) is carried by Part V \u00a715\u2013\u00a717 and by \u00a716\u2019s sentinel-vs-observability table (line 454: \u2018Prevents \u2026 being unable to tell where cycle time went\u2019). Every stripped observation is already in the body: \u2018only install telemetry exists\u2019 is at \u00a72 line 32 and the \u00a734 caveat line 919; \u20183 runs stalled pre-audit\u2019 is in the per-run table line 48; \u2018every metric is extrapolated\u2019 is stated even more carefully at line 919 (\u2018extrapolated from audit-issue category counts, not measured timings\u2019). The restatement actually drops the \u2018every metric here is extrapolated\u2019 self-caveat, but that caveat survives independently and more prominently at line 919, so no information is lost from the document as a whole."
  },
  {
   "locator": "Appendix A, line 949 (row: Domain registry + conformance/orphan check)",
   "excerpt": "| Domain registry + conformance/orphan check (VI) | Unpredictable add; orphaned removal | The ragged bundle matrix; one removal touched 15+ surfaces and orphaned a user-scope install | Direct |",
   "type": "repo_metric",
   "design_claim": "Adding or removing a domain/tool bundle without a canonical registry and conformance/orphan check makes additions unpredictable and removals leave orphaned installs scattered across many surfaces; a registry plus conformance and orphan checks make add/remove predictable and complete.",
   "action": "strip_keep_claim",
   "target_note": "The 'ragged bundle matrix' and the specific incident (one removal touched 15+ surfaces, orphaned a user-scope install) are repo-specific; relocate the incident to the plan as motivation. Design claim is preserved in Part VI.",
   "restatement": "Without a canonical domain registry and a conformance/orphan check, bundle additions are unpredictable and a single removal can touch many surfaces and leave orphaned installs behind; a registry plus conformance and orphan checks make add and remove operations predictable and complete.",
   "preserved": true,
   "loss": "No net loss \u2014 this is the best-covered item. The design claim (a canonical registry + conformance + orphan check makes add unpredictable\u2192predictable and removal complete, across all install scopes) is carried by Part VI \u00a720\u2013\u00a722 (registry as source of truth, the conformance/orphan check, registry-driven teardown). The stripped incident is in the body verbatim and in far richer detail: \u00a76 line 143 gives the full gitnexus story (\u201815+ surfaces by hand\u2019, the user-scope `~/.claude/skills/` + hook orphan that resurfaced every session), and the \u2018ragged bundle\u2019 characterization is at line 127. The plan also reproduces it as a regression guard (implementation-plan.md:66, 232 \u2014 teardown dry-run \u2018reconstructs the gitnexus removal\u2019). The Appendix row is a one-line summary of body content; stripping it loses nothing."
  },
  {
   "locator": "Appendix A, line 950 (row: Knowledge governance)",
   "excerpt": "| Knowledge governance: index + supersession + freshness/conflict (VII) | ADR sprawl; stale/conflicting memory | 68 ADRs + ~1,470 cross-references (approx., by grep) + no index + 2 live-but-superseded records; memory has no freshness/conflict field or check; multi-level memory is additive (\"no override, no de-duplication\") | Direct (counts approx.) |",
   "type": "repo_metric",
   "design_claim": "A large, densely cross-referenced body of durable decision records with no index does not scale; without a supersession lifecycle and a freshness/conflict check, superseded records stay live and additive multi-level memory accumulates stale or conflicting entries. Knowledge needs an index, a supersession lifecycle, and a freshness/conflict sentinel.",
   "action": "strip_keep_claim",
   "target_note": "The counts (68 ADRs, ~1,470 cross-references, 2 live-but-superseded records) are repo metrics; relocate to the plan as the brownfield knowledge-governance baseline. The 'no override, no de-duplication' additive-memory characterization is a repo fact about the platform's memory model \u2014 relocate to the plan too. Design claim is preserved in Part VII. (Note: the same counts also appear at line 642 in the body \u2014 out of this section's scope but flagged for the body-section pass.)",
   "restatement": "A large body of densely cross-referenced decision records with no index does not scale to retrieval; without a supersession lifecycle, superseded records stay live alongside current ones, and additive multi-level memory that neither overrides nor de-duplicates accumulates stale and conflicting entries. Durable knowledge therefore needs a generated index, a supersession lifecycle, and a freshness/conflict sentinel.",
   "preserved": true,
   "loss": "No net loss, with one caveat the disposition itself already flagged. Design claim (a large densely-linked decision store with no index does not scale; without supersession + freshness/conflict checks, superseded records stay live and additive memory accumulates conflicts; knowledge needs index + supersession + sentinel) is carried by Part VII \u00a724 (lines 632\u2013640). Critically, the exact counts being \u2018stripped\u2019 from Appendix A are duplicated verbatim in the Part VII body at line 642 (\u201868 live ADRs with ~1,470 internal cross-references \u2026 no index \u2026 two live-but-Superseded records \u2026 additive \u2014 no override, no de-duplication\u2019) \u2014 and that body line literally reads \u2018(evidence: Appendix A)\u2019, so removing Appendix A would orphan that cross-reference. The disposition acknowledges this (\u2018the same counts also appear at line 642 \u2026 flagged for the body-section pass\u2019). So the information survives, but the work is not self-contained: this Appendix-only edit leaves a dangling \u2018evidence: Appendix A\u2019 pointer at line 642 that must be repaired in the same change or the document becomes internally inconsistent. The additive-memory \u2018no override, no de-duplication\u2019 platform fact also survives at line 642. Flag: fix the line-642 back-reference."
  },
  {
   "locator": "Appendix A, line 951 (row: Maintainability / tiering)",
   "excerpt": "| Maintainability / tiering (VIII) | Over/under-gating; meta-run fragility | Two runs are self-improving meta-runs; one was abandoned/split mid-pipeline | Direct |",
   "type": "repo_metric",
   "design_claim": "Gate coverage should be tiered by blast radius to avoid over- and under-gating; self-improving meta-runs (the pipeline operating on itself) are a fragile case that the design must accommodate, since such a run can be abandoned or split mid-pipeline.",
   "action": "strip_keep_claim",
   "target_note": "The observation (two runs are self-improving meta-runs; one was abandoned/split mid-pipeline) is repo-specific provenance; relocate to the plan as the maintainability motivation. Design claim is preserved in Part VIII.",
   "restatement": "Uniform gating either over-gates low-blast-radius work or under-gates high-blast-radius work; gate coverage must be tiered by blast radius, and the design must tolerate fragile cases such as self-improving meta-runs (the pipeline operating on itself), which may be abandoned or split mid-run.",
   "preserved": true,
   "loss": "No net loss. Design claim (uniform gating over/under-gates; tier gate coverage by blast radius; the design must tolerate self-improving meta-runs, which may be abandoned/split mid-run) is carried across Part VIII: tiering at \u00a729 line 791 and R10 (line 706); meta-run fragility at \u00a729 line 787 (\u2018The pipeline improves itself; pin the run and tier gates so over-tightened gates don\u2019t bite the meta-runs\u2019) and A24 (line 767). The stripped provenance is in the body: \u2018one run abandoned mid-pipeline / split into two runs\u2019 is in the per-run table at line 49. The single detail NOT found verbatim elsewhere is the precise count \u2018two runs are self-improving meta-runs\u2019 \u2014 the body discusses meta-runs as a category and names one abandoned/split run, but does not enumerate \u2018two\u2019. That specific count would be lost if it is not relocated to the plan as the disposition directs; it is low-value (the design claim does not depend on the exact number), but if precise relocation is the rule, this is the one count that genuinely only exists in this Appendix row."
  },
  {
   "locator": "Appendix B, row \"Domain kinds\" (line 966)",
   "excerpt": "Engineering-layer vs cross-cutting-domain | Force everything into the 9 layers | Two kinds in the registry \u2014 accepted; observability \u2248 MCP, not a new layer",
   "type": "repo_metric",
   "design_claim": "A registry of design domains should distinguish engineering layers from cross-cutting domains, rather than forcing every concern into the fixed layer taxonomy; a cross-cutting concern resembles an existing tool/integration domain rather than warranting a brand-new layer.",
   "action": "strip_keep_claim",
   "restatement": "Domain kinds: model both engineering layers and cross-cutting domains in the registry, rather than forcing every concern into the fixed layer taxonomy \u2014 accepted because a cross-cutting concern (e.g. observability) resembles an existing tool/integration domain rather than a new layer.",
   "preserved": true,
   "loss": "No loss, with one note. This item is a sub-fragment of the same row already covered by the full-row disposition; its restatement generalizes \"MCP\" to \"an existing integration/tooling domain\" and drops the concrete example \"observability\". On its own the example would be lost, but the full-row disposition for line 966 retains \"observability\" as the illustration, so the example survives in the principles doc. The abstract design claim (classify a cross-cutting concern by analogy to an existing integration/tooling domain rather than promoting it to a first-class layer) is fully preserved. Caveat: the two dispositions must be applied together \u2014 if only this fragment-level one were applied, the observability example would vanish."
  },
  {
   "locator": "Appendix B, row \"Domain kinds\" (line 966)",
   "excerpt": "observability \u2248 MCP, not a new layer",
   "type": "code_citation",
   "design_claim": "Some cross-cutting concerns are best classified as resembling an existing integration/tooling domain rather than as a new first-class layer.",
   "action": "strip_keep_claim",
   "restatement": "A cross-cutting concern is classified by analogy to an existing integration/tooling domain rather than promoted to a new first-class layer.",
   "preserved": true,
   "loss": "No loss, with one note. This item is a sub-fragment of the same row already covered by the full-row disposition; its restatement generalizes \"MCP\" to \"an existing integration/tooling domain\" and drops the concrete example \"observability\". On its own the example would be lost, but the full-row disposition for line 966 retains \"observability\" as the illustration, so the example survives in the principles doc. The abstract design claim (classify a cross-cutting concern by analogy to an existing integration/tooling domain rather than promoting it to a first-class layer) is fully preserved. Caveat: the two dispositions must be applied together \u2014 if only this fragment-level one were applied, the observability example would vanish."
  },
  {
   "locator": "Appendix B, row \"Pattern home\" (line 967)",
   "excerpt": "A dedicated discipline skill + code teeth | Fold into the primitive design KB | Routing-collision risk \u2014 mitigated by a sharp, bounded description",
   "type": "code_citation",
   "design_claim": "A cross-cutting discipline deserves its own dedicated knowledge/discipline module (with enforcement teeth) rather than being folded into a per-primitive design knowledge module; the cost is routing-collision risk between the two, mitigated by giving the new module a sharp, explicitly-bounded description.",
   "action": "strip_keep_claim",
   "restatement": "Pattern home: give a cross-cutting discipline its own dedicated knowledge module plus enforcement teeth, rather than folding it into a per-component design knowledge module \u2014 accepted; the routing-collision risk between the two modules is mitigated by a sharp, explicitly-bounded description.",
   "preserved": true,
   "loss": "No loss. All four elements of the trade-off survive: the chosen option (a dedicated discipline/knowledge module WITH enforcement teeth), the rejected option (folding into a per-component design knowledge module), the cost/risk (routing-collision between the two modules), and the mitigation (a sharp, explicitly-bounded description). The 'why' a reader needs \u2014 that giving a cross-cutting discipline its own home creates a routing ambiguity that must be bounded \u2014 is preserved. No repo-specific identifier was load-bearing here; the original row carried none, so the restatement is a faithful generalization."
  },
  {
   "locator": "Appendix B, row \"Staleness detection\" (line 958)",
   "excerpt": "Hash declared structured-boundary fields (RFC 8785) | Whole-file byte-hash / hash the output | Requires declaring the boundary per doc type; v1 whole-descendant invalidation \u2014 accepted because always-correct",
   "type": "code_citation",
   "design_claim": "Staleness should be detected by hashing declared structured-boundary fields using a canonicalization standard, not by whole-file byte-hashing or hashing the output; the cost is declaring the boundary per document type, with conservative whole-descendant invalidation in the first version.",
   "action": "strip_keep_claim",
   "restatement": "Staleness detection: hash declared structured-boundary fields using canonical-form normalization (e.g. RFC 8785 JSON canonicalization), rather than whole-file byte-hashing or hashing the output \u2014 accepted that each document type must declare its boundary, with conservative whole-descendant invalidation initially because it is always correct. (RFC 8785 is a public standard, not a repo specific; kept as an illustrative reference.)",
   "preserved": true,
   "loss": "No loss. The restatement preserves the chosen mechanism (hash declared structured-boundary fields under canonical-form normalization), the rejected alternatives (whole-file byte-hash and hash-the-output), and both accepted costs/rationales: that each doc type must declare its boundary, and that conservative whole-descendant invalidation is accepted because it is always-correct. The temporal qualifier \"v1\" is preserved as \"initially,\" keeping the signal that the conservative invalidation is a first-version posture, not permanent. RFC 8785 is correctly retained as a public standard (not a repo specific) with an explicit note, so the canonicalization contract \u2014 which is itself design-load-bearing \u2014 is not weakened to a vague 'normalize somehow'."
  },
  {
   "locator": "Appendix B, row \"Observability export\" (line 961)",
   "excerpt": "JSONL of record + export to a single-container self-hosted backend | A heavy multi-service stack, or no export | One local container; experimental semconv \u2014 accepted; pipeline never depends on the backend",
   "type": "code_citation",
   "design_claim": "Observability should be a durable record (append-only log) plus optional export to a single lightweight self-hosted backend, rather than a heavy multi-service stack or no export at all; the pipeline must never take a hard dependency on the observability backend.",
   "action": "strip_keep_claim",
   "restatement": "Observability export: emit an append-only record and optionally export it to a single lightweight self-hosted backend, rather than standing up a heavy multi-service stack or exporting nothing \u2014 accepted that the export semantics may be experimental, and the pipeline never depends on the backend being present. (\"JSONL\" and \"semconv\" are generic format/standard references, not repo specifics.)",
   "preserved": true,
   "loss": "No loss. The restatement keeps the chosen shape (append-only record plus optional export to a single lightweight self-hosted backend), the rejected alternatives (a heavy multi-service stack, or no export), and \u2014 critically \u2014 the hard constraint that the pipeline never takes a dependency on the backend. The 'single-container' detail is faithfully generalized to 'single lightweight self-hosted backend' (singularity and lightness both retained). The 'experimental semconv' caveat survives as 'export semantics may be experimental,' and JSONL/semconv are correctly flagged as generic format/standard references rather than repo specifics. The decision-critical 'why' (observability must be non-blocking) is fully preserved."
  },
  {
   "locator": "Appendix C, D9 (line 984)",
   "excerpt": "Observability surface: append-only JSONL + projection, OTel-shaped (artifact lineage via the in-git freshness graph, not an ingested lineage backend \u2014 D-OBS-2)",
   "type": "code_citation",
   "design_claim": "The observability surface is an append-only event log plus a projection, shaped to the OpenTelemetry standard; artifact lineage is served by the in-git freshness graph rather than a separate ingested lineage backend.",
   "action": "strip_keep_claim",
   "restatement": "Observability is an append-only event stream plus a projection, shaped to the OpenTelemetry standard; artifact lineage is served by the in-version-control freshness graph, not a separate ingested lineage backend (D-OBS-2). The format choice (JSONL) is a plan-level detail; the principle is append-only, text-diffable events.",
   "preserved": true,
   "loss": "No loss. The restatement preserves the decision's three load-bearing parts: the surface is an append-only event log PLUS a projection; it is shaped to the OpenTelemetry standard; and artifact lineage is served by the freshness graph rather than a separate ingested lineage backend. The internal cross-reference D-OBS-2 is correctly retained (it is an intra-document decision id, not a codebase citation, so it stays per the strip rule). Demoting \"JSONL\" to a plan-level format detail while abstracting the principle to 'append-only, text-diffable events' is faithful \u2014 JSONL is line-oriented and diffable, so the abstraction enriches rather than loses, and the concrete format choice is exactly the kind of thing that belongs in the plan. Generalizing \"in-git\" to \"in-version-control\" is the intended de-repo-ing and changes no design meaning. The rejected alternative (an ingested lineage backend) and the rationale for rejecting it (lineage already lives in the freshness graph) both survive."
  },
  {
   "locator": "Appendix C, D13 (line 988)",
   "excerpt": "Full observability export to a self-hostable single-container OTel backend with a durable local store; no immutable/WORM store. The specific product is a **plan** choice (open to re-evaluation)",
   "type": "code_citation",
   "design_claim": "Observability exports in full to a self-hostable single-container OpenTelemetry backend with a durable local store and no immutable/WORM requirement; the specific product is a plan-level choice, not an architectural one.",
   "action": "strip_keep_claim",
   "restatement": "Observability exports in full to a self-hostable, single-container, OpenTelemetry-standard backend with a durable local store and no immutable/WORM requirement. The specific product is a plan-level choice, open to re-evaluation.",
   "preserved": true,
   "loss": "No loss. The D13 table entry names no specific product to begin with, so nothing repo-specific is actually stripped here \u2014 the restatement is near-identical to the original and keeps every design attribute (self-hostable, single-container, OTel-standard, durable local store, no immutable/WORM, plan-level/re-evaluable). The full rationale survives independently in the surviving body (\u00a715-\u00a718, esp. line 486 on durability and 'no WORM', line 487 on the export path) and in the technology boundary registry (TB11, line 1082: 'OTel-shaped, self-hosted, JSONL-of-record, no WORM requirement, never on the critical path'). The concrete product realization (GreptimeDB) already lives in the plan (implementation-plan.md lines 37, 41, 76), so the 'plan choice, open to re-evaluation' framing is backed by an actual plan home. Skeptic check passed: no rationale, constraint, or 'why' is dropped."
  },
  {
   "locator": "Appendix C, D14 (line 989)",
   "excerpt": "Hybrid orchestrator \u2014 static topology (`pipelines.yaml`) declarative, dynamic routing in code; realized on **Claude Code Dynamic Workflows** (the script is the deterministic dispatcher reading the manifest \u2014 D-ORCH-1)",
   "type": "code_citation",
   "design_claim": "The orchestrator is hybrid: static topology declared in a manifest, dynamic routing in code, with a single deterministic dispatcher that reads the manifest.",
   "action": "strip_keep_claim",
   "restatement": "The orchestrator is hybrid: a declarative static-topology manifest plus dynamic routing in code, realized on a chosen orchestration substrate; the dispatcher script reads the manifest and routes deterministically (D-ORCH-1). The named substrate and the manifest filename are plan-level choices.",
   "preserved": false,
   "loss": "Information distortion, not pure loss, but a reader-facing defect. The restatement asserts 'The named substrate and the manifest filename are plan-level choices.' That is true for the manifest filename, but FALSE for the substrate per the architecture's own surviving text. D-PF-1 (line 1003, NOT in this disposition set, so it survives verbatim) explicitly names the orchestration substrate as an ARCHITECTURALLY-SIGNIFICANT binding: 'Within Claude Code, Dynamic Workflows is the named orchestration-substrate binding (D-ORCH-1)' and 'a platform swap is an architecture-level change.' D-ORCH-1 (line 1008) and TB7 (line 1078) reinforce this: the role is agnostic, but the specific engine binding is architectural. Demoting the substrate to 'plan-level' at D14 therefore contradicts D-PF-1/D-ORCH-1/TB7 inside the same principles doc and erases the deliberate role-agnostic/binding-architectural distinction the architecture draws. Fix: split the claim \u2014 the static-topology manifest FILENAME and the routing-code mechanics are plan-level, but the chosen orchestration SUBSTRATE is an architecturally-significant binding (cross-ref D-PF-1), changeable only at the architecture level. Strip the literal name 'Claude Code Dynamic Workflows' and the literal 'pipelines.yaml' filename (relocate both to the plan, where they already appear \u2014 implementation-plan.md lines 191-195), but do NOT relabel the substrate's architectural status."
  },
  {
   "locator": "Appendix C, D15 (line 990)",
   "excerpt": "Strangler-Fig + parallel-run for the prose\u2192**per-segment workflow-script** migration",
   "type": "code_citation",
   "design_claim": "The migration from the legacy form to the new orchestration uses a Strangler-Fig pattern with a parallel-run safety net, cut over per gated segment.",
   "action": "strip_keep_claim",
   "restatement": "Migration from the legacy form to the new orchestration uses Strangler-Fig replacement with a parallel-run safety net, cut over one gated segment at a time.",
   "preserved": true,
   "loss": "No loss. The restatement keeps every design element: Strangler-Fig replacement, the parallel-run safety net, and per-gated-segment (one-at-a-time) cutover. The only stripped specificity is 'workflow-script', a substrate-flavored term, correctly generalized to 'new orchestration'. The 'why' of decomposing per gated segment (a human approval gate cannot live inside a single workflow run) is carried by surviving entries D-ORCH-1 (line 1008), D14, and the runtime row at line 848, so cutting it from D15 loses no rationale. The concrete migration mechanics (parallel-run diff harness, prose path stays until proven) already live in the plan (implementation-plan.md lines 52, 64, 78, 191, 289, 349). Skeptic check passed."
  },
  {
   "locator": "Appendix C, D18 (line 993)",
   "excerpt": "Provision the observability backend via Dockerfile install + opt-in start (not docker-compose, not postStart)",
   "type": "code_citation",
   "design_claim": "The observability backend is provisioned by image-build install with an opt-in start, not by a multi-container compose stack and not auto-started at environment-attach time.",
   "action": "strip_keep_claim",
   "restatement": "The observability backend is provisioned at image-build time and started opt-in, never as a multi-container stack and never auto-started on environment attach \u2014 keeping it off the critical path.",
   "preserved": true,
   "loss": "No loss, arguably a gain. Each stripped term is a faithful generalization: 'Dockerfile' to 'image-build time', 'docker-compose' to 'multi-container stack', 'postStart' to 'auto-started on environment attach'. The restatement also surfaces a rationale the bare original lacked ('keeping it off the critical path'), which is grounded in surviving TB3 (line 1074) and TB11 (line 1082). One minor watch-item, not a loss: the original's specific reason for avoiding postStart in the body (\u00a730 / line 813) is COST \u2014 'never on every session start ... to stay on the cheap default machine' \u2014 whereas the restatement frames it as critical-path; both are valid and the cost rationale survives in \u00a730, so no reader-needed 'why' is dropped. Concrete realization (Dockerfile install of GreptimeDB, scripts/obs-up.sh opt-in) lives in the plan (implementation-plan.md line 390). Skeptic check passed."
  },
  {
   "locator": "Appendix C, D-KN-2 (line 999)",
   "excerpt": "exposed through a **typed MCP query surface** (parameterized queries \u2014 *not* raw Cypher or NL-to-query)",
   "type": "code_citation",
   "design_claim": "The decision graph is queried through a typed, parameterized query surface, not raw graph-query-language or natural-language-to-query.",
   "action": "strip_keep_claim",
   "restatement": "The decision graph is exposed through a typed, parameterized query surface (a registered tool), not raw graph-query-language strings or natural-language-to-query.",
   "preserved": true,
   "loss": "No loss. The excerpt's whole content is the design claim: the decision-graph query surface is governed as a registered tool and a cross-cutting domain, analogized to observability. The restatement carries all three elements verbatim in substance (governed tool, cross-cutting domain, same-as-observability) and retains the D-TOOL-1/D-DOM-1 codes parenthetically as references. The 'why' (a query surface needs governance like any tool/domain) is the sentence itself and survives. The surrounding D-KN-2 rationale that a reader actually needs (authored-not-extracted edges, no graph DB, ~68 ADRs threshold, bi-temporal supersession) lives in the prose body at line 652, not in this excerpt, so it is not this disposition's burden to carry."
  },
  {
   "locator": "Appendix C, D-KN-2 (line 999)",
   "excerpt": "**no graph DB** (file/SQLite-derived from frontmatter; reach for a DB only past ~10\u2075 edges \u2014 we have ~68 ADRs)",
   "type": "repo_metric",
   "design_claim": "At the project's scale a graph database is unnecessary; the decision graph is derived from committed metadata into a file/embedded store, and a true graph DB is warranted only at a far larger edge count.",
   "action": "strip_keep_claim",
   "restatement": "No graph database is required: the graph is derived from committed decision metadata into a file or embedded store. A dedicated graph DB is warranted only past roughly 10^5 edges \u2014 far beyond a typical decision corpus, which numbers in the dozens.",
   "preserved": true,
   "loss": "No loss. The excerpt's whole content is the design claim: the decision-graph query surface is governed as a registered tool and a cross-cutting domain, analogized to observability. The restatement carries all three elements verbatim in substance (governed tool, cross-cutting domain, same-as-observability) and retains the D-TOOL-1/D-DOM-1 codes parenthetically as references. The 'why' (a query surface needs governance like any tool/domain) is the sentence itself and survives. The surrounding D-KN-2 rationale that a reader actually needs (authored-not-extracted edges, no graph DB, ~68 ADRs threshold, bi-temporal supersession) lives in the prose body at line 652, not in this excerpt, so it is not this disposition's burden to carry."
  },
  {
   "locator": "Appendix C, D-KN-2 (line 999)",
   "excerpt": "The decision-graph MCP server registers as a tool (D-TOOL-1) / cross-cutting domain (D-DOM-1), like observability.",
   "type": "code_citation",
   "design_claim": "The decision-graph query surface is governed as a registered tool and a cross-cutting domain, the same way the observability surface is.",
   "action": "strip_keep_claim",
   "restatement": "The decision-graph query surface registers as a governed tool (D-TOOL-1) and a cross-cutting domain (D-DOM-1), the same way the observability surface does.",
   "preserved": true,
   "loss": "No loss. The excerpt's whole content is the design claim: the decision-graph query surface is governed as a registered tool and a cross-cutting domain, analogized to observability. The restatement carries all three elements verbatim in substance (governed tool, cross-cutting domain, same-as-observability) and retains the D-TOOL-1/D-DOM-1 codes parenthetically as references. The 'why' (a query surface needs governance like any tool/domain) is the sentence itself and survives. The surrounding D-KN-2 rationale that a reader actually needs (authored-not-extracted edges, no graph DB, ~68 ADRs threshold, bi-temporal supersession) lives in the prose body at line 652, not in this excerpt, so it is not this disposition's burden to carry."
  },
  {
   "locator": "Appendix C, D-PF-1 (line 1003)",
   "excerpt": "Platform foundations are named **architecturally-significant bindings** (Claude Code \u00b7 GitHub Codespaces \u00b7 GitHub \u00b7 VSCode \u00b7 GitHub Actions) per the Appendix-F litmus",
   "type": "code_citation",
   "design_claim": "The platform foundations the system runs on are architecturally-significant bindings (a swap is an architecture-level change), even though the role each fills is technology-agnostic.",
   "action": "strip_keep_claim",
   "restatement": "Platform foundations (the agent runtime, the cloud dev-environment, the source host, the editor, the CI system) are named architecturally-significant bindings per the Appendix-F litmus: each role is technology-agnostic, but the chosen binding is structural, so a platform swap is an architecture-level change.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix C, D-PF-1 (line 1003)",
   "excerpt": "Within Claude Code, **Dynamic Workflows is the named orchestration-substrate binding** (D-ORCH-1).",
   "type": "code_citation",
   "design_claim": "The orchestration substrate is itself a named architecturally-significant binding within the chosen agent platform.",
   "action": "strip_keep_claim",
   "restatement": "Within the chosen agent platform, the orchestration substrate is itself a named architecturally-significant binding (D-ORCH-1).",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix C, D-OBS-2 (line 1005)",
   "excerpt": "The observability backend ingests **OpenTelemetry only**; artifact provenance/lineage is served by the freshness gate's in-git `derived_from` graph (Part IV), **not** an ingested OpenLineage backend \u2014 no second service for lineage already held in git (TB2). OpenLineage's Job\u2192Run\u2192Dataset vocabulary is borrowed to shape that view.",
   "type": "code_citation",
   "design_claim": "The observability backend ingests only the OpenTelemetry standard; artifact lineage is served by the in-version-control derived-from graph rather than a second ingested-lineage service, borrowing the lineage standard's Job/Run/Dataset vocabulary only to shape the view.",
   "action": "strip_keep_claim",
   "restatement": "The observability backend ingests the OpenTelemetry standard only; artifact provenance/lineage is served by the freshness gate's in-version-control derived-from graph (Part IV), not a second ingested-lineage service for data already held in version control (TB2). The lineage standard's Job/Run/Dataset vocabulary is borrowed only to shape that view.",
   "preserved": true,
   "loss": "No loss. This excerpt carries a real rationale, not just a citation: the reason for ingesting OpenTelemetry only and NOT running an OpenLineage backend is 'no second service for lineage already held in git (TB2)' \u2014 a cost-avoidance constraint. The restatement preserves that 'why' fully ('not a second ingested-lineage service for data already held in version control (TB2)'), keeps the TB2 boundary reference, and keeps the scoping constraint that the lineage vocabulary is borrowed ONLY to shape the view (not to stand up a service). The fuller version of the same rationale at line 437 ('it would cost a second container for provenance we already hold in git') corroborates and is reachable. TB2 is defined at line 1073."
  },
  {
   "locator": "Appendix C, D-DR-1 (line 1007)",
   "excerpt": "actors with external side effects carry coordinator-derived **cycle-scoped, per-side-effect** idempotency keys (`{run}:{stage}:{cycle}`) backed by a **durable effect-committed record**",
   "type": "code_citation",
   "design_claim": "Actors with external side effects carry coordinator-derived, cycle-scoped, per-side-effect idempotency keys backed by a durable effect-committed record checked on replay.",
   "action": "strip_keep_claim",
   "restatement": "Actors with external side effects carry coordinator-derived, cycle-scoped, per-side-effect idempotency keys (keyed by run, stage, and cycle) backed by a durable effect-committed record checked on replay; real receivers may ignore caller-supplied keys, so the record is authoritative.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix C, D-DR-1 (line 1007)",
   "excerpt": "**Confirmed against the orchestration substrate:** Dynamic Workflows' resume is *session-local* and does NOT survive process restart / Codespace rebuild, so the run-event log is the cross-restart recovery layer \u2014 engine + log compose (D-ORCH-1).",
   "type": "adr_as_evidence",
   "design_claim": "The orchestration engine's built-in resume is session-local and does not survive process restart or environment rebuild, so a durable event log must be the cross-restart recovery layer; engine and log compose.",
   "action": "strip_keep_claim",
   "restatement": "The orchestration engine's built-in resume is session-local and does not survive a process restart or environment rebuild, so the durable run-event log is the cross-restart recovery layer \u2014 engine and log compose (D-ORCH-1). The provenance of this finding (a substrate-capability check) relocates to the plan/evidence file.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix C, D-ORCH-1 (line 1008)",
   "excerpt": "The orchestration substrate is **Claude Code Dynamic Workflows** \u2014 the workflow script is the sole deterministic dispatcher (TB7) that reads `pipelines.yaml` (D2) and routes on typed state (D14)",
   "type": "code_citation",
   "design_claim": "The orchestration substrate provides a single deterministic dispatcher that reads the topology manifest and routes on typed state; actors hand off by file path and never spawn actors (one-level nesting).",
   "action": "strip_keep_claim",
   "restatement": "The chosen orchestration substrate provides the sole deterministic dispatcher (TB7) that reads the topology manifest (D2) and routes on typed state (D14); actors hand off by file path (TB6) and never spawn actors (one-level nesting). The named substrate and manifest filename are plan-level choices.",
   "preserved": true,
   "loss": "This is the strongest disposition of the set. It preserves the full causal chain: the WHY ('cannot hold a human approval gate mid-run') \u2192 the WHAT (one workflow per human-gated segment) \u2192 the HOW (thin coordinator persists state in the durable event log). Critically, it RECOVERS the maturity rationale that the sibling line-1008 disposition dropped: 'Because the substrate is token-heavy and early-maturity, segments are kept bounded and the durable event log is the always-available record.' This restates the original's terse 'Research-preview + token-heavy => bounded segments, JSONL as the always-available record' as an explicit cause->effect, demoting only the substrate's named maturity status to plan/evidence. The reader keeps every constraint and every reason. No loss."
  },
  {
   "locator": "Appendix C, D-ORCH-1 (line 1008)",
   "excerpt": "Because a workflow **cannot hold a human approval gate mid-run**, the pipeline decomposes into **one workflow per human-gated segment**, sequenced by a thin coordinator that persists state in the run-event log (D-DR-1). ... Research-preview + token-heavy \u21d2 bounded segments, JSONL as the always-available record.",
   "type": "code_citation",
   "design_claim": "Because the orchestration unit cannot hold a human approval gate mid-run, the pipeline decomposes into one workflow per human-gated segment, sequenced by a thin coordinator that persists state in the durable event log; the engine gives in-session orchestration only and durability/recovery is the log.",
   "action": "strip_keep_claim",
   "restatement": "Because an orchestration unit cannot hold a human approval gate mid-run, the pipeline decomposes into one workflow per human-gated segment, sequenced by a thin coordinator that persists state in the durable event log (D-DR-1). The engine gives in-session orchestration only; durability and recovery come from the log. Because the substrate is token-heavy and early-maturity, segments are kept bounded and the durable event log is the always-available record. The substrate's maturity status is a plan-level/evidence detail.",
   "preserved": true,
   "loss": "This is the strongest disposition of the set. It preserves the full causal chain: the WHY ('cannot hold a human approval gate mid-run') \u2192 the WHAT (one workflow per human-gated segment) \u2192 the HOW (thin coordinator persists state in the durable event log). Critically, it RECOVERS the maturity rationale that the sibling line-1008 disposition dropped: 'Because the substrate is token-heavy and early-maturity, segments are kept bounded and the durable event log is the always-available record.' This restates the original's terse 'Research-preview + token-heavy => bounded segments, JSONL as the always-available record' as an explicit cause->effect, demoting only the substrate's named maturity status to plan/evidence. The reader keeps every constraint and every reason. No loss."
  },
  {
   "locator": "Appendix C, D-KN-5 (line 1012)",
   "excerpt": "KB load + *consultation* are observable per run via **namespaced run-event attributes** (`proj.skill.declared/loaded/used`) since OTel GenAI doesn't cover which-skill-loaded",
   "type": "code_citation",
   "design_claim": "Knowledge load and consultation are made observable per run via project-namespaced event attributes, because the OpenTelemetry GenAI conventions do not cover which knowledge artifact was loaded or used; this lets dead grants be pruned and missing-but-needed knowledge surface.",
   "action": "strip_keep_claim",
   "restatement": "Knowledge-skill load and consultation are observable per run via project-namespaced run-event attributes (declared/loaded/used), since the OpenTelemetry GenAI conventions do not cover which-knowledge-artifact-loaded \u2014 so dead grants prune and a missing-but-needed knowledge artifact surfaces. The concrete attribute names are an implementation detail.",
   "preserved": true,
   "loss": "The design claim and its full rationale survive. The restatement keeps the WHY ('the OTel GenAI conventions do not cover which-knowledge-artifact-loaded'), the WHAT (load AND consultation observable per run \u2014 both verbs preserved, which matters because 'declared/loaded/used' is a three-state lifecycle, not a boolean), and the consequence ('dead grants prune and a missing-but-needed knowledge artifact surfaces'). Only the concrete attribute namespace 'proj.skill.declared/loaded/used' is demoted to implementation detail, which is correct \u2014 the abstract claim is 'project-namespaced attributes carrying declared/loaded/used', and the restatement preserves the three-state semantics in plain words. The R28 cross-reference ('closes the asymmetry where tools were governed and knowledge was not') is dropped, but that is a within-doc traceability code, not external design rationale, and the asymmetry point is itself an internal-consistency note rather than a constraint a reader needs to act on. No substantive loss."
  },
  {
   "locator": "Appendix C, D-FM-1 (line 1013)",
   "excerpt": "**Rules + anti-patterns carry a failure-modes catalog** (`.claude/canonical/failure-modes.yaml`): each rule/anti-pattern maps to its failure modes (software guidewords + misuse-cases)",
   "type": "code_citation",
   "design_claim": "Each rule and anti-pattern carries an associated failure-modes catalog mapping it to its failure modes (guidewords plus misuse-cases), each with what-good-looks-like and a concrete detection mechanism \u2014 bridging from desired behavior to how the bad case is caught.",
   "action": "strip_keep_claim",
   "restatement": "Rules and anti-patterns carry a failure-modes catalog: each maps to its failure modes (software guidewords plus misuse-cases), each with what-good-looks-like and a concrete detection mechanism \u2014 the bridge from what good looks like to how the bad is caught. The catalog's file location is an implementation detail.",
   "preserved": true,
   "loss": "The design claim survives. The restatement keeps the substance \u2014 a coverage scan against an established concern taxonomy keeps the rule set COMPLETE, not merely internally consistent \u2014 and abstracts the named taxonomies correctly ('an architecture-documentation framework plus a software-quality model, plus project-specific concerns' for arc42 + ISO 25010 + project concerns). One mild risk: arc42 and ISO 25010 are recognized industry standards, not repo-specific citations, so naming them is arguably design information (a reader implementing the scan benefits from knowing WHICH taxonomies). The restatement generalizes them to descriptions rather than names. This is a minor loss of specificity, but the disposition explicitly routes the named workflow and the concrete taxonomy names to the plan/implementation layer, so the standards are not destroyed \u2014 they are relocated. The core claim (taxonomy-driven completeness, not just consistency) is fully preserved; treat the standard-name abstraction as acceptable demotion rather than loss."
  },
  {
   "locator": "Appendix C, D-FM-1 (line 1013)",
   "excerpt": "a coverage scan against a concern taxonomy (arc42 + ISO 25010 + project concerns) keeps the rule set *complete*, not just internally consistent (the `failure-mode-catalog` completeness review)",
   "type": "named_run",
   "design_claim": "A coverage scan of the rule set against a concern taxonomy (recognized architecture and quality-attribute taxonomies plus project-specific concerns) keeps the rule set complete, not merely internally consistent.",
   "action": "strip_keep_claim",
   "restatement": "A coverage scan against an established concern taxonomy (an architecture-documentation framework plus a software-quality model, plus project-specific concerns) keeps the rule set complete, not merely internally consistent. The named completeness-review workflow is a plan-level/implementation reference.",
   "preserved": true,
   "loss": "The design claim survives. The restatement keeps the substance \u2014 a coverage scan against an established concern taxonomy keeps the rule set COMPLETE, not merely internally consistent \u2014 and abstracts the named taxonomies correctly ('an architecture-documentation framework plus a software-quality model, plus project-specific concerns' for arc42 + ISO 25010 + project concerns). One mild risk: arc42 and ISO 25010 are recognized industry standards, not repo-specific citations, so naming them is arguably design information (a reader implementing the scan benefits from knowing WHICH taxonomies). The restatement generalizes them to descriptions rather than names. This is a minor loss of specificity, but the disposition explicitly routes the named workflow and the concrete taxonomy names to the plan/implementation layer, so the standards are not destroyed \u2014 they are relocated. The core claim (taxonomy-driven completeness, not just consistency) is fully preserved; treat the standard-name abstraction as acceptable demotion rather than loss."
  },
  {
   "locator": "Appendix C, D-IL-1 (line 1010)",
   "excerpt": "Flashlight, not autopilot \u2014 no auto-promotion.",
   "type": "worked_example",
   "design_claim": "The telemetry-to-improvement loop only proposes changes and requires human approval before promotion; it never auto-promotes.",
   "action": "strip_keep_claim",
   "restatement": "The telemetry-to-improvement loop is mandatory but human-gated: it curates evals, computes gate-ROI, clusters failures, and proposes changes; a human approves before promotion and changes validate on an offline corpus. It illuminates, it does not act autonomously \u2014 no auto-promotion. (No repo specifics; the line stays as written.)",
   "preserved": true,
   "loss": "No loss \u2014 and this disposition is correctly scoped as the lightest touch. 'Flashlight, not autopilot \u2014 no auto-promotion' contains zero repo-references; it is a pure design principle (the loop proposes, a human promotes, it never acts autonomously). The restatement keeps the entire mechanism (curates evals, computes gate-ROI, clusters failures, proposes changes; human approves before promotion; changes validate on an offline corpus) plus the metaphor and the hard constraint 'no auto-promotion'. The disposition's own note 'the line stays as written' is accurate \u2014 there is nothing to strip here, so applying it cannot lose anything."
  },
  {
   "locator": "Appendix D, line 1017 (header note)",
   "excerpt": "Sources were gathered via web search; dates are publisher-stated where visible and labelled \"foundational\" otherwise. Verify links and dates directly before any ADR cites them \u2014 search output is input to verify, not ground truth.",
   "type": "adr_as_evidence",
   "design_claim": "Cited sources are provisional and must be independently verified before being relied upon; search output is an input to verification, not ground truth.",
   "action": "strip_keep_claim",
   "restatement": "Sources backing a principles document should be treated as provisional inputs to verification, not as ground truth; verify links and dates before any decision record relies on them.",
   "preserved": true,
   "loss": "None material. The abstract claim \u2014 cited sources are provisional inputs to verification, not ground truth; verify links/dates before any decision record relies on them \u2014 survives intact in the restatement. One placement nuance, not a loss of the claim: this note is a 'verify before relying' warning that governs the entire appendix's citation lists. Since those lists relocate to architecture-evidence.md, the warning should travel into that file too (in context with the citations a reader will actually consult), not only survive as an abstract sentence in the principles doc. The design claim itself is fully preserved."
  },
  {
   "locator": "Appendix D, line 1019 (verification-pass note)",
   "excerpt": "Verification pass (adversarial citation check). 10 of 13 recent/dated citations verified. Three were corrected here: (1) a \"context-contamination / clean-restart\" arXiv paper (2605.08563) could not be verified and is withdrawn ... it was likely an LLM-fabricated reference. ... (2) the \"OpenLineage AgentRunFacet RFC\" does not exist ... corrected below. (3) the OTel GenAI-observability post is real but supports only the span-tree model, not the \"eval scores attachable\" half ...; scoped below.",
   "type": "named_run",
   "design_claim": "Citations supporting an architecture must survive an adversarial verification pass; unverifiable or fabricated references are withdrawn, and design decisions that lose a source must still stand on an independently-verified basis. The clean-context re-derivation remains sound because branch-isolation evidence supports it independently.",
   "action": "relocate_to_evidence_file",
   "target_note": "The narrative of this specific verification run (the 10-of-13 count, the three named corrections, the withdrawn arXiv 2605.08563, the non-existent OpenLineage RFC, the scoped OTel post) is repo-run provenance \u2014 it documents the actual citation-vetting that was performed. Move the full verification-pass record to architecture-evidence.md. In the principles doc, keep only the abstract claim: citations must pass adversarial verification, and a decision must rest on independently-verified support rather than any single citation. Note the surviving internal cross-references (\u00a713 move 5 / D7) belong with the design claim, not the evidence file.",
   "preserved": true,
   "loss": "None. This is the highest-risk item and the disposition handles it correctly. The load-bearing 'why' here is not the run narrative (10-of-13, the fabricated arXiv 2605.08563, the non-existent OpenLineage RFC) \u2014 that is genuine repo-run provenance and rightly moves to the evidence file. The load-bearing rationale is that Adopted decision D7 (clean-context re-derivation, line 982) does NOT depend on the withdrawn fabricated paper: it stands independently on the verified Execution Lineage branch-isolation result. The disposition explicitly keeps this in the design_claim AND keeps the \u00a713-move-5 / D7 internal cross-reference with the design claim rather than burying it in the evidence file. A reader of D7 still learns why the decision survives the withdrawn citation. No rationale or constraint is lost."
  },
  {
   "locator": "Appendix D, line 1021 (Freshness / lineage / invalidation cluster)",
   "excerpt": "Execution Lineage (arXiv 2605.06365, 2026-05-07); AI21 caching in agentic pipelines (2026-05-13); Restate updating agents (2026-03-11); the stale-world-model framing (2026-04-10); dbt `state:modified+` (2026-05); Dagster asset versioning (foundational); Bazel Skyframe (foundational); Salsa red-green / early cutoff (foundational); write-audit-publish (dbt Labs, 2024-12).",
   "type": "named_run",
   "design_claim": "Freshness, lineage tracking, and selective invalidation in a derived-artifact pipeline are grounded in established prior art: lineage-based recomputation, content/version-addressed invalidation (red-green / early-cutoff), selective rebuild of modified-plus-downstream, and write-audit-publish staging.",
   "action": "relocate_to_evidence_file",
   "target_note": "The design claim (freshness/lineage/invalidation has established prior art) is timeless and stays as an abstract sentence in the principles doc. The specific source list with arXiv IDs and publisher dates is bibliographic provenance \u2014 move the citation list to architecture-evidence.md keyed to the freshness/invalidation design point. Strip dates and IDs from the principles doc.",
   "preserved": true,
   "loss": "None. The design_claim names every mechanism (lineage-based recomputation, content/version-addressed red-green/early-cutoff invalidation, modified-plus-downstream selective rebuild, write-audit-publish staging), and each already carries its full rationale in the body of Part IV \u2014 write-audit-publish gating and early-cutoff scope at lines 392-393, the detect-and-hard-gate vs early-cutoff trade-off and the spike-gated frontier at lines 427-429. The appendix line is pure bibliography (arXiv IDs, publisher dates, product names). Relocating it to the evidence file loses no 'why' a reader needs; the design reasoning lives in Part IV regardless."
  },
  {
   "locator": "Appendix D, line 1023 (Observability cluster)",
   "excerpt": "OpenTelemetry GenAI semantic conventions (foundational; status: experimental); OTel GenAI agent spans (foundational); OTel GenAI observability (2026-05-14 \u2014 supports the span-tree model; eval-score attachment is a separate OTel artifact, `gen_ai.evaluation.result`); OpenLineage run cycle (foundational; the agent-facet work lives in OTel's GenAI SIG, not an OpenLineage \"AgentRunFacet\"); dbt `run_results.json` (2026-05); Dagster asset health (foundational); Braintrust observability-vs-evals (2026-02-09); Monte Carlo five pillars (foundational); Temporal event history (foundational); Microsoft + Cisco multi-agent OTel conventions (2025-10 / 2026-03).",
   "type": "named_run",
   "design_claim": "Pipeline observability should follow an open, standards-shaped model: a span-tree execution model, separation of observability telemetry from evaluation scores, run-cycle lineage events, and the established data-quality pillars \u2014 grounded in widely-adopted observability conventions and prior art.",
   "action": "relocate_to_evidence_file",
   "target_note": "Keep the abstract observability design claim in the principles doc as one sentence. Move the dated/named source list (OTel conventions, OpenLineage, dbt run_results.json, Braintrust, Monte Carlo, Temporal, vendor conventions) plus the date-stamped corrections to architecture-evidence.md keyed to the observability design point.",
   "preserved": true,
   "loss": "None. The design_claim preserves the span-tree execution model, the telemetry-vs-evaluation-score separation, run-cycle lineage, and the data-quality pillars. The substantive correction embedded in the citation (lineage lives in git, not an ingested OpenLineage backend; eval scores are a separate OTel artifact) is already a first-class design decision in body \u00a717 \u2014 see line 444 ('backend ingests OpenTelemetry only ... there is no OpenLineage backend') and D-OBS-2 / D9 at line 984. The dated source list and the date-stamped corrections are provenance only. Nothing load-bearing is lost by relocation."
  },
  {
   "locator": "Appendix D, line 1025 (Extensibility / plugin-registry cluster)",
   "excerpt": "the Open-Closed Principle and microkernel/plug-in architecture (foundational); VS Code contribution points and Eclipse extension registry (foundational); Terraform provider lockfile (2025-11); Kubernetes finalizers + owner-references (2025); OpenTelemetry Collector component model (2026); Backstage software catalog + templates (foundational); Open Plugin Specification v1.0.0 (2026-04); AI-agent plugin/extension architecture surveys (2026-02).",
   "type": "named_run",
   "design_claim": "Extensibility via a plugin/registry pattern is grounded in established architecture: the Open-Closed Principle, microkernel/plug-in designs, contribution-point/extension-registry models, locked dependency manifests, and ownership/finalizer lifecycle \u2014 new capability is added without modifying the core.",
   "action": "relocate_to_evidence_file",
   "target_note": "Keep the abstract extensibility claim (open-closed plugin/registry pattern with established prior art) in the principles doc. Move the named-product source list (VS Code, Eclipse, Terraform lockfile, Kubernetes, OTel Collector, Backstage, Open Plugin Spec) with versions/dates to architecture-evidence.md keyed to the extensibility design point.",
   "preserved": true,
   "loss": "None. The abstract claim \u2014 extensibility via an open-closed plugin/registry pattern grounded in established prior art (microkernel, contribution-point/extension-registry, locked dependency manifests, ownership/finalizer lifecycle), adding capability without modifying the core \u2014 is fully carried in the design_claim, and the mechanism itself lives in Part VI (the domain-pack pattern). The named-product list with versions/dates (VS Code, Eclipse, Terraform lockfile, Kubernetes, OTel Collector, Backstage, Open Plugin Spec) is bibliographic provenance. No constraint or rationale is lost."
  },
  {
   "locator": "Appendix D, line 1027 (Durable-knowledge governance, Part VII)",
   "excerpt": "Nygard + Fowler ADR (supersede-never-edit; foundational + 2026); MADR (foundational); adr-log / Log4brains / Backstage ADR plugin (foundational; generated index/site); decision-graph tools (2026); Zep/Graphiti bi-temporal validity + edge invalidation (2025-01); mem0 / Letta / LangMem memory lifecycle (2025\u20132026); the AGENTS.md standard + Claude memory docs (foundational; ~200-line budget, additive precedence); Chroma \"context rot\" (2025-07).",
   "type": "named_run",
   "design_claim": "Durable-knowledge governance (Part VII) rests on established prior art: append-only/supersede-never-edit decision records, generated decision indexes, decision-graph linkage, bi-temporal validity with edge invalidation, memory-lifecycle management, and a bounded context budget to avoid context rot.",
   "action": "relocate_to_evidence_file",
   "target_note": "Keep the abstract durable-knowledge-governance claim and its internal Part VII cross-reference in the principles doc. Move the named source list (Nygard/Fowler, MADR, adr-log/Log4brains/Backstage, Zep/Graphiti, mem0/Letta/LangMem, AGENTS.md, Chroma) and the repo-specific '~200-line budget, additive precedence' detail to architecture-evidence.md. The ~200-line memory budget is a Claude-Code-platform-specific number; treat it as provenance, not a timeless principle.",
   "preserved": true,
   "loss": "None \u2014 and the one detail the disposition itself flags as a risk is independently safe. The disposition correctly treats the '~200-line budget, additive precedence' as Claude-Code-platform provenance rather than a timeless principle, and proposes relocating it. Verified that this is NOT a loss: body line 672 already states the ~200-line budget, the additive/concatenate-and-arbitrate precedence model, the 'a rule lives at exactly one level' constraint, path-scoped .claude/rules/ loading, and the 'context rot' rationale \u2014 all in Part VII proper. The bi-temporal-validity / supersede-not-overwrite / invalidate-not-delete mechanisms likewise live in the body (lines 639, 670, 714 R18). The appendix citation list is pure bibliography. Relocating it loses nothing a reader needs; every 'why' is in Part VII."
  },
  {
   "locator": "Appendix D, line 1029 (LLM-as-judge reliability, \u00a79 reviewer gate)",
   "excerpt": "LLM-as-judge reliability (\u00a79 reviewer gate) \u2014 verified pass. Binary > ordinal for gate reliability (RAND + multiple, verified); style-bias-now-dominant / position-bias-negligible-on-frontier-judges (verified, with one persistence counter-signal); single-judge fragility incl. the ~28%-flip case (verified); \"Judging the Judges: bias-mitigation strategies\" (arXiv 2604.23178, verified ...); Prior Prejudice (ACL 2026 Findings, verified for the belief-bias / CoT-can-hurt point only); diverse-cross-provider-panel > single (verified). Excluded as unverified: the \"Prior Prejudice/Soumik\" CoT-figure attribution (misattributed), and the Future-AGI calibration numbers ... not load-bearing here.",
   "type": "named_run",
   "design_claim": "An LLM-as-judge reviewer gate (\u00a79) should use binary rather than ordinal verdicts for reliability, prefer a diverse cross-provider panel over a single judge (single-judge verdicts are fragile and can flip), and account for style bias; chain-of-thought can sometimes hurt via belief bias.",
   "action": "relocate_to_evidence_file",
   "target_note": "Keep the abstract reviewer-gate design claims (binary>ordinal, panel>single-judge, single-judge fragility, style-bias awareness, CoT-can-hurt) plus the \u00a79 internal cross-reference in the principles doc. Move the verified/excluded citation ledger \u2014 arXiv 2604.23178, the ~28%-flip figure, the misattributed CoT figure, the Future-AGI calibration exclusion \u2014 to architecture-evidence.md keyed to the reviewer-gate design point.",
   "preserved": true,
   "loss": "No design loss. This entry is a pure citation ledger (a 'verified pass' source list with verified/excluded sub-lists). Every abstract design claim it cites is already stated, with its 'why', in the document body: \u00a79 'Hardening the reviewer gate' (lines 337-344) gives binary>ordinal ('reliability degrades monotonically as scoring moves from binary to multi-level ordinal'), single-judge fragility incl. the ~28%-flip figure (line 337, restated at 346), style-bias-now-dominant / position-bias-negligible, the diverse cross-provider quorum, and CoT-can-hurt-on-belief-laden-judgment (line 341). Rule R20 (line 716), anti-pattern A20 (line 763), and decision D-RG-1 (line 1006) restate the same. The disposition correctly keeps the abstract claims plus the \u00a79 pointer and moves only the arXiv 2604.23178 reference, the misattributed CoT figure, and the Future-AGI calibration exclusion to the evidence file. Those are provenance, not rationale. Caveat (not a loss): the target evidence file architecture-evidence.md does not yet exist; relocate_to_evidence_file must create it, otherwise the ledger is dropped rather than relocated."
  },
  {
   "locator": "Appendix D, line 1031 (Durable execution / operational recovery, \u00a718)",
   "excerpt": "Durable execution / operational recovery (\u00a718) \u2014 verified pass. Temporal Event History (append-only log = recovery + audit; plaintext/codec + PII caveat; event-history limits) (verified); Restate journal / event-sourcing (verified); LangGraph persistence/checkpointing + the pre-interrupt re-execution footgun, persistent-checkpointer requirement (verified); AWS idempotency-key-generated-in-step (verified). Excluded as unverified: a Temporal `temporal-replayer` k8s init-container deploy-gate (refuted ...), a TS `replayWorkflowHistory()` method name (SDK conflation), and a tiered read-vs-write retry rule .... The \"industry consensus\" label is partly vendor-blog-anchored (medium); the mechanisms are corroborated by primary docs.",
   "type": "named_run",
   "design_claim": "Durable execution and operational recovery (\u00a718) rest on established mechanisms: an append-only event history serving both recovery and audit (with a plaintext/PII caveat), journal/event-sourcing replay, persistent checkpointing (and the re-execution-before-interrupt footgun it must avoid), and idempotency keys generated inside the step.",
   "action": "relocate_to_evidence_file",
   "target_note": "Keep the abstract durable-execution design claims plus the \u00a718 internal cross-reference in the principles doc. Move the verified/excluded citation ledger (Temporal Event History, Restate, LangGraph checkpointing, AWS idempotency, the refuted temporal-replayer pattern, the replayWorkflowHistory SDK conflation, the retry-rule exclusion, the vendor-blog-anchored confidence note) to architecture-evidence.md keyed to the durable-execution design point.",
   "preserved": true,
   "loss": "No design loss. Pure citation ledger. Every mechanism it cites is fully restated with rationale in \u00a718 'Operational recovery' (lines 493-499): append-only log as recovery+audit journal with atomic append (495); the plaintext/PII confidentiality caveat ('entries are plaintext unless encrypted, keep credentials/PII out', 495 + TB10); event-history limits as 'the log is bounded (compact/rotate)' (495); idempotency keys generated inside the step / by the coordinator from durable run-state, never model-minted (496, R21); and the persistent-checkpointer requirement ('Requires a persistent checkpoint; an in-memory one loses the approval on eviction', 497, 800). The LangGraph 're-execution-before-interrupt footgun' survives as the design lesson it teaches \u2014 'replay what already happened rather than re-running it' and the worst-moment crash-testing of the inverse window (493, 499). R21 (717), A21 (764), D-DR-1 (1007) carry the same. The relocated items (Temporal/Restate/LangGraph/AWS sources, the refuted temporal-replayer pattern, the replayWorkflowHistory SDK conflation, the retry-rule exclusion, the vendor-blog confidence note) are provenance and negative-evidence hygiene, not constraints a principles reader needs. Same caveat: the evidence file must actually be created."
  },
  {
   "locator": "Appendix D, line 1033 (Architecture-diagramming vocabulary, for Appendix E)",
   "excerpt": "Architecture-diagramming vocabulary (for Appendix E): the C4 model (c4model.com; foundational); arc42 runtime/building-block views (foundational); Mermaid diagram-type coverage (v11.x, 2026); flowchart-vs-DFD-vs-decision-tree distinctions (2026).",
   "type": "named_run",
   "design_claim": "The diagram-type vocabulary used by the document (Appendix E) is grounded in established architecture-diagramming models: C4 for context/container views, arc42 runtime and building-block views, and the distinctions between flowcharts, data-flow diagrams, and decision trees.",
   "action": "strip_keep_claim",
   "restatement": "Diagram-type choices throughout the document (see Appendix E) draw on established architecture-diagramming vocabulary \u2014 C4 context/container views, arc42 runtime and building-block views, and the distinctions among flowcharts, data-flow diagrams, and decision trees. (Tool-version and date specifics such as the Mermaid version belong in the evidence file, not the principles doc.)",
   "preserved": true,
   "loss": "Minimal, acceptable loss. The abstract claim (diagram vocabulary grounded in C4 context/container views, arc42 runtime/building-block views, and flowchart-vs-DFD-vs-decision-tree distinctions) is preserved verbatim in the restatement and still backs Appendix E. The only dropped specifics are tool-version/date trivia (Mermaid v11.x, '2026' date stamps) which carry no rationale or constraint. One inconsistency to flag: the restatement says version specifics 'belong in the evidence file' but the action is strip_keep_claim with no relocate target recorded, so the Mermaid-version detail is in practice dropped, not relocated. Since it is non-load-bearing trivia this is not a meaningful information loss, but the disposition's own stated intent (move to evidence file) is not actually executed by a strip action."
  },
  {
   "locator": "Appendix E, line 1037 (intro paragraph)",
   "excerpt": "All are Mermaid; the C4 views (V3, V4) use Mermaid's experimental C4 support and may be regenerated via Structurizr/PlantUML if C4 rigor grows. V2 is rendered as the coverage table in \u00a73.",
   "type": "code_citation",
   "design_claim": "Diagrams are authored in a text-based, version-controllable, widely-renderable diagram format; C4-style views are a higher-rigor option that can be regenerated from the same node/edge set if needed.",
   "action": "strip_keep_claim",
   "restatement": "All figures are authored in a text-based, diffable, widely-renderable diagram format; the system-context and container views follow the C4 convention and can be regenerated in a more rigorous C4 toolchain from the same node/edge set if C4 fidelity becomes important. (The \u00a73 cross-reference for the coverage matrix is internal structure and is retained.)",
   "target_note": "Retain the abstract format/rigor claim and the \u00a73 internal cross-reference; drop the named tool stack (Mermaid / Structurizr / PlantUML) and the 'experimental C4 support' tool detail, which bind the doc to a specific rendering toolchain.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix E, line 1037 (intro paragraph)",
   "excerpt": "Diagram-type choices follow the architecture-diagramming research (Appendix D).",
   "type": "evidence_map",
   "design_claim": "Each diagram's type (flowchart, sequence, state machine, C4, etc.) is chosen deliberately against an established diagramming-vocabulary rationale rather than ad hoc.",
   "action": "strip_keep_claim",
   "restatement": "Diagram-type choices follow a deliberate, documented diagramming-vocabulary rationale (see Appendix D).",
   "target_note": "This is an intra-document cross-reference to Appendix D (an internal appendix, not a codebase artifact), so the \u00a7/Appendix pointer is retained per the KEEP rule; no repo specifics are present. Entry recorded only to confirm it was reviewed and deliberately kept.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix E, line 1037 (intro paragraph)",
   "excerpt": "Figure numbers reflect authoring order, not document order \u2014 read them by section using the table below (the \"Part\" column is the reading sequence).",
   "type": "evidence_map",
   "design_claim": "The figure catalog is indexed by intra-document reading sequence (Part/section), letting a reader navigate the visuals in document order independent of their authoring numbering.",
   "action": "strip_keep_claim",
   "restatement": "Keep verbatim \u2014 the Figure IDs (Vn), Part column, and \u00a7N section pointers are intra-document structure, not codebase references, and are explicitly in the KEEP set.",
   "target_note": "No repo specifics. The entire V1\u2013V18 catalog table (lines 1041\u20131058), its Part/\u00a7 locators, and the per-figure titles are internal cross-references and are retained unchanged.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix E, lines 1053\u20131054 (catalog rows V13, V14)",
   "excerpt": "| V13 | Worked example \u2014 before | sequence | X \u00a733 |  /  | V14 | Worked example \u2014 after | sequence (mirror of V13) | X \u00a733 |",
   "type": "worked_example",
   "design_claim": "A paired before/after worked example is part of the visual set, demonstrating the architecture's effect by contrast.",
   "action": "strip_keep_claim",
   "restatement": "Keep the catalog rows as-is in the principles doc IF the \u00a733 worked example is itself made repo-agnostic; the catalog titles ('Worked example \u2014 before/after') and the \u00a733 pointer carry no repo specifics. If the \u00a733 worked example is instead judged to narrate a specific real run and is relocated to the plan, these two catalog rows relocate with it.",
   "target_note": "Disposition is contingent on the separate treatment of \u00a733 (X \u00a733), which is out of this section's scope. Within Appendix E the rows are generic; flagged so the \u00a733 decision is mirrored here and the catalog does not end up pointing at relocated content.",
   "preserved": true,
   "loss": "No information lost by the catalog-row treatment itself. The rows ('Worked example \u2014 before/after', pointer 'X \u00a733') carry zero repo specifics \u2014 they are titles and an intra-document section pointer. The design claim (a paired before/after example demonstrates the architecture by contrast) is inherent in the titles and survives. CAVEAT the disposition itself flags and I confirm: the rows only stay correct if they keep pointing at live content. The \u00a733 target they reference (lines 857\u2013902) is NOT repo-agnostic \u2014 it narrates one specific real run ('In one run...', 'misses 13/14, phantom ref', blueprint v3\u2192v4, AC-FR-1..14) and is the same run booked in the \u00a734 metrics table (the 'execution-pipeline-design' 7-audit/3-recon row). So \u00a733 is real-run evidence that belongs in the plan/evidence file, not the principles doc. If \u00a733 is relocated and these two rows are left behind pointing at it, the catalog dangles. The disposition correctly makes V13/V14 contingent on the \u00a733 decision and says they relocate with it \u2014 apply that contingency and nothing is lost; apply the rows in isolation and you risk a dangling pointer. Net: preserved, with the stated contingency mandatory not optional."
  },
  {
   "locator": "Appendix F, TB1 row \u2014 \"Evidenced in\" cell",
   "excerpt": "\u00a730; `devcontainer.json`; Codespaces machine-types docs",
   "type": "evidenced_in_column",
   "design_claim": "The single-container dev-environment boundary is grounded in the dev-environment design section and the platform's container-config and machine-type facts.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the dev-environment design section (\u00a730) for the durability/footprint rationale.",
   "target_note": "Keep only the intra-document \u00a730 cross-reference; drop the `devcontainer.json` file citation and the vendor machine-types-docs citation.",
   "preserved": true,
   "loss": "No design information lost. The cell is a pure-citation column. The \u00a730 intra-document cross-reference is kept (and \u00a730 holds the durability/footprint rationale). The two dropped items are genuine citations with no standalone design content: `devcontainer.json` names a config file (the boundary 'single container, Dockerfile + Features' is already stated in the boundary body and in \u00a730), and 'Codespaces machine-types docs' is an external vendor reference that grounds the tier facts \u2014 those facts live in \u00a730 line 813 and are themselves being handled by separate dispositions. Reader needs nothing from these two citations that \u00a730 does not supply."
  },
  {
   "locator": "Appendix F, TB1 row \u2014 boundary body, machine spec",
   "excerpt": "Default machine **4-core / 16 GB / 32 GB** (the smallest tier meeting the 4-core `hostRequirements` minimum; the `8 GB` floor is a loose minimum, not the provisioned size). Footprint is a **tunable cost dial** \u2014 resizable to 64\u2013128 GB at proportional $/hr.",
   "type": "repo_metric",
   "design_claim": "A single-container dev environment's footprint is an architecturally-tunable cost dial: a default tier is provisioned, but it can be scaled up within the platform's tier ceiling when justified and cost-flagged.",
   "action": "strip_keep_claim",
   "restatement": "Footprint is a tunable cost dial: a default machine tier is provisioned, and it may be resized upward within the platform's available tiers when the larger size is justified and the cost is flagged.",
   "target_note": "Drop the concrete tier numbers (4-core / 16 GB / 32 GB, 8 GB, 64\u2013128 GB), the `hostRequirements` config-key name, and the $/hr framing; the abstract cost-dial claim survives.",
   "preserved": false,
   "loss": "MILD loss, and it depends on whether \u00a730 is also being stripped. The abstract cost-dial claim survives cleanly. But three pieces of design-relevant 'why' are at risk if they vanish from the principles doc: (1) the rationale that the 8 GB hostRequirements value is a LOOSE MINIMUM not the provisioned size \u2014 this corrects a real misreading (someone could otherwise provision an 8 GB box and break the 4-core floor); the bare phrase 'a default tier is provisioned' does not carry that gotcha. (2) the constraint that the default is 'the smallest tier MEETING THE 4-CORE minimum' \u2014 the 4-core floor is an architectural requirement, not a cost preference, and the restatement drops the reason a specific tier is the floor. (3) the 'proportional $/hr' framing names HOW the cost dial costs (linear, not stepped) \u2014 'cost is flagged' loses the proportionality. Note these same details are duplicated in \u00a730 line 813, so if \u00a730 stays intact the document does not lose them outright; but Appendix F is the binding registry a plan-author reads to choose a machine, and the 4-core-floor + loose-8GB-minimum rationale is exactly the kind of constraint a plan-author needs at the point of choice. Recommend the restatement retain the abstracted rationale ('default = smallest tier meeting the platform's minimum core count; provisioned size exceeds the platform's loose memory floor; cost scales proportionally with size') even while dropping the literal numbers."
  },
  {
   "locator": "Appendix F, TB1 row \u2014 \"Rules out / rules in\" cell, size clause",
   "excerpt": "OUT (size): a footprint past the largest tier (128 GB) \u00b7 IN: single-container tools; a heavier one is in-bounds if the larger machine is justified + cost-flagged",
   "type": "repo_metric",
   "design_claim": "A tool whose footprint exceeds the platform's largest available machine tier is out of bounds; an otherwise heavy single-container tool is in bounds when the larger machine is justified and cost-flagged.",
   "action": "strip_keep_claim",
   "restatement": "OUT (size): a footprint exceeding the largest available machine tier \u00b7 IN: single-container tools; a heavier one is in-bounds if the larger machine is justified and cost-flagged.",
   "target_note": "Replace the literal \"128 GB\" with \"the largest available machine tier\"; the rest of the clause is already tech-agnostic.",
   "preserved": true,
   "loss": "No design information lost. Replacing the literal '128 GB' with 'the largest available machine tier' is a faithful abstraction of the exact same claim: a footprint past the platform ceiling is OUT. The number 128 was only the concrete instantiation of 'the ceiling'; the constraint is 'exceeds the ceiling', which the restatement states directly. The 'justified + cost-flagged' escape hatch and the single-container IN clause are kept verbatim. A reader loses no rationale \u2014 if anything the abstracted form is more durable, since the tier ceiling is a moving vendor number. (Minor consistency note, not a loss: '128 GB' also appears in the same row's body via the '64\u2013128 GB' range handled by the prior disposition; abstract both together so the row does not name a number in one cell and abstract it in another.)"
  },
  {
   "locator": "Appendix F, TB2 row \u2014 \"Evidenced in\" cell",
   "excerpt": "\u00a730 durability table; `.gitignore`",
   "type": "evidenced_in_column",
   "design_claim": "Git-as-system-of-record (durable state is committed; the runtime dir is ephemeral) is grounded in the dev-environment durability model.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the durability table in the dev-environment design section (\u00a730).",
   "target_note": "Keep the \u00a730 durability-table cross-reference; drop the `.gitignore` file citation.",
   "preserved": true,
   "loss": "No design information lost. Pure-citation column. The \u00a730 durability-table cross-reference is kept, and that table (lines 806\u2013811) is where the durable-vs-ephemeral rationale actually lives. The dropped `.gitignore` is a bare file citation grounding 'the runtime dir is ephemeral'; that fact is already stated in the boundary body ('the runtime dir is ephemeral') and shown in the \u00a730 durability table ('Run-event JSONL (gitignored runtime dir) \u2014 ephemeral'). The reader needs nothing from `.gitignore` that the kept \u00a730 reference does not supply."
  },
  {
   "locator": "Appendix F, TB3 row \u2014 \"Evidenced in\" cell",
   "excerpt": "\u00a718 (\"never depends on the backend being up\")",
   "type": "evidenced_in_column",
   "design_claim": "No-mandatory-runtime-dependency-on-an-external-service (the pipeline completes with backends down) is grounded in the observability/operations design section.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the observability design section (\u00a718), which establishes that the pipeline never depends on a backend being up.",
   "target_note": "Keep the \u00a718 cross-reference; the parenthetical quote is intra-document, retain or paraphrase \u2014 no repo file specifics here.",
   "preserved": true,
   "loss": "No design information lost. Pure-citation column with a single intra-document reference (\u00a718) and a parenthetical quote that is itself from \u00a718 \u2014 no repo file paths, no vendor citation, nothing to strip. The disposition keeps \u00a718 and retains/paraphrases the quote. The 'why' (the pipeline completes with backends down) is preserved in both the boundary body and \u00a718 (verified at line 487: 'The pipeline never depends on the backend being up'). This is the lowest-risk item of the set \u2014 arguably nothing is being stripped at all, since there was no repo-specific citation here to begin with; the action is effectively a no-op that retains everything."
  },
  {
   "locator": "Appendix F, TB4 row \u2014 \"Evidenced in\" cell",
   "excerpt": "\u00a75, \u00a78 (the litmus)",
   "type": "evidenced_in_column",
   "design_claim": "Deterministic-gates-only (code enforces checkable rules; LLM judgment is reserved for reviewer agents) is grounded in the gate-design and litmus sections.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the gate-design sections (\u00a75, \u00a78) and their deterministic-vs-judgment litmus.",
   "target_note": "Pure intra-document cross-refs (\u00a75, \u00a78) \u2014 KEEP as-is; no repo specifics to strip.",
   "preserved": true,
   "loss": "No loss. The disposition is a no-op KEEP: \u00a75 (\"The principle\", line 167) and \u00a78 (\"The gates and the manifest keystone\", line 266) are pure intra-document cross-refs with no repo specifics to strip, and the deterministic-vs-judgment litmus they point to is real (line 360: \u201ccan a deterministic function decide pass/fail exactly?\u201d). The abstract design claim (deterministic code enforces checkable rules; LLM judgment reserved for reviewer agents) is fully carried by \u00a75/\u00a78. Verified accurate."
  },
  {
   "locator": "Appendix F, TB5 row \u2014 boundary body + \"Evidenced in\" cell",
   "excerpt": "tools and validators read rules from canonical (CANON-1) ... \u00a76; `canonical.py`; CANON-1",
   "type": "code_citation",
   "design_claim": "Single-source-of-truth (no embedded rule copies; tools and validators read vocabularies/schemas from one canonical source) is grounded in the substrate design and its canonical-source decision.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the substrate design section (\u00a76) and the canonical-source decision (CANON-1).",
   "target_note": "Keep the \u00a76 and CANON-1 intra-document cross-refs; drop the `canonical.py` file citation. CANON-1 is a document decision id, not a codebase file, so it stays.",
   "preserved": true,
   "loss": "Design claim survives: \u00a76 substrate table (line 171, rows 1-2) fully states the single-canonical-source rule and that validators read from canonical and never hardcode, naming canonical.py in its own \u201cRealized as\u201d cell \u2014 so dropping the canonical.py citation from TB5 loses nothing not already in \u00a76. Minor caveat: CANON-1 is retained but is NEVER defined anywhere in the architecture doc (it appears only inside TB5's own row); its actual definition is a CI fitness check living in implementation-plan.md (\u201cCANON-1 stays green\u201d, \u201cCANON-1 in CI\u201d). The disposition's rationale (\u201ca document decision id, not a codebase file\u201d) is shaky \u2014 it is a plan-level CI check id, not a self-defined architecture decision. Keeping a bare, undefined acronym in the principles doc leaves a dangling reference for a first-time reader. Recommend either defining CANON-1 in the principles doc or relocating its grounding to the plan/evidence file rather than leaving the orphan label."
  },
  {
   "locator": "Appendix F, TB6 row \u2014 \"Evidenced in\" cell",
   "excerpt": "\u00a710",
   "type": "evidenced_in_column",
   "design_claim": "File-path-reference handoff with stateless actors (inter-stage state passes as file paths) is grounded in the orchestration design section.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the orchestration design section (\u00a710).",
   "target_note": "Pure intra-document cross-ref \u2014 KEEP as-is.",
   "preserved": true,
   "loss": "No loss. Pure intra-document cross-ref, correctly kept. \u00a710 (\"Primitive role assignment\", line 348) contains the file-path-handoff / stateless-actor rationale at line 360 (\u201ccommunicate by file-path reference, which is what makes them stateless and re-runnable\u201d). Abstract claim fully grounded. Verified accurate."
  },
  {
   "locator": "Appendix F, TB7 row \u2014 boundary body + \"Evidenced in\" cell",
   "excerpt": "OUT: orchestration tech that breaks sole-dispatch (ADR-0045) ... \u00a710",
   "type": "adr_as_evidence",
   "design_claim": "Single-dispatcher topology (no agent-spawns-agent) is a binding boundary: orchestration must preserve a sole-dispatcher arrangement.",
   "action": "strip_keep_claim",
   "restatement": "OUT: orchestration tech that breaks the sole-dispatcher topology (the prohibition on agents spawning agents). Grounding: see the orchestration design section (\u00a710).",
   "target_note": "Strip the \"(ADR-0045)\" citation, which asserts a repo decision as evidence; keep the \u00a710 cross-ref and the abstract sole-dispatch claim.",
   "preserved": true,
   "loss": "Design claim and rationale survive: \u00a710 (line 360) states the sole-dispatcher rule abstractly and names the enforcement (\u201cforbids actor-spawns-actor (one-level nesting), so a gate can always be interposed\u201d), and D-ORCH-1 (line 1008) carries the same. The disposition mischaracterizes ADR-0045 as merely \u201ca repo decision asserted as evidence\u201d \u2014 it is actually the concrete enforcement mechanism (\u201cSub-agents MUST NOT declare Agent in their tools: frontmatter\u201d, grounded in documented platform constraint T-001). Stripping the (ADR-0045) citation loses a traceability pointer to HOW the topology is mechanically enforced, but NOT the WHY or the design claim, both of which live in \u00a710. Acceptable strip; the lost item is an enforcement-traceability link, not a rationale or constraint a reader needs to understand the boundary."
  },
  {
   "locator": "Appendix F, TB8 row \u2014 boundary body + \"Evidenced in\" cell",
   "excerpt": "validators are Python 3.x invoked via `python3` ... \u00a76; `auditing-shared`",
   "type": "code_citation",
   "design_claim": "Validators are a subprocess-dispatched library written in the container's first-class scripting runtime \u2014 the boundary rules out validators needing a runtime not present in the container.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the substrate design section (\u00a76), which establishes the subprocess-dispatched validation library.",
   "target_note": "Keep \u00a76; drop the `auditing-shared` skill citation. Note: TB8 itself names Python/`python3` in the boundary statement \u2014 a borderline vendor-specific binding. Flag for the editor, but it is the boundary's own wording (a named runtime binding), not an evidence citation; this entry covers only the evidence-column citation.",
   "preserved": true,
   "loss": "Design claim survives: \u00a76 row 2 (line 176) describes the validation library as \u201cPure deterministic checkers that read rules from canonical... dispatched by subprocess,\u201d realized as auditing-shared/scripts/ + canonical.py \u2014 so dropping the auditing-shared skill citation loses nothing not already in \u00a76. The disposition is honest about scope and correctly flags (but does not resolve) the real issue: the boundary BODY itself names \u201cPython 3.x invoked via python3,\u201d a vendor/runtime-specific binding that arguably violates the \u201cnames no vendor\u201d boundary discipline stated in Appendix F's own preamble (line 1064). That naming is out of scope for this evidence-column-only item and is flagged for the editor, which is the correct disposition. No information lost by this specific strip."
  },
  {
   "locator": "Appendix F, TB9 row \u2014 \"Evidenced in\" cell",
   "excerpt": "artifact templates; run-events; Appendix E",
   "type": "evidenced_in_column",
   "design_claim": "Text-first, diffable formats (Markdown+YAML, JSON/JSON-Schema, JSONL, Mermaid) are grounded in the artifact-format conventions and the format appendix.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the artifact-format conventions and the formats appendix (Appendix E).",
   "target_note": "Keep the Appendix E intra-document cross-ref; drop the repo-specific \"artifact templates\" and \"run-events\" file/artifact citations (replace with the generic \"artifact-format conventions\").",
   "preserved": true,
   "loss": "Design claim survives in the boundary body itself, which already enumerates all four formats (Markdown+YAML, JSON/JSON-Schema, JSONL, Mermaid) \u2014 so the claim does not depend on the evidence cell. BUT the restatement is inaccurate: it calls Appendix E \u201cthe formats appendix,\u201d when Appendix E is the \u201cVisual index\u201d (line 1035) \u2014 a diagram catalogue that grounds ONLY the \u201cMermaid for diagrams\u201d quarter of the claim. The grounding for the NON-diagram formats (Markdown/YAML/JSON/JSONL) came precisely from the stripped \u201cartifact templates\u201d and \u201crun-events\u201d pointers. After the strip, the only surviving cross-ref (Appendix E) does not actually evidence three of the four format families. Net: the design claim is not lost (it's in the row text), but the disposition's restatement overstates what Appendix E covers and silently drops the evidence for Markdown/YAML/JSON/JSONL without relocating it. Recommend the restatement point to the actual artifact-format conventions section (or land the artifact-template/run-event evidence in the plan/evidence file) rather than implying Appendix E covers all formats."
  },
  {
   "locator": "Appendix F, TB10 row \u2014 boundary body + \"Evidenced in\" cell",
   "excerpt": "secrets via env-block / Codespaces Secrets ... \u00a730; ADR-0039 (MCP discipline)",
   "type": "adr_as_evidence",
   "design_claim": "Credential indirection only: secrets pass via an environment-injection mechanism, never inline in config argv, URLs, or committed files.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the dev-environment design section (\u00a730) for the credential-indirection rule.",
   "target_note": "Strip \"ADR-0039 (MCP discipline)\" \u2014 an ADR-number citation asserting a repo decision; keep \u00a730. The boundary body's \"Codespaces Secrets\" names a vendor mechanism \u2014 generalize to \"a platform secrets store\" alongside the env-block; this entry flags it though it is in the boundary wording, not the evidence column.",
   "preserved": true,
   "loss": "None. The design claim (credential indirection only; never inline in argv/URL/committed files) lives in the kept boundary-cell prose and is enforced by the plan's '.mcp.json argv/URL' fitness function (implementation-plan.md:128). The kept \u00a730 grounding (line 813) carries the env-block + secrets-store mechanism. 'ADR-0039 (MCP discipline)' is a pure repo-decision-number citation; its only unique signal \u2014 that the rule applies to MCP config \u2014 is already carried by the generic 'config argv, URL' wording plus the plan's MCP-specific fitness function. Generalizing 'Codespaces Secrets' to 'a platform secrets store' alongside the env-block keeps the mechanism, not a vendor lock-in. No rationale, constraint, or why is dropped."
  },
  {
   "locator": "Appendix F, TB11 row \u2014 \"Evidenced in\" cell",
   "excerpt": "\u00a715\u2013\u00a718",
   "type": "evidenced_in_column",
   "design_claim": "The observability boundary (OTel-shaped, self-hosted single-container, JSONL-of-record, no WORM, lineage via the in-git derived_from graph rather than an ingested backend) is grounded in the observability design sections.",
   "action": "strip_keep_claim",
   "restatement": "Grounding: see the observability design sections (\u00a715\u2013\u00a718).",
   "target_note": "Pure intra-document cross-ref range \u2014 KEEP as-is. Note: TB11 references the freshness gate's in-git `derived_from` graph (Part IV) and D-OBS-2 \u2014 both intra-document, KEEP.",
   "preserved": true,
   "loss": "None. Pure intra-document cross-ref kept as-is. The full design claim (OTel-shaped, self-hosted single-container, JSONL-of-record, no WORM, lineage via the in-git derived_from graph not an ingested backend) survives in the kept boundary cell and is independently grounded in-document at lines 437/442/444 (Part V observability) and D9/D-OBS-2 (lines 984/1005). Nothing is stripped."
  },
  {
   "locator": "Appendix F, closing paragraph (line 1084)",
   "excerpt": "These boundaries were previously implicit and scattered \u2014 across \u00a730 (dev-environment), \u00a76 (the substrate), the MCP discipline, and individual ADRs. This registry consolidates them as the single binding contract the plan checks every technology choice against (see the plan's \u00a71 and WS-0 fitness functions).",
   "type": "worked_example",
   "design_claim": "The boundary registry consolidates constraints that were previously implicit and scattered across multiple design sections and decisions into a single binding contract that the plan's setup work checks every technology choice against.",
   "action": "strip_keep_claim",
   "restatement": "These boundaries were previously implicit and scattered across several design sections and individual decisions. This registry consolidates them as the single binding contract that the plan checks every technology choice against (see the plan's \u00a71 and its initial fitness-function work).",
   "target_note": "Keep the abstract \"previously scattered, now consolidated\" framing and the \u00a730/\u00a76/plan-\u00a71/WS-0 intra-document cross-refs; drop \"the MCP discipline\" as a repo-specific provenance pointer (generalize to \"individual decisions\"). The provenance detail of *exactly* which scattered sources these came from is repo-specific motivation \u2014 relocate that finer-grained provenance to the plan if the plan wants the brownfield migration story.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix F, closing paragraph (line 1084)",
   "excerpt": "New boundaries are added here the same way decisions are added to Appendix C: state the constraint tech-agnostically, give the litmus that makes it architectural, and let a fitness function enforce it.",
   "type": "worked_example",
   "design_claim": "New boundaries are added by the same procedure as architectural decisions: state the constraint tech-agnostically, give the litmus that makes it architectural, and let a fitness function enforce it.",
   "action": "strip_keep_claim",
   "restatement": "Keep verbatim \u2014 already tech-agnostic; the Appendix C reference is an intra-document cross-reference.",
   "target_note": "No repo specifics. The Appendix C cross-ref is intra-document \u2014 KEEP. No change needed.",
   "preserved": true,
   "loss": ""
  },
  {
   "locator": "Appendix F, table header (line 1070)",
   "excerpt": "| # | Boundary (tech-agnostic constraint) | Rules **out** / rules **in** | Evidenced in |",
   "type": "evidence_map",
   "design_claim": "The boundary registry presents each constraint with the technologies it rules out/in; the per-row provenance pointer is the evidence-map artifact.",
   "action": "strip_keep_claim",
   "restatement": "Drop the \"Evidenced in\" column entirely from the principles table, retaining only \"# | Boundary | Rules out / Rules in\".",
   "target_note": "Removing the column is the structural change that the per-row strip_keep_claim entries above implement. Per-row groundings worth keeping (the intra-document \u00a7N pointers) can fold into the boundary cell prose; the repo-file citations are discarded as covered above.",
   "preserved": true,
   "loss": ""
  }
 ],
 "lossy": [
  {
   "locator": "\u00a71 Executive summary, line 17",
   "excerpt": "By audit-finding category, roughly three-quarters of findings fall in this mechanical class (a categorization estimate, not a measured rework-time share \u2014 see Appendix A).",
   "type": "repo_metric",
   "design_claim": "In an unguarded probabilistic pipeline, the large majority of defects are cheap, mechanical ones (missing sections, broken cross-refs, stale predecessors) that a deterministic check could catch, rather than substantive design flaws.",
   "action": "strip_keep_claim",
   "restatement": "The large majority of defects in an unguarded pipeline are cheap, mechanical ones \u2014 missing sections, broken cross-references, stale predecessors \u2014 exactly the class a deterministic check can catch, rather than substantive design flaws.",
   "target_note": "The 'see Appendix A' pointer and the 'three-quarters' figure are repo-specific; the design claim (mechanical defects dominate) is timeless.",
   "preserved": false,
   "loss": "The restatement keeps the qualitative claim (mechanical defects dominate) but silently DROPS the epistemic hedge that is the load-bearing part of this sentence. The original is careful to say 'roughly three-quarters ... a categorization estimate, not a measured rework-time share.' That parenthetical is not a citation \u2014 it is a rationale telling the reader how much to trust the claim and on what basis (category mapping, not measured time). The restatement asserts 'the large majority' as flat fact with no calibration. Worse, the RULE requires repo-specific evidence worth keeping to land in the plan or an evidence file; the 125-of-165 figure and the 'see Appendix A' provenance currently live ONLY in this doc (verified: grep finds them nowhere in implementation-plan.md). The disposition specifies no landing target, so the quantitative evidence and its strength-of-evidence caveat vanish entirely rather than relocating. A reader of the principles doc loses the 'how confident, and why' that the original deliberately supplied. To preserve: keep the 'categorization estimate, not measured rework-time' caveat in the restated claim, and relocate the 125/165 + Appendix A evidence to the plan or an evidence file."
  },
  {
   "locator": "\u00a72 Problem A, line 28",
   "excerpt": "The four lightest documents (Research Plan, Acceptance Tests, Phase Validators, `tasks.json`) have no machine-readable contract or no wired validator.",
   "type": "repo_metric",
   "design_claim": "The lightest-weight artifacts in a pipeline tend to lack a machine-readable contract or a wired validator, leaving them unguarded.",
   "action": "strip_keep_claim",
   "restatement": "The lightest-weight artifacts in a pipeline tend to lack a machine-readable contract or a wired validator, leaving them unguarded.",
   "target_note": "Named document types and the count are repo-specific; the general claim is that light artifacts are under-contracted.",
   "preserved": false,
   "loss": "The named documents are NOT pure decoration here \u2014 they ARE the evidence for the claim, and they encode a non-obvious 'why': these four are the LIGHTEST artifacts, and the claim is precisely that lightness correlates with being under-contracted. Dropping the names leaves 'lightest-weight artifacts tend to lack a contract' as an unfalsifiable generalization with its supporting instances removed. Appendix A independently relies on these names ('Contracts for ATs / Phase Validators / Research Plan / tasks.json ... Template-less artifacts'), so stripping line 28 without relocating the list creates a dangling reference in the evidence map. The disposition names no landing target, so the concrete instances vanish. To preserve: relocate the named-document list (which artifacts lacked a contract/validator, and that they were the lightest) to the plan or evidence file so Appendix A still resolves and the claim keeps its proof."
  },
  {
   "locator": "\u00a72 'Evidence \u2014 prior runs' heading + line 36",
   "excerpt": "### Evidence \u2014 prior runs\\n\\nAcross 14 feature runs, eleven reached the audit stages and eight required at least one reconciliation cycle.",
   "type": "named_run",
   "design_claim": "Empirically, most runs that reach the audit stages require at least one reconciliation cycle \u2014 i.e., late-caught defects forcing rework are the norm, not the exception.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "The whole 'Evidence \u2014 prior runs' subsection (the prose count of 14/11/8 runs) is repo provenance motivating the design. Move to the PLAN as brownfield motivation. The abstract claim (most runs need reconciliation; late defects are the norm) is already carried by the \u00a72 bullets, so the principles doc loses nothing by relocating the evidence.",
   "preserved": false,
   "loss": "The disposition's premise is wrong: the abstract claim is NOT already carried by the \u00a72 bullets. The empirical frequency claim \u2014 'most runs that reach the audit stages require at least one reconciliation cycle; late-caught defects forcing rework are the NORM, not the exception' \u2014 appears ONLY at line 36. The \u00a72 bullets (lines 25-32) state seam-level structural facts (ungated seams, light docs, triage queueing, lineage, measurability); none assert that reconciliation is the dominant empirical outcome. The \u00a71 summary (line 17) asserts the mechanism (defects caught late trigger rework) and the category mix (~three-quarters mechanical), but not the run-level frequency. A grep for 'norm/exception/most runs' finds the claim nowhere else in principles-doc-bound prose. FIX: change the disposition from a bare relocate_to_plan with empty restatement to relocate_to_plan PLUS an abstract restatement left in the principles doc, e.g. 'Empirically, most runs that reach the audit stages require at least one reconciliation cycle \u2014 late-caught defects forcing expensive rework are the norm, not the exception.' Relocating the 14/11/8 counts to the plan is correct; dropping the abstract frequency claim is the loss."
  },
  {
   "locator": "\u00a72 contract-layer table, lines 101-111",
   "excerpt": "| Doc type | Template? | Schema? | Frontmatter spec? | Cross-artifact contract? | Authoring agent integration | Validator wired in? | ... Intent Clarification | yes, v1.0.0 ... | ADR | yes, v1.0.0 | placement + prescription validators ... | Acceptance Tests | none ... | tasks.json | different KB, inline literal ...",
   "type": "worked_example",
   "design_claim": "Contract completeness varies sharply by document type: heavier, earlier documents tend to have templates, schemas, and wired validators, while lighter or later artifacts have none \u2014 coverage correlates with a document's perceived weight, not its risk.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Full per-doc-type matrix with repo version strings (v1.0.0), named validators, and named doc types. Move to the PLAN as the concrete contract-coverage audit. Abstract claim (coverage is asymmetric and uncorrelated with risk) survives via line 99 restatement and the \u00a72 drift list.",
   "preserved": false,
   "loss": "This is the one item that loses an idea. The disposition's OWN design_claim contains a sharp analytical insight \u2014 'coverage correlates with a document's perceived weight, not its risk' \u2014 i.e. the pipeline gates documents by how heavy they feel, not by how much damage they cause, which is backwards. I searched the whole architecture doc: that weight-vs-risk inversion appears NOWHERE abstractly. \u00a72 line 28 only states the flat fact that 'the four lightest documents have no machine-readable contract' \u2014 it never makes the point that lightness is a bad proxy for risk. The proposed survivor (line 99 restatement) keeps only 'asymmetric... some barely specified', which is strictly weaker: asymmetry is a description; 'weight not risk' is the diagnosis. Relocating the matrix to the plan as raw data and leaving only line 99 in the principles doc therefore drops the actual rationale the disposition itself identified as worth keeping. Fix: carry the 'coverage tracks perceived weight, not risk' sentence into the principles doc (or strengthen the line-99 restatement to include it) before relocating the matrix. Also confirmed the plan does not yet hold this matrix, so the move is net-new and the matrix data itself is only preserved if it actually lands there."
  },
  {
   "locator": "\u00a73 Figure V2, line 145",
   "excerpt": "**Figure V2 \u2014 The domain-bundle coverage matrix** is the table immediately above: only `claude-code` and `cicd` carry the full bundle; the all-\u274c `observability` row is the obvious gap. (Rendered as the table rather than a redundant heatmap \u2014 the cells already read at a glance.)",
   "type": "worked_example",
   "design_claim": "A coverage matrix makes the bundle gaps legible at a glance \u2014 the domains with the full bundle and the wholly-missing domain stand out immediately, which is why a table beats a redundant heatmap.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Figure V2 just labels the repo-specific coverage table (named domains claude-code/cicd/observability). Move with the table to the PLAN. Keep the figure-id 'V2' convention only if the principles doc still references it; otherwise it travels with the table. The general point (a matrix exposes bundle gaps at a glance) can be folded into the \u00a73 restatement if desired.",
   "preserved": false,
   "loss": "Figure V2 is a pure pointer to the coverage table plus one piece of generalizable rationale: 'a matrix exposes bundle gaps at a glance, which is why a table beats a redundant heatmap.' The disposition relocates the figure with the table to the plan (correct, since the figure has no meaning without the table) but leaves folding the matrix-reads-at-a-glance point into \u00a73 as OPTIONAL ('if desired'). That one clause is the only design-presentation rationale in the figure \u2014 it explains a deliberate choice (table over heatmap) and is reusable wherever the principles doc presents coverage. Leaving it optional risks dropping it entirely: the table moves to the plan, the figure moves with it, and the rationale evaporates because nobody is obligated to fold it anywhere. Secondary risk: if any surviving principles-doc text still says 'Figure V2,' that reference dangles once the table leaves. Marked not-preserved because the disposition does not guarantee the one generalizable claim survives; fix by making the 'a coverage matrix exposes bundle gaps at a glance' fold mandatory in the \u00a73 restatement rather than optional, and sweep for residual 'Figure V2' references."
  },
  {
   "locator": "\u00a718 Online vs offline evals, line 509",
   "excerpt": "*Offline* = the historical-run regression corpus (rollout WS-0), replayed against every change",
   "type": "named_run",
   "design_claim": "An offline regression corpus of historical runs is replayed against every change (new gate, contract bump, manifest edit, rubric/prompt change) to prove each iteration is concretely better, not merely different.",
   "action": "strip_keep_claim",
   "restatement": "Offline evaluation replays the historical-run regression corpus against every change to give a concrete before/after, so each iteration is proven better, not merely different.",
   "target_note": "'WS-0' is an intra-document work-stream id (foundational rollout work-stream) and is in the KEEP set; it is not a repo run-slug. 'historical-run regression corpus' is an abstract construct. Keep WS-0 reference as-is; no repo specifics to strip.",
   "preserved": false,
   "loss": "Real loss. The restatement silently drops the enumerated change-types the source spells out: 'a new gate, a contract bump, a manifest edit, a rubric/prompt change.' That list is a design constraint, not decoration \u2014 it defines the SCOPE of what triggers an offline replay (config/contract/manifest/rubric changes, i.e. the full substrate surface), and the disposition's own design_claim names all four. Collapsing them to 'every change' loses the reader's signal about which classes of change the regression corpus must guard. Since the disposition's stated rationale is that there is nothing repo-specific to strip here (it is a near no-op), the restatement should have been near-verbatim and kept the enumeration. Secondary concern: the disposition classifies 'rollout WS-0' as an intra-document KEEP, but WS-0 is actually a workstream label defined in this project's implementation-plan.md (WS-0 Substrate foundation), so in a codebase-agnostic principles doc it reads as a plan-specific reference, not pure intra-document structure (\u00a7N/Rn/figure) \u2014 its KEEP status is at least debatable. The primary, concrete defect is the dropped change-type enumeration."
  },
  {
   "locator": "\u00a722, line 586 \u2014 \"Terraform removes a provider only when every reference drops it; Kubernetes keeps an object \\\"Terminating\\\" until finalizers confirm cleanup\"",
   "excerpt": "This is the lesson the field is unanimous on \u2014 Terraform removes a provider only when every reference drops it; Kubernetes keeps an object \"Terminating\" until finalizers confirm cleanup and owner-references prevent orphans.",
   "type": "worked_example",
   "design_claim": "Removal must be a reconciliation across all install sites with a completion gate, not a delete \u2014 mirroring established infrastructure-tool practice (reference-counted teardown, finalizer-gated termination, owner-reference orphan prevention).",
   "action": "strip_keep_claim",
   "restatement": "Removal must be a reconciliation across every install site with a completion gate rather than a bare delete, mirroring the established discipline of reference-counted teardown and finalizer-gated termination in mature infrastructure tooling.",
   "preserved": false,
   "loss": "Minor but real loss in the restatement: the source names THREE distinct prior-art mechanisms \u2014 reference-counted teardown (Terraform), finalizer-gated termination (Kubernetes 'Terminating'), AND owner-references preventing orphans (Kubernetes). The restatement keeps the first two verbatim but DROPS 'owner-reference orphan prevention.' That third mechanism is not redundant with finalizers: finalizers gate completion, whereas owner-references are the structural device that makes orphan-prevention automatic on cascade \u2014 and orphan-prevention is precisely the \u00a722 lesson (the orphan check as completion gate). The disposition's own design_claim field correctly lists 'owner-reference orphan prevention' as part of the claim, but the restatement silently omits it. Mitigation: the worked example survives intact in the prior-art references section (governed-pipeline-architecture.md:1025, 'Kubernetes finalizers + owner-references'), so the citation is not vanished \u2014 but a \u00a722 reader relying on the restatement alone loses the third analogy that motivates orphan-prevention specifically. Recommend the restatement append '...and owner-reference-style orphan prevention.'"
  },
  {
   "locator": "\u00a723 Tool governance, line 606-608 \u2014 \"the tool *vocabulary* is canonical (`tools.yaml`) and MCP is itself a domain\"",
   "excerpt": "Today the tool *vocabulary* is canonical (`tools.yaml`) and MCP is itself a domain (its platform/design KBs + auditor), but the **operational tool lifecycle is ungoverned**",
   "type": "code_citation",
   "design_claim": "A tool vocabulary may already be declared canonically while the operational tool lifecycle (assignment, permission, load, init, availability, add/update/remove) remains ungoverned \u2014 the gap the pattern closes.",
   "action": "strip_keep_claim",
   "restatement": "Even where the set of available tools is already declared canonically, the operational tool lifecycle \u2014 which agent gets which tool, at what permission, how it loads and initializes, whether it is available in the agent's run context, and how it is added, updated, or removed \u2014 can remain ungoverned; that is the gap the pattern closes.",
   "preserved": false,
   "loss": "Real conceptual loss in the restatement. The restatement keeps 'the set of available tools is already declared canonically' (the tools.yaml-as-vocabulary fact) but DROPS the clause 'MCP is itself a domain (its platform/design KBs + auditor).' That dropped clause is load-bearing rationale, not a citation: it is the conceptual bridge that justifies the very next sentence's claim that the fix is 'the same machinery, not a new system' \u2014 MCP already exemplifies the domain-pack pattern (it has platform + design KBs and an auditor), so governing the tool lifecycle reuses an existing shape rather than inventing one. Strip it and the 'reuse, don't build new' argument loses its concrete anchor. Mitigation: the fact that MCP is a registered cross-cutting domain survives in the plan's BOM backfill (implementation-plan.md:222, 'the MCP cross-cutting domain') and in KB-mcp-design, so the substance is not destroyed \u2014 but the 'why this is reuse' rationale at the point of the claim is weakened. Recommend the restatement retain '(MCP itself is already governed as a domain \u2014 its platform/design KBs plus an auditor \u2014 so this reuses that shape).'"
  },
  {
   "locator": "\u00a729 'Two prerequisites' paragraph (line 789) \u2014 relocation companion",
   "excerpt": "the execution side already has schema'd result artifacts, stub detection, and dimensional verdicts; extract its proven gating into the shared harness",
   "type": "repo_metric",
   "design_claim": "The concrete reuse source for the shared gating harness is this repo's existing execution-pipeline gating.",
   "action": "relocate_to_plan",
   "restatement": "",
   "target_note": "Move the repo-specific identification of the execution pipeline as the existing mature gating implementation into the plan as a build instruction (extract the execution-side result-artifact/stub-detection/dimensional-verdict gating into the shared harness). The principles doc keeps only the abstract 'reuse the mature thing' rule.",
   "preserved": false,
   "loss": "INFORMATION LOSS RISK \u2014 the disposition's premise is partly false. It asserts the repo-specific 'execution pipeline is the existing mature gating implementation' should move to the plan as a build instruction, implying the plan is the safe home. But the plan does NOT currently carry this. implementation-plan.md mentions 'shared gate logic stays in auditing-shared' (lines 288, 355) and stubs the execution pipeline in the manifest (line 167), yet nowhere identifies the execution pipeline as the mature source whose schema'd result artifacts / stub detection / dimensional verdicts get EXTRACTED into the shared harness. The WS-0 corpus harness is even built against execution-pipeline-design-r1 (line 404), but that is a test-corpus use, not 'extract its gating.' So if this disposition is applied as a strip-from-architecture move WITHOUT actually writing the relocated build instruction into the plan, the concrete reuse source \u2014 the single most actionable 'how to build the shared harness' fact \u2014 vanishes entirely. The disposition is only non-lossy if the relocation is performed and verified in the plan (an explicit WS-0/WS-1 instruction: extract the execution-side result-artifact + stub-detection + dimensional-verdict gating into auditing-shared). Treat as a required, not optional, write. Flag to the editor."
  },
  {
   "locator": "\u00a730 Discipline paragraph (line 813)",
   "excerpt": "install persistent tools (validator deps, the observability backend + its log-shipper) in the **Dockerfile** (prebuild-captured, survives rebuild); start the observability backend **opt-in**, never on every session start, and never via docker-compose for a service not always run; the default machine is 4-core/16 GB (the `8 GB` in `hostRequirements` is a loose minimum, not the provisioned size \u2014 the smallest tier meeting the 4-core floor is 16 GB, per the GitHub Codespaces machine-types docs; resizable at proportional cost ...); forward the backend's port at **private** visibility; route any remote-backend secret through Codespaces Secrets with env-block indirection.",
   "type": "repo_metric",
   "design_claim": "Persistent tooling is baked into the prebuilt image so it survives rebuild; optional services start opt-in (never on every session, never via always-on multi-container orchestration); the machine footprint is a tunable cost dial with a single-container hard rule; service ports are forwarded privately; secrets use env-block / vault indirection, never inline.",
   "action": "strip_keep_claim",
   "restatement": "Install persistent tooling (validator dependencies, the observability backend and its log-shipper) into the prebuilt image so it survives rebuild; start optional services opt-in, never on every session and never as an always-on multi-container service; the machine footprint is a tunable cost dial, but single-container is the hard rule; forward service ports at private visibility; route any remote secret through env-block / vault indirection, never inline.",
   "target_note": "Strip platform/product specifics: 'Dockerfile', 'docker-compose', the concrete machine sizes (4-core/16 GB, 8 GB, the smallest-tier reasoning), the `hostRequirements` key, 'GitHub Codespaces machine-types docs', 'Codespaces Secrets'. Keep the abstract disciplines.",
   "preserved": false,
   "loss": "PARTIAL LOSS of a rationale. The source contains the causal clause 'keep the backend opt-in TO STAY ON THE CHEAP DEFAULT' \u2014 this is the WHY that ties the machine-sizing fact to the opt-in discipline (cost is the reason optional services must not start on every session). The item-3 restatement keeps both the 'opt-in' discipline and the 'footprint is a tunable cost dial' fact as separate statements, but DROPS the explicit causal link between them. Because item 4 relocates only the concrete sizing numbers to the plan (not this cost-causality clause), the 'opt-in to save cost' rationale risks falling through the gap between item 3 (which strips it) and item 4 (which doesn't claim it). Recommendation: add to the item-3 restatement an explicit connective such as 'start optional services opt-in \u2014 running them only when needed is what keeps the environment on its cheap default footprint.' All other disciplines in this paragraph (prebuilt-image persistence, single-container hard rule, private port visibility, env-block secret indirection) are preserved correctly."
  },
  {
   "locator": "Appendix C, D14 (line 989)",
   "excerpt": "Hybrid orchestrator \u2014 static topology (`pipelines.yaml`) declarative, dynamic routing in code; realized on **Claude Code Dynamic Workflows** (the script is the deterministic dispatcher reading the manifest \u2014 D-ORCH-1)",
   "type": "code_citation",
   "design_claim": "The orchestrator is hybrid: static topology declared in a manifest, dynamic routing in code, with a single deterministic dispatcher that reads the manifest.",
   "action": "strip_keep_claim",
   "restatement": "The orchestrator is hybrid: a declarative static-topology manifest plus dynamic routing in code, realized on a chosen orchestration substrate; the dispatcher script reads the manifest and routes deterministically (D-ORCH-1). The named substrate and the manifest filename are plan-level choices.",
   "preserved": false,
   "loss": "Information distortion, not pure loss, but a reader-facing defect. The restatement asserts 'The named substrate and the manifest filename are plan-level choices.' That is true for the manifest filename, but FALSE for the substrate per the architecture's own surviving text. D-PF-1 (line 1003, NOT in this disposition set, so it survives verbatim) explicitly names the orchestration substrate as an ARCHITECTURALLY-SIGNIFICANT binding: 'Within Claude Code, Dynamic Workflows is the named orchestration-substrate binding (D-ORCH-1)' and 'a platform swap is an architecture-level change.' D-ORCH-1 (line 1008) and TB7 (line 1078) reinforce this: the role is agnostic, but the specific engine binding is architectural. Demoting the substrate to 'plan-level' at D14 therefore contradicts D-PF-1/D-ORCH-1/TB7 inside the same principles doc and erases the deliberate role-agnostic/binding-architectural distinction the architecture draws. Fix: split the claim \u2014 the static-topology manifest FILENAME and the routing-code mechanics are plan-level, but the chosen orchestration SUBSTRATE is an architecturally-significant binding (cross-ref D-PF-1), changeable only at the architecture level. Strip the literal name 'Claude Code Dynamic Workflows' and the literal 'pipelines.yaml' filename (relocate both to the plan, where they already appear \u2014 implementation-plan.md lines 191-195), but do NOT relabel the substrate's architectural status."
  },
  {
   "locator": "Appendix F, TB1 row \u2014 boundary body, machine spec",
   "excerpt": "Default machine **4-core / 16 GB / 32 GB** (the smallest tier meeting the 4-core `hostRequirements` minimum; the `8 GB` floor is a loose minimum, not the provisioned size). Footprint is a **tunable cost dial** \u2014 resizable to 64\u2013128 GB at proportional $/hr.",
   "type": "repo_metric",
   "design_claim": "A single-container dev environment's footprint is an architecturally-tunable cost dial: a default tier is provisioned, but it can be scaled up within the platform's tier ceiling when justified and cost-flagged.",
   "action": "strip_keep_claim",
   "restatement": "Footprint is a tunable cost dial: a default machine tier is provisioned, and it may be resized upward within the platform's available tiers when the larger size is justified and the cost is flagged.",
   "target_note": "Drop the concrete tier numbers (4-core / 16 GB / 32 GB, 8 GB, 64\u2013128 GB), the `hostRequirements` config-key name, and the $/hr framing; the abstract cost-dial claim survives.",
   "preserved": false,
   "loss": "MILD loss, and it depends on whether \u00a730 is also being stripped. The abstract cost-dial claim survives cleanly. But three pieces of design-relevant 'why' are at risk if they vanish from the principles doc: (1) the rationale that the 8 GB hostRequirements value is a LOOSE MINIMUM not the provisioned size \u2014 this corrects a real misreading (someone could otherwise provision an 8 GB box and break the 4-core floor); the bare phrase 'a default tier is provisioned' does not carry that gotcha. (2) the constraint that the default is 'the smallest tier MEETING THE 4-CORE minimum' \u2014 the 4-core floor is an architectural requirement, not a cost preference, and the restatement drops the reason a specific tier is the floor. (3) the 'proportional $/hr' framing names HOW the cost dial costs (linear, not stepped) \u2014 'cost is flagged' loses the proportionality. Note these same details are duplicated in \u00a730 line 813, so if \u00a730 stays intact the document does not lose them outright; but Appendix F is the binding registry a plan-author reads to choose a machine, and the 4-core-floor + loose-8GB-minimum rationale is exactly the kind of constraint a plan-author needs at the point of choice. Recommend the restatement retain the abstracted rationale ('default = smallest tier meeting the platform's minimum core count; provisioned size exceeds the platform's loose memory floor; cost scales proportionally with size') even while dropping the literal numbers."
  }
 ],
 "counts": {
  "items": 203,
  "lossy": 12,
  "by_type": {
   "repo_metric": 36,
   "named_run": 18,
   "worked_example": 32,
   "code_citation": 79,
   "evidence_map": 21,
   "adr_as_evidence": 6,
   "evidenced_in_column": 11
  }
 }
}